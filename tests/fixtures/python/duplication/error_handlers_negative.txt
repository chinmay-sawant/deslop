def load_user(logger, path):
    try:
        return open(path).read()
    except FileNotFoundError:
        logger.warning("missing file")
        return "{}"


def load_cache(logger, path):
    try:
        return open(path).read()
    except ValueError as error:
        logger.error(error)
        raise RuntimeError("cache parse failed") from error