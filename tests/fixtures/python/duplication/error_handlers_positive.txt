def load_user(logger, path):
    try:
        return open(path).read()
    except Exception as error:
        logger.error(error)
        return "{}"


def load_cache(logger, path):
    try:
        return open(path).read()
    except Exception as error:
        logger.error(error)
        return "{}"