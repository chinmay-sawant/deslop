import logging

logger = logging.getLogger(__name__)


def authenticate(username: str, password: str) -> dict:
    user = find_user(username)
    if not user or user["password"] != hash_password(password):
        raise ValueError(f"Authentication failed for {username} with password {password}")
    return user


def connect_to_database(url: str) -> None:
    try:
        db.connect(url)
    except Exception as e:
        raise ConnectionError(f"Failed to connect to database at {url}: {e}")


def process_payment(card_number: str, amount: float) -> dict:
    try:
        result = charge(card_number, amount)
    except Exception as e:
        logger.error(f"Payment failed for card {card_number}: {e}")
        raise


def send_email(to: str, api_key: str, body: str) -> None:
    try:
        email_client.send(to=to, key=api_key, body=body)
    except Exception as e:
        raise RuntimeError(f"Email to {to} failed with key {api_key}: {e}")
