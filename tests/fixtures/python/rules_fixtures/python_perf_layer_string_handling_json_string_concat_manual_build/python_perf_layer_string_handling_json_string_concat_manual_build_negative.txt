import json


def build_json_array(items: list[dict]) -> str:
    return json.dumps(items)


def build_jsonl(records: list[dict]) -> str:
    return "\n".join(json.dumps(rec) for rec in records) + "\n"
