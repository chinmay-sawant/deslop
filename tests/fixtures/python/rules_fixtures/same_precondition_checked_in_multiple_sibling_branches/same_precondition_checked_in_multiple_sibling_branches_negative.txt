def process_request(request, action):
    if not request.user.is_authenticated:
        raise PermissionError("Not authenticated")

    if not request.user.has_perm(action):
        raise PermissionError(f"No {action} permission")

    if action == "create":
        return create_resource(request)
    elif action == "update":
        return update_resource(request)
    elif action == "delete":
        return delete_resource(request)
