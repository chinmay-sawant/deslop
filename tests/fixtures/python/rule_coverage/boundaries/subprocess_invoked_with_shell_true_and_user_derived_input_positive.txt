from __future__ import annotations

import subprocess


def run_user_command(user_value: str) -> str:
    cmd = f"echo {user_value}"
    return subprocess.check_output(cmd, shell=True, text=True).strip()
