from pydantic_settings import BaseSettings, SettingsConfigDict

class AppSettings(BaseSettings):
    # Correct: use a prefix to isolate app-specific configuration
    model_config = SettingsConfigDict(env_prefix="MYAPP_")
    api_key: str
    db_url: str
