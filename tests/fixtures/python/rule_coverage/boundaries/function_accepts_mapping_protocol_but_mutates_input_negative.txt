from __future__ import annotations

from collections.abc import Mapping


def inject_default_region(config: Mapping[str, str]) -> dict[str, str]:
    output = dict(config)
    output.setdefault("region", "us-east-1")
    return output
