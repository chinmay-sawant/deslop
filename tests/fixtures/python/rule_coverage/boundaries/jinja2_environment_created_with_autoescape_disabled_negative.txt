from jinja2 import Environment, select_autoescape

# Correct: Jinja2 environment created with autoescape enabled
env = Environment(autoescape=select_autoescape(['html', 'xml']))
template = env.from_string("Hello {{ name }}")
