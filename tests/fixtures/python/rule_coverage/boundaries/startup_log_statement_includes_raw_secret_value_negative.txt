import logging
from app.utils import mask_url

def start_app(config):
    # Correct: sensitive parts of the configuration are masked before logging
    masked_url = mask_url(config['DATABASE_URL'])
    logging.info(f"App starting with database URL: {masked_url}")
