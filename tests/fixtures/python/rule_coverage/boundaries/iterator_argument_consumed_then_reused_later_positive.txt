from __future__ import annotations

from collections.abc import Iterable


def summarize_rows(rows: Iterable[int]) -> tuple[int, int | None]:
    count = sum(1 for _ in rows)
    first = next(iter(rows), None)
    return count, first
