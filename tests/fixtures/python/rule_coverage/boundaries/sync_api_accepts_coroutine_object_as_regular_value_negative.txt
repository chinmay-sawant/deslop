from __future__ import annotations

import asyncio

store = {}


def put_value(name: str, value: object) -> None:
    if asyncio.iscoroutine(value):
        raise TypeError("coroutine passed to sync API")
    store[name] = value
