import asyncio

async def process_task(task_coro):
    # Correct: the API is async and properly awaits the coroutine
    result = await task_coro
    print(f"Task result: {result}")

async def my_task():
    return "done"

asyncio.run(process_task(my_task()))
