import os

def test_authentication():
    # Correct: test secrets are loaded from environment or placeholders
    api_key = os.getenv("TEST_API_KEY", "dummy-key-for-tests")
    client = AuthClient(api_key=api_key)
    assert client.login() is True
