def cors_config(environment: str) -> dict:
    if environment == "production":
        origins = ["https://app.example.com"]
    else:
        origins = ["*"]
    return {"allow_origins": origins, "allow_methods": ["GET", "POST"]}
