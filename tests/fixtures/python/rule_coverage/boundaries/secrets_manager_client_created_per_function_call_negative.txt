from __future__ import annotations

import boto3

secrets_client = boto3.client("secretsmanager")


def read_secret(name: str) -> dict[str, object]:
    return secrets_client.get_secret_value(SecretId=name)
