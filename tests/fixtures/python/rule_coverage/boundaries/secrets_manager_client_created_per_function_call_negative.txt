import boto3

# Correct: client created once and reused
secrets_client = boto3.client("secretsmanager")

def get_db_password():
    response = secrets_client.get_secret_value(SecretId="db_pass")
    return response["SecretString"]
