from __future__ import annotations

import copy


def materialize_items(template: dict[str, object], count: int) -> list[dict[str, object]]:
    base = copy.deepcopy(template)
    return [dict(base) for _ in range(count)]
