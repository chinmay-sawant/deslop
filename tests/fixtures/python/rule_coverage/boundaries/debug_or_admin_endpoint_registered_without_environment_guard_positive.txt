from flask import Flask

app = Flask(__name__)

# Violation: debug/admin endpoint registered without environment guard
@app.route("/admin/shell")
def interactive_shell():
    return "Secret Admin Shell"
