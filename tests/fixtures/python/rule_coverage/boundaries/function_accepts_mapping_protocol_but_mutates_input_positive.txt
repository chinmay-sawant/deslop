from __future__ import annotations

from collections.abc import Mapping


def inject_default_region(config: Mapping[str, str]) -> Mapping[str, str]:
    config["region"] = "us-east-1"
    return config
