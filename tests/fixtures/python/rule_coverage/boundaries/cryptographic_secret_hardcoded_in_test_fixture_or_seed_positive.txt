import hmac

SECRET_KEY = "super-secret-dev-key"


def sign_message(message: str) -> str:
    digest = hmac.digest(SECRET_KEY.encode("utf-8"), message.encode("utf-8"), "sha256")
    return digest.hex()
