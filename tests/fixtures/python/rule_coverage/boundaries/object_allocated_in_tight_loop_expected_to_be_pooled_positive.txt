from __future__ import annotations


class Formatter:
    def render(self, value: int) -> str:
        return f"item={value}"


def format_items(values: list[int]) -> list[str]:
    output = []
    for value in values:
        formatter = Formatter()
        output.append(formatter.render(value))
    return output
