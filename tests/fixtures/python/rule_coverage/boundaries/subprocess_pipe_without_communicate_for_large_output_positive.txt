from __future__ import annotations

import subprocess


def capture_lines() -> str:
    proc = subprocess.Popen(["python", "-c", "print('x' * 10000)"], stdout=subprocess.PIPE, text=True)
    return proc.stdout.read()
