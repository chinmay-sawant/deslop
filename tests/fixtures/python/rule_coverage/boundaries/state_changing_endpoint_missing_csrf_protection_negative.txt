from __future__ import annotations


def update_email(request: dict[str, str], store: dict[str, str], csrf_service) -> None:
    if not csrf_service.is_valid(request.get("csrf_token", "")):
        raise PermissionError("invalid csrf token")
    store[request["user_id"]] = request["email"]
