from __future__ import annotations


def numbers():
    yield from (1, 2, 3)


def summarize() -> tuple[int, int]:
    stream = numbers()
    first_total = sum(stream)
    second_total = sum(stream)
    return first_total, second_total
