import boto3

def get_db_password():
    # Violation: secrets manager client created per function call (inefficient)
    client = boto3.client("secretsmanager")
    response = client.get_secret_value(SecretId="db_pass")
    return response["SecretString"]
