import os
from flask import Flask

app = Flask(__name__)

# Correct: admin endpoint protected by an environment check
if os.getenv("ENV") == "development":
    @app.route("/admin/shell")
    def interactive_shell():
        return "Secret Admin Shell"
