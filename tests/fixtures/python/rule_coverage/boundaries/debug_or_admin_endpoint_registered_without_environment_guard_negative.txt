from __future__ import annotations

import os

routes = {}


def register_routes() -> None:
    routes["/health"] = lambda: "ok"
    if os.getenv("APP_ENV") == "development":
        routes["/admin/debug"] = lambda: "debug details"
