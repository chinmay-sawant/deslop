from __future__ import annotations


def numbers():
    yield from (1, 2, 3)


def summarize() -> tuple[int, int]:
    first_total = sum(numbers())
    second_total = sum(numbers())
    return first_total, second_total
