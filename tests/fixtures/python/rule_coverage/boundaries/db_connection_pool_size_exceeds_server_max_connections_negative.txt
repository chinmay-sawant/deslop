from __future__ import annotations

SERVER_MAX_CONNECTIONS = 50


def choose_pool_size(workers: int) -> int:
    requested = workers * 4
    return min(requested, SERVER_MAX_CONNECTIONS - 5)


POOL_SIZE = choose_pool_size(8)
