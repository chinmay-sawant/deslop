from jinja2 import Template

def render_greeting(user_input):
    # Correct: user input is passed as a template variable, not part of the template source
    template_str = "Hello {{ name }}!"
    return Template(template_str).render(name=user_input)
