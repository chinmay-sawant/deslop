from __future__ import annotations

import socket


def probe(host: str) -> bytes:
    with socket.create_connection((host, 80), timeout=2) as client:
        client.sendall(b"HEAD / HTTP/1.0\r\n\r\n")
        return client.recv(64)
