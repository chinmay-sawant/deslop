from __future__ import annotations


def log_config(logger, config: dict[str, str]) -> None:
    scrubbed = {k: ("***" if "secret" in k.lower() or "password" in k.lower() else v) for k, v in config.items()}
    logger.debug("config=%s", scrubbed)
