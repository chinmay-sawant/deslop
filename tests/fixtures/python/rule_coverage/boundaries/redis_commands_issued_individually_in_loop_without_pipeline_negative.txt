import redis

r = redis.Redis()

def save_items(items):
    # Correct: use a pipeline to batch redis commands for better performance
    with r.pipeline() as pipe:
        for key, value in items.items():
            pipe.set(key, value)
        pipe.execute()
