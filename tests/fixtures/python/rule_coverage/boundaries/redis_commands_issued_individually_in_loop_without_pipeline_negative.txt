from __future__ import annotations


def cache_metrics(client, metrics: dict[str, int]) -> None:
    pipe = client.pipeline()
    for key, value in metrics.items():
        pipe.set(key, value)
    pipe.execute()
