from __future__ import annotations

from pathlib import Path


def read_user_file(data_root: str, user_path: str) -> str:
    full_path = Path(data_root) / user_path
    return full_path.read_text(encoding="utf-8")
