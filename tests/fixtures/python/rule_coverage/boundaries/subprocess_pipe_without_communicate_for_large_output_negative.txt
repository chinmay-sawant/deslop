from __future__ import annotations

import subprocess


def capture_lines() -> str:
    proc = subprocess.Popen(["python", "-c", "print('x' * 10000)"], stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True)
    out, _err = proc.communicate(timeout=5)
    return out
