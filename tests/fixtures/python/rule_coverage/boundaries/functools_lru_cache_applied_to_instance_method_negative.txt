from functools import lru_cache

@lru_cache(maxsize=32)
def get_global_data(key):
    # Correct: lru_cache applied to a standalone function
    return fetch_from_db(key)

class DataProvider:
    def get_data(self, key):
        return get_global_data(key)
