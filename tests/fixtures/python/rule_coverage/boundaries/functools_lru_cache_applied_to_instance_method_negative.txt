from __future__ import annotations


class PriceService:
    def __init__(self) -> None:
        self._cache: dict[str, int] = {}

    def quote(self, sku: str) -> int:
        if sku not in self._cache:
            self._cache[sku] = len(sku) * 100
        return self._cache[sku]
