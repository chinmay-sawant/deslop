from __future__ import annotations


class Formatter:
    def render(self, value: int) -> str:
        return f"item={value}"


def format_items(values: list[int]) -> list[str]:
    formatter = Formatter()
    return [formatter.render(value) for value in values]
