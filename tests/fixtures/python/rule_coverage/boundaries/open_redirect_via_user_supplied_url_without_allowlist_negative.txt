from __future__ import annotations


def login_redirect(next_url: str) -> dict[str, str]:
    if not next_url.startswith("/"):
        raise ValueError("only relative redirects are allowed")
    return {"status": "302", "Location": next_url}
