import asyncpg

async def init_db():
    # Violation: pool size (100) likely exceeds default server max connections
    return await asyncpg.create_pool(
        dsn="postgres://user:pass@localhost/db",
        min_size=20,
        max_size=100
    )
