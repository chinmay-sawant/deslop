import json
from flask import Flask

app = Flask(__name__)
# Correct: load config once into app.config at startup
with open("app_config.json") as f:
    app.config.update(json.load(f))

@app.route("/settings")
def get_settings():
    return app.config["API_SETTINGS"]
