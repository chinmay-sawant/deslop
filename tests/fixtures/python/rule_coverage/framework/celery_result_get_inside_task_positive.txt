from celery import shared_task
from .other_tasks import sub_task

@shared_task
def parent_task():
    result = sub_task.delay()
    # Violation: calling .get() on a result inside a task (potential deadlock/resource exhaustion)
    return result.get()
