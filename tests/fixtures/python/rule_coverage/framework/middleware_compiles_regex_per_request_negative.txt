from __future__ import annotations

def middleware_compiles_regex_per_request_negative():
    token_re = re.compile(r"^Bearer [A-Za-z0-9._-]+$")

    def middleware(request):
        return bool(token_re.match(request.headers.get("authorization", "")))

    return middleware
