import re

# Correct: compile regex once at module level for efficiency
PUBLIC_API_PATTERN = re.compile(r"^/api/v[1-2]/public/.*")

class AuthMiddleware:
    def process_request(self, request):
        if PUBLIC_API_PATTERN.match(request.path):
            return None
        return check_auth(request)
