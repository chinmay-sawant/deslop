from __future__ import annotations

def sqlalchemy_create_engine_per_request_negative():
    engine = create_engine("sqlite:///app.db")

    def handler():
        with Session(engine) as session:
            return session.query(Account).count()

    return handler
