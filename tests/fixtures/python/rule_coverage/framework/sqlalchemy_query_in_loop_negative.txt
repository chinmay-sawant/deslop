from __future__ import annotations

def sqlalchemy_query_in_loop_negative():
    ids = [11, 12, 13]
    with Session(engine) as session:
        return session.query(User).filter(User.id.in_(ids)).all()
