from __future__ import annotations

def upstream_http_call_per_item_in_handler_positive():
    item_ids = [1, 2, 3]
    rows = []
    for item_id in item_ids:
        rows.append(requests.get(f"https://api.example.com/items/{item_id}", timeout=2.0).json())
    return rows
