import httpx
from fastapi import Depends

# Violation: dependency creates a new HTTP client instance per request (inefficient connection pooling)
def get_client():
    return httpx.Client()

@app.get("/data")
def read_data(client: httpx.Client = Depends(get_client)):
    return client.get("https://api.example.com").json()
