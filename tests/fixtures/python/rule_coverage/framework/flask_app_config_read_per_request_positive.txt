import json
from flask import Flask

app = Flask(__name__)

@app.route("/settings")
def get_settings():
    # Violation: application config file read from disk per request
    with open("app_config.json") as f:
        config = json.load(f)
    return config
