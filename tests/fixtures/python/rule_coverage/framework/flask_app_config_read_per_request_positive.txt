from __future__ import annotations

def flask_app_config_read_per_request_positive():
    def handler():
        timeout = current_app.config["UPSTREAM_TIMEOUT"]
        retries = current_app.config["UPSTREAM_RETRIES"]
        return timeout + retries

    return handler
