import os
from flask import Flask

app = Flask(__name__)

# Correct: use environment variables to control debug mode
DEBUG = os.getenv("FLASK_DEBUG", "False").lower() == "true"

if __name__ == "__main__":
    app.run(debug=DEBUG)
