from flask import render_template

def generate_report(items):
    # Correct: pass the collection to a template and use a loop inside the template engine
    return render_template("report.html", items=items)
