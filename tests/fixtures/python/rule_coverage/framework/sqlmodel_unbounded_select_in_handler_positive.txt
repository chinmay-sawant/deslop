from sqlmodel import select

@app.get("/users")
def get_users(session: Session = Depends(get_session)):
    # Violation: unbounded select in an API handler (can fetch too many rows)
    statement = select(User)
    return session.exec(statement).all()
