from __future__ import annotations

def django_queryset_evaluated_multiple_times_positive():
    queryset = Ticket.objects.filter(priority="high")
    first_pass = list(queryset)
    second_pass_ids = [ticket.id for ticket in queryset]
    return len(first_pass), second_pass_ids
