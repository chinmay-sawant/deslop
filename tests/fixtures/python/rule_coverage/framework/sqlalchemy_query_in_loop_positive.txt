from __future__ import annotations

def sqlalchemy_query_in_loop_positive():
    ids = [11, 12, 13]
    with Session(engine) as session:
        rows = []
        for row_id in ids:
            rows.append(session.query(User).filter(User.id == row_id).first())
    return rows
