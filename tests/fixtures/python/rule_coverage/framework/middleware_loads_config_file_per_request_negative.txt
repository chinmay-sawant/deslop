from __future__ import annotations

def middleware_loads_config_file_per_request_negative():
    rules = {"enabled": True}

    def middleware(request):
        return rules.get("enabled", False)

    return middleware
