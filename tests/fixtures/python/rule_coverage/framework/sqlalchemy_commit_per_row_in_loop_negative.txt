from __future__ import annotations

def sqlalchemy_commit_per_row_in_loop_negative():
    session = Session()
    for email in ["a@x.com", "b@x.com", "c@x.com"]:
        session.add(User(email=email))
    session.commit()
