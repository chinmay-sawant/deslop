from __future__ import annotations

def flask_global_db_connection_per_request_positive():
    def handler():
        conn = psycopg.connect("dbname=main")
        row = conn.execute("SELECT 1").fetchone()
        return row[0]

    return handler
