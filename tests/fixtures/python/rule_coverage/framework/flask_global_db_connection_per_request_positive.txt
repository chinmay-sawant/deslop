from flask import Flask

app = Flask(__name__)
# Violation: database connection created as a module-level global (not thread-safe/fork-safe for requests)
db = connect_db()

@app.route("/user")
def get_user():
    return db.query("SELECT ...")
