from __future__ import annotations

def middleware_loads_config_file_per_request_positive():
    def middleware(request):
        with open("/etc/service/rules.json", "r", encoding="utf-8") as fh:
            rules = json.load(fh)
        return rules.get("enabled", False)

    return middleware
