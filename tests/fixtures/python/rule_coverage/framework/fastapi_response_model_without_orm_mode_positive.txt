from pydantic import BaseModel

# Violation: response model without orm_mode (or from_attributes) when returning DB objects
class UserSchema(BaseModel):
    id: int
    username: str

@app.get("/user/{id}", response_model=UserSchema)
def get_user(id: int):
    return db.query(User).get(id) # Fails because User is not a dict
