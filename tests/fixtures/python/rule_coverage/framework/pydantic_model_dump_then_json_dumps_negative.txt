from pydantic import BaseModel

class User(BaseModel):
    id: int
    name: str

def serialize_user(user: User):
    # Correct: use the built-in model_dump_json() for direct serialization to JSON
    return user.model_dump_json()
