from __future__ import annotations

def middleware_creates_http_client_per_request_negative():
    client = httpx.Client(base_url="https://auth.internal")

    def middleware(request):
        resp = client.get("/introspect")
        return resp.status_code

    return middleware
