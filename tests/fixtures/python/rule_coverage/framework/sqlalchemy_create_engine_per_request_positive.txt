from __future__ import annotations

def sqlalchemy_create_engine_per_request_positive():
    def handler():
        engine = create_engine("sqlite:///app.db")
        with Session(engine) as session:
            return session.query(Account).count()

    return handler
