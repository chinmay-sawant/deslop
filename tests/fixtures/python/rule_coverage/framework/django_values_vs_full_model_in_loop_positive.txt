from __future__ import annotations

def django_values_vs_full_model_in_loop_positive():
    names = []
    for row in Product.objects.filter(is_live=True):
        names.append((row.id, row.name))
    return names
