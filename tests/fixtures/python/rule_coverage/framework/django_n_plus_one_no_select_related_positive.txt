from __future__ import annotations

def django_n_plus_one_no_select_related_positive():
    orders = Order.objects.filter(status="open")
    names = []
    for order in orders:
        names.append(order.customer.name)
    return names
