from sqlmodel import Session

def save_items(engine, items):
    # Violation: committing each item individually in a loop using SQLModel
    with Session(engine) as session:
        for item in items:
            session.add(item)
            session.commit()
