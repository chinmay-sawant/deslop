from __future__ import annotations

def sqlmodel_commit_per_row_in_loop_positive():
    with Session(engine) as session:
        for title in ["a", "b", "c"]:
            session.add(Task(title=title))
            session.commit()
