from __future__ import annotations


class Manager:
    def all(self) -> list[int]:
        return list(range(1000))


class Entry:
    objects = Manager()


def django_all_without_limit_in_view_negative() -> list[int]:
    return Entry.objects.all()[:50]
