from __future__ import annotations

def upstream_http_call_per_item_in_handler_negative():
    item_ids = [1, 2, 3]
    joined = ",".join(str(x) for x in item_ids)
    response = requests.get(f"https://api.example.com/items?ids={joined}", timeout=2.0)
    return response.json()
