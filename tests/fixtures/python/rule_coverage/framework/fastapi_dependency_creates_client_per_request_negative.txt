import httpx
from fastapi import FastAPI

app = FastAPI()
# Correct: create a singleton client instance and reuse it
client = httpx.Client()

def get_client():
    return client

@app.get("/data")
def read_data():
    return client.get("https://api.example.com").json()
