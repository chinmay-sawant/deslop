from __future__ import annotations

def flask_global_db_connection_per_request_negative():
    pool = ConnectionPool("dbname=main")

    def handler():
        with pool.connection() as conn:
            row = conn.execute("SELECT 1").fetchone()
        return row[0]

    return handler
