from flask import Flask, g

app = Flask(__name__)

def get_db():
    # Correct: use 'g' to manage database connections per-request
    if 'db' not in g:
        g.db = connect_db()
    return g.db

@app.teardown_appcontext
def close_db(e):
    db = g.pop('db', None)
    if db: db.close()
