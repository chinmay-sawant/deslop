import os
import typer

app = typer.Typer()

# Correct: lookup environment variables once at module level or in a callback
APP_ENV = os.getenv("APP_ENV", "dev")

@app.command()
def deploy():
    print(f"Deploying to {APP_ENV}")
