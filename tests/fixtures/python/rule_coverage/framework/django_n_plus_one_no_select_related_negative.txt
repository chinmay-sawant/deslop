from __future__ import annotations

def django_n_plus_one_no_select_related_negative():
    orders = Order.objects.select_related("customer").filter(status="open")
    return [order.customer.name for order in orders]
