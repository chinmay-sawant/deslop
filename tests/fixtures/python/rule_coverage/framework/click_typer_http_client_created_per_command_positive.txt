import httpx
import typer

app = typer.Typer()

@app.command()
def check_status():
    # Violation: HTTP client created per command invocation
    with httpx.Client() as client:
        resp = client.get("https://api.myapp.com/status")
        print(resp.json())
