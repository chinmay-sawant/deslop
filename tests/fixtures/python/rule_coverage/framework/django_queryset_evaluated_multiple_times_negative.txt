from __future__ import annotations

def django_queryset_evaluated_multiple_times_negative():
    queryset = Ticket.objects.filter(priority="high")
    rows = list(queryset)
    row_ids = [ticket.id for ticket in rows]
    return len(rows), row_ids
