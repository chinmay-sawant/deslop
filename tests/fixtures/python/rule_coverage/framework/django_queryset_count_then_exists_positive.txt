from __future__ import annotations

def django_queryset_count_then_exists_positive():
    pending = Invoice.objects.filter(state="pending")
    if pending.count() > 0 and pending.exists():
        return "has work"
    return "empty"
