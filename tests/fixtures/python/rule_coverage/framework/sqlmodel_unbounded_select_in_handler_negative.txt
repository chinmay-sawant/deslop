from __future__ import annotations

def sqlmodel_unbounded_select_in_handler_negative():
    with Session(engine) as session:
        rows = session.exec(select(AuditRow).limit(100)).all()
    return rows
