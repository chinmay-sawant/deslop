from sqlmodel import select

@app.get("/users")
def get_users(session: Session = Depends(get_session), limit: int = 100):
    # Correct: apply a limit to the select statement to bound the result set
    statement = select(User).limit(limit)
    return session.exec(statement).all()
