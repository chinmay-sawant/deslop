import httpx
import typer

app = typer.Typer()
# Correct: create a single client instance to be reused across commands
client = httpx.Client(base_url="https://api.myapp.com")

@app.command()
def check_status():
    resp = client.get("/status")
    print(resp.json())
