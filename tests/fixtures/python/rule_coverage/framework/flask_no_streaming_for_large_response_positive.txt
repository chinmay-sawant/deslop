from __future__ import annotations

def flask_no_streaming_for_large_response_positive():
    def download():
        lines = [f"row-{i}\n" for i in range(5000)]
        payload = "".join(lines)
        return Response(payload, mimetype="text/plain")

    return download
