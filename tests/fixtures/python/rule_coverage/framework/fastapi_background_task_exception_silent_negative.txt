from __future__ import annotations

def fastapi_background_task_exception_silent_negative():
    async def send_email_task(message_id: str):
        try:
            await email_client.send(message_id)
        except Exception as exc:
            logger.exception("background send failed: %s", exc)

    return send_email_task
