from __future__ import annotations

def django_raw_sql_in_loop_negative():
    user_ids = [10, 11, 12]
    with connection.cursor() as cursor:
        cursor.execute("SELECT id FROM users WHERE id = ANY(%s)", [user_ids])
        return cursor.fetchall()
