from __future__ import annotations

def sqlmodel_session_exec_in_loop_negative():
    ids = [1, 2, 3]
    with Session(engine) as session:
        stmt = select(User).where(User.id.in_(ids))
        return session.exec(stmt).all()
