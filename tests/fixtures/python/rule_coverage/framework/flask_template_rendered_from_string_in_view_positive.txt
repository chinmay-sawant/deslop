from flask import render_template_string

@app.route("/hello/<name>")
def hello(name):
    # Violation: rendering a template from a string in a view (XSS risk/inefficient)
    return render_template_string(f"<h1>Hello {name}</h1>")
