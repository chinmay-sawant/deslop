from __future__ import annotations

def sqlalchemy_n_plus_one_lazy_load_positive():
    with Session(engine) as session:
        orders = session.query(Order).all()
        return [order.customer.name for order in orders]
