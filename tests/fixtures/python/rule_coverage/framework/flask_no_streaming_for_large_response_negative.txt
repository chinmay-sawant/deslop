from __future__ import annotations

def flask_no_streaming_for_large_response_negative():
    def stream_rows():
        for i in range(5000):
            yield f"row-{i}\n"

    def download():
        return Response(stream_rows(), mimetype="text/plain")

    return download
