from __future__ import annotations

def sqlmodel_session_exec_in_loop_positive():
    ids = [1, 2, 3]
    with Session(engine) as session:
        users = []
        for user_id in ids:
            users.append(session.exec(select(User).where(User.id == user_id)).one())
    return users
