from __future__ import annotations

def django_queryset_count_then_exists_negative():
    pending = Invoice.objects.filter(state="pending")
    if pending.exists():
        return "has work"
    return "empty"
