from __future__ import annotations

from abc import ABC, abstractmethod


class Storage(ABC):
    @abstractmethod
    def save(self, item: str) -> None:
        raise NotImplementedError


def abstract_contracts_and_heavy_concrete_implementations_live_in_same_file_negative(
    storage: Storage, item: str
) -> None:
    storage.save(item)
