from __future__ import annotations

import requests


class ProfileClientPositive:
    def __init__(self, user_id: str) -> None:
        self.user_id = user_id
        self.profile = requests.get(f"https://example.com/users/{user_id}", timeout=2).json()
