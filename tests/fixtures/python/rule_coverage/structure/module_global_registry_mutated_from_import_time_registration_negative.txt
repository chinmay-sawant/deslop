# module: registry.py
class PluginRegistry:
    def __init__(self):
        self.plugins = {}
        
    def register(self, name, cls):
        self.plugins[name] = cls

# Correct: use explicit, runtime registration functions to populate state deterministically
def initialize_all_plugins(registry):
    from plugins import FastPlugin
    registry.register("fast", FastPlugin)
