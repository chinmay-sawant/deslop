# Violation: naming a class one thing but giving it responsibilities that entirely belong to another domain
class DataFetcher:
    def fetch_data(self): pass
    
    def delete_all_database_records(self):
        db.execute("DROP TABLE users")
