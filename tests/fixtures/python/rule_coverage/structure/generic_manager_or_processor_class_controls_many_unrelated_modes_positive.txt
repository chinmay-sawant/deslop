from __future__ import annotations


class GenericProcessor:
    def process(self, mode: str, payload: str) -> str:
        if mode == "email":
            return f"email:{payload}"
        if mode == "billing":
            return f"bill:{payload}"
        if mode == "analytics":
            return f"track:{payload}"
        return payload
