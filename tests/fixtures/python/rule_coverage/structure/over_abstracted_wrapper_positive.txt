# Violation: wrapping native primitives in custom classes without adding any new behavior is over-abstraction
class StringWrapper:
    def __init__(self, value: str):
        self._value = value
        
    def get_value(self) -> str:
        return self._value

def print_message(msg: StringWrapper):
    print(msg.get_value())
