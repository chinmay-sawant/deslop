from __future__ import annotations

from typing import Protocol


class StockGateway(Protocol):
    def reserve_stock(self, sku: str, qty: int) -> bool:
        ...


def bidirectional_import_between_feature_modules_negative(stock: StockGateway, sku: str) -> bool:
    return stock.reserve_stock(sku, 1)
