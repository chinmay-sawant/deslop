# Violation: passing a trivial operation through five abstraction layers makes the codebase impossible to trace
def view_handler():
    return Controller().dispatch()
    
class Controller:
    def dispatch(self): return Service().execute()
    
class Service:
    def execute(self): return Repo().get()
    
class Repo:
    def get(self): return "ok"
