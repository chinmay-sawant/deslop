# module: registry.py
PLUGINS = {}
def register(name):
    def decorator(cls):
        # Violation: mutating global state silently at import time via decorators makes execution order implicit and fragile
        PLUGINS[name] = cls
        return cls
    return decorator

# module: plugins.py
@register("fast")
class FastPlugin: pass
