from abc import ABC, abstractmethod
import boto3

# Violation: mixing clean abstract contracts with heavy, third-party-dependent concrete implementations in the same file
class IStorageService(ABC):
    @abstractmethod
    def save(self, data): pass

class AWSStorageService(IStorageService):
    def __init__(self):
        self.s3 = boto3.client('s3')
        
    def save(self, data):
        self.s3.put_object(Bucket="my-bucket", Key="data", Body=data)
