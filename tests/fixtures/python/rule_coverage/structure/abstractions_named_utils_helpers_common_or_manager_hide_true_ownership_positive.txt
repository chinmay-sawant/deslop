from __future__ import annotations


class Utils:
    def charge_card(self, cents: int) -> str:
        return f"charged:{cents}"

    def send_invoice(self, email: str) -> str:
        return f"sent:{email}"
