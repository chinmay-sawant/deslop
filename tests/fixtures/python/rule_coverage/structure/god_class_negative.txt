from __future__ import annotations


class OrderService:
    def parse_order(self, raw: str) -> dict[str, str]:
        key, value = raw.split("=", 1)
        return {key: value}


class NotificationService:
    def send_email(self, email: str) -> str:
        return f"sent:{email}"
