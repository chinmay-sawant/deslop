# Correct: strictly separate data retrieval (Queries) from side-effect execution (Commands)
def get_user(user_id):
    return db.query("SELECT * FROM users WHERE id = ?", user_id)

def send_password_reset(user):
    email_client.send(user.email, "Reset your password")
