from __future__ import annotations

from typing import Protocol


class SyncUserRepo(Protocol):
    def get(self, user_id: str) -> dict[str, str]:
        ...


class AsyncUserRepo(Protocol):
    async def get(self, user_id: str) -> dict[str, str]:
        ...
