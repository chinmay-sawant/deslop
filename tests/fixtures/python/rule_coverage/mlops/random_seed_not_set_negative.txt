from __future__ import annotations

import random

import numpy as np

random.seed(7)
np.random.seed(7)


def sample_pair() -> tuple[int, float]:
    return random.randint(1, 9), float(np.random.rand())
