import pandas as pd

def handle_data_upload(df):
    # Violation: printing an entire DataFrame in production pollutes logs and causes massive IO overhead
    print("Received dataframe:")
    print(df)
    return df.shape
