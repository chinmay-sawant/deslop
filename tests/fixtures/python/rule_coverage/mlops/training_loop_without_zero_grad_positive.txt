from __future__ import annotations

from typing import Any


def train_one_epoch(model: Any, optimizer: Any, batches: list[Any]) -> None:
    for batch in batches:
        loss = model(batch)
        loss.backward()
        optimizer.step()
