import numpy as np

def scale_array(arr):
    # Violation: iterating over a numpy array using a Python loop completely defeats vectorization
    for i in range(len(arr)):
        arr[i] = arr[i] * 2.5
    return arr
