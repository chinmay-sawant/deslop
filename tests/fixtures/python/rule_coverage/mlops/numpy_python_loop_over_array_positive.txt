from __future__ import annotations

import numpy as np


def l2_norm(values: np.ndarray) -> float:
    total = 0.0
    for value in values:
        total += float(value * value)
    return total ** 0.5
