from __future__ import annotations

from collections.abc import Iterable


def stream_to_sink(chunks: Iterable[str]) -> int:
    written = 0
    for chunk in chunks:
        written += len(chunk)
    return written
