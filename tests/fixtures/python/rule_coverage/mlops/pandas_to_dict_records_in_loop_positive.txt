from __future__ import annotations

import pandas as pd


def duplicate_records(df: pd.DataFrame, copies: int) -> list[list[dict[str, object]]]:
    payloads: list[list[dict[str, object]]] = []
    for _ in range(copies):
        payloads.append(df.to_dict("records"))
    return payloads
