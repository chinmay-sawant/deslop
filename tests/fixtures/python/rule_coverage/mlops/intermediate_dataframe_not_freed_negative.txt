from __future__ import annotations

import pandas as pd


def summarize(df: pd.DataFrame) -> pd.DataFrame:
    expanded = df.merge(df, on="id", suffixes=("_l", "_r"))
    result = expanded.groupby("id").size().reset_index(name="pairs")
    del expanded
    return result
