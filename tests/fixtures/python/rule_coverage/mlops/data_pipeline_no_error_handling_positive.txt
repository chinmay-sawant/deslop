from __future__ import annotations


def parse_scores(rows: list[dict[str, str]]) -> list[int]:
    scores: list[int] = []
    for row in rows:
        scores.append(int(row["score"]))
    return scores
