def process_data(records):
    results = []
    for record in records:
        # Violation: data pipeline lacks error handling; a single malformed record will crash the whole pipeline
        results.append(complex_transform(record))
    return results
