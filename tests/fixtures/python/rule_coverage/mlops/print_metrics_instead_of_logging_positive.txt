def train_epoch(model, dataloader):
    loss = run_epoch(model, dataloader)
    # Violation: using print for training metrics instead of a proper tracking framework
    print(f"Epoch finished. Loss: {loss}")
