import numpy as np

def collect_data(stream):
    # Correct: use a standard Python list for accumulation, then convert to numpy array once
    temp_list = []
    for value in stream:
        temp_list.append(value)
    return np.array(temp_list)
