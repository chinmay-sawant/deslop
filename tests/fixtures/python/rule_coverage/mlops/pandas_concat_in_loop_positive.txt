from __future__ import annotations

import pandas as pd


def merge_batches(frames: list[pd.DataFrame]) -> pd.DataFrame:
    merged = pd.DataFrame()
    for frame in frames:
        merged = pd.concat([merged, frame], ignore_index=True)
    return merged
