import torch
import numpy as np

def initialize_training():
    # Violation: failing to set global random seeds leads to non-reproducible training runs
    model = create_model()
    dataset = create_dataset()
    return model, dataset
