import torch
import gc

def run_experiments(configs):
    for config in configs:
        model = create_model(config).cuda()
        train_model(model)
        
        # Correct: explicitly delete the model and clear the CUDA cache to free up GPU memory
        del model
        gc.collect()
        torch.cuda.empty_cache()
