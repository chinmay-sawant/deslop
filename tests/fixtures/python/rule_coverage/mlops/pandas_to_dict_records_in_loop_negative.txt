from __future__ import annotations

import pandas as pd


def duplicate_records(df: pd.DataFrame, copies: int) -> list[list[dict[str, object]]]:
    records = df.to_dict("records")
    return [records for _ in range(copies)]
