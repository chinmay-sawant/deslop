from __future__ import annotations

from typing import Any


def train_and_log(metrics: list[float], tracker: Any) -> None:
    for step, metric in enumerate(metrics):
        if step % 10 == 0:
            tracker.log({"step": step, "loss": metric})
