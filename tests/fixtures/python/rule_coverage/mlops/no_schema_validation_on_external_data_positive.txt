import pandas as pd

def load_and_train():
    # Violation: reading external data for ML training without any schema or dtype validation
    df = pd.read_csv("external_dataset.csv")
    model.fit(df[["feature1", "feature2"]], df["target"])
