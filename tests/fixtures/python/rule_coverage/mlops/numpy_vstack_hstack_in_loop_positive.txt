from __future__ import annotations

import numpy as np


def stack_rows(rows: list[np.ndarray]) -> np.ndarray:
    matrix = np.empty((0, rows[0].shape[0]))
    for row in rows:
        matrix = np.vstack([matrix, row])
    return matrix
