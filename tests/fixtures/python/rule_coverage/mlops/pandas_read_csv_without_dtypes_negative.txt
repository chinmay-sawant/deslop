import pandas as pd

def load_data(filepath):
    # Correct: explicitly define memory-efficient dtypes when reading CSV files
    dtypes = {"user_id": "int32", "is_active": "bool", "score": "float32"}
    return pd.read_csv(filepath, dtype=dtypes)
