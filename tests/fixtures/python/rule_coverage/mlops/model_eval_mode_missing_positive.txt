import torch

def run_inference(model, input_tensor):
    # Violation: running inference without setting the model to evaluation mode (Dropout/BatchNorm behave incorrectly)
    with torch.no_grad():
        output = model(input_tensor)
    return output
