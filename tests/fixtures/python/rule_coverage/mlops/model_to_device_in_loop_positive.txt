from __future__ import annotations

import torch


def infer_many(model: torch.nn.Module, batches: list[torch.Tensor], device: str) -> None:
    for batch in batches:
        model.to(device)
        _ = model(batch.to(device))
