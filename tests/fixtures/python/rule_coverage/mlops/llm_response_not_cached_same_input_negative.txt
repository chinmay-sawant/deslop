from __future__ import annotations


class Client:
    def complete(self, prompt: str) -> str:
        return prompt[::-1]


_CACHE: dict[str, str] = {}


def get_response(prompt: str, client: Client) -> str:
    if prompt not in _CACHE:
        _CACHE[prompt] = client.complete(prompt)
    return _CACHE[prompt]
