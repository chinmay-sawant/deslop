from __future__ import annotations


class Client:
    def complete(self, prompt: str) -> str:
        return prompt.upper()


def classify(prompts: list[str], client: Client) -> list[str]:
    outputs: list[str] = []
    for prompt in prompts:
        outputs.append(client.complete(prompt))
    return outputs
