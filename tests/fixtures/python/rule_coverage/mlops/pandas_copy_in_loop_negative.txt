from __future__ import annotations

import pandas as pd


def scale_many(df: pd.DataFrame, multipliers: list[float]) -> list[pd.Series]:
    return [df["score"] * multiplier for multiplier in multipliers]
