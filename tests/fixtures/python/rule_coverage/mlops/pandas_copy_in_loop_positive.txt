from __future__ import annotations

import pandas as pd


def scale_many(df: pd.DataFrame, multipliers: list[float]) -> list[pd.DataFrame]:
    results: list[pd.DataFrame] = []
    for multiplier in multipliers:
        clone = df.copy()
        clone["score"] = clone["score"] * multiplier
        results.append(clone)
    return results
