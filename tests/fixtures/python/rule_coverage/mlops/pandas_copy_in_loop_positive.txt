import pandas as pd

def process_iterations(df, multipliers):
    results = []
    for m in multipliers:
        # Violation: performing a full DataFrame copy inside a loop is extremely memory and CPU intensive
        df_copy = df.copy()
        df_copy['value'] = df_copy['value'] * m
        results.append(df_copy)
    return results
