import numpy as np

def collect_data(stream):
    arr = np.array([])
    for value in stream:
        # Violation: using np.append inside a loop forces a full array copy every iteration (O(N^2))
        arr = np.append(arr, value)
    return arr
