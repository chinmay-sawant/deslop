from __future__ import annotations

import numpy as np


def build_vector(values: list[float]) -> np.ndarray:
    result = np.array([], dtype=float)
    for value in values:
        result = np.append(result, value)
    return result
