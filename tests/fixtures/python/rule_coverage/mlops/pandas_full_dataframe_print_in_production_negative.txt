from __future__ import annotations

import logging

import pandas as pd

logger = logging.getLogger(__name__)


def audit(df: pd.DataFrame) -> int:
    logger.info("rows=%d columns=%d", len(df), len(df.columns))
    return len(df)
