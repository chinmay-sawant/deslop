from __future__ import annotations

import torch


def run_experiments(experiments: list[int], model: torch.nn.Module) -> None:
    for seed in experiments:
        _ = model(torch.tensor([seed], dtype=torch.float32, device="cuda"))
