from __future__ import annotations

import pandas as pd


def score_sum(df: pd.DataFrame) -> float:
    total = 0.0
    for _, row in df.iterrows():
        total += float(row["score"])
    return total
