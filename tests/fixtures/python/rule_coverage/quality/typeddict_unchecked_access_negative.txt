from typing import TypedDict

class PartialConfig(TypedDict, total=False):
    retries: int
    timeout: float

def execute(config: PartialConfig):
    # Correct: always use .get() with a default value when accessing optional keys in a TypedDict
    retries = config.get("retries", 3)
    print(f"Retrying {retries} times")
