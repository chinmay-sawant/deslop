from __future__ import annotations

import asyncio


async def async_retry_sleep_without_backoff_positive(attempts: int) -> int:
    for _ in range(attempts):
        await asyncio.sleep(1)
    return attempts
