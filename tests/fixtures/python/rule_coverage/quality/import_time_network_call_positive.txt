import requests

# Violation: blocking the main thread with a network call during module import guarantees timeouts
INSTANCE_IP = requests.get("http://169.254.169.254/latest/meta-data/local-ipv4", timeout=2).text

def get_ip():
    return INSTANCE_IP
