import subprocess

def get_version():
    # Correct: execute subprocesses within functions, or better yet, read from a pre-built static file
    return subprocess.check_output(["git", "rev-parse", "HEAD"]).decode("utf-8").strip()
