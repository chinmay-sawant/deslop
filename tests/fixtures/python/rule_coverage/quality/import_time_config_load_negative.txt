import json

SYSTEM_CONFIG = None

def get_config():
    # Correct: lazy-load configs on the first request to allow testing frameworks to inject mocks
    global SYSTEM_CONFIG
    if SYSTEM_CONFIG is None:
        with open("/etc/config.json") as f:
            SYSTEM_CONFIG = json.load(f)
    return SYSTEM_CONFIG
