from dataclasses import dataclass

@dataclass
class ConfigOptions:
    host: str
    port: int

def setup():
    # Correct: use strict dataclasses to enforce shape, type checking, and documentation
    return ConfigOptions(host="localhost", port=8080)
