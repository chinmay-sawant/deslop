from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True)
class RenderOptions:
    theme: str
    page: str
    debug: bool = False


def option_bag_model_negative(options: RenderOptions) -> str:
    return f"{options.theme}:{options.page}:{options.debug}"
