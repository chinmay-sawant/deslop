from __future__ import annotations


def broad_except_used_to_mask_type_or_shape_bug_negative(payload: dict[str, object]) -> int:
    if "count" not in payload:
        raise KeyError("count is required")
    value = payload["count"]
    if not isinstance(value, int):
        raise TypeError("count must be an int")
    return value + 1
