from __future__ import annotations

import requests


def default_timeout_missing_on_external_boundary_wrapper_negative(url: str) -> int:
    response = requests.get(url, timeout=5.0)
    return response.status_code
