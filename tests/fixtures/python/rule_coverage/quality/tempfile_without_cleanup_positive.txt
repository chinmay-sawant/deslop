from __future__ import annotations

import tempfile


def tempfile_without_cleanup_positive(data: str) -> str:
    handle = tempfile.NamedTemporaryFile(delete=False, mode="w", encoding="utf-8")
    handle.write(data)
    handle.close()
    return handle.name
