import tempfile
import os

def process_data(data):
    # Violation: creating temporary files without using a context manager or explicit cleanup leaks inodes and disk space
    temp = tempfile.NamedTemporaryFile(delete=False)
    temp.write(data)
    temp.close()
    
    run_external_tool(temp.name)
    return "Done"
