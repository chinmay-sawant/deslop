# Violation: using an unbounded string parameter for a finite set of options prevents IDE autocomplete and type checking
def configure_logging(mode: str):
    if mode == "json":
        setup_json()
    elif mode == "text":
        setup_text()
    else:
        raise ValueError("Invalid mode")
