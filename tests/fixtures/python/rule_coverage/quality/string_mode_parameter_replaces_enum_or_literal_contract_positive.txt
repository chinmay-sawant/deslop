from __future__ import annotations


def string_mode_parameter_replaces_enum_or_literal_contract_positive(mode: str, value: int) -> str:
    if mode == "hex":
        return hex(value)
    if mode == "bin":
        return bin(value)
    return str(value)
