from __future__ import annotations

import asyncio


async def background_task_exception_unobserved_negative() -> str:
    async def fail_later() -> None:
        raise RuntimeError("boom")

    task = asyncio.create_task(fail_later())
    try:
        await task
    except RuntimeError:
        return "handled"
    return "ok"
