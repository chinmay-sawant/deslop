from __future__ import annotations


def duplicate_items_silently_dropped_without_contract_signal_negative(items: list[str]) -> list[str]:
    seen: set[str] = set()
    for item in items:
        if item in seen:
            raise ValueError(f"duplicate item: {item}")
        seen.add(item)
    return items
