from __future__ import annotations


def fallback_branch_swallows_invariant_violation_and_returns_plausible_default_negative(total: int, count: int) -> float:
    if count <= 0:
        raise ValueError("count must be positive")
    return total / count
