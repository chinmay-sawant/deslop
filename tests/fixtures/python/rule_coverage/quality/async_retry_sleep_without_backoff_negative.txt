from __future__ import annotations

import asyncio


async def async_retry_sleep_without_backoff_negative(attempts: int) -> int:
    for retry in range(attempts):
        await asyncio.sleep(0.2 * (2**retry))
    return attempts
