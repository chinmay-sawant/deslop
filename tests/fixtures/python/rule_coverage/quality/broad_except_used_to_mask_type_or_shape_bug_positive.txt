from __future__ import annotations


def broad_except_used_to_mask_type_or_shape_bug_positive(payload: dict[str, object]) -> int:
    try:
        return int(payload["count"]) + 1
    except Exception:
        return 0
