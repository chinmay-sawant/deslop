import redis

redis_client = None

def init_redis():
    # Correct: encapsulate client connections in an explicit init function to handle retries and lifecycles
    global redis_client
    redis_client = redis.Redis(host='localhost', port=6379, db=0)

def set_value(k, v):
    redis_client.set(k, v)
