from __future__ import annotations


def mutable_default_argument_negative(item: str, bucket: list[str] | None = None) -> list[str]:
    values = [] if bucket is None else list(bucket)
    values.append(item)
    return values
