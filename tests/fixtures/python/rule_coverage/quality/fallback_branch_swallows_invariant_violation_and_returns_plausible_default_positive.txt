from __future__ import annotations


def fallback_branch_swallows_invariant_violation_and_returns_plausible_default_positive(total: int, count: int) -> float:
    try:
        assert count > 0, "count must be positive"
        return total / count
    except AssertionError:
        return 0.0
