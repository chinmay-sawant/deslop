from __future__ import annotations


def mutable_module_global_state_negative(key: str, state: dict[str, int]) -> dict[str, int]:
    next_state = dict(state)
    next_state[key] = next_state.get(key, 0) + 1
    return next_state
