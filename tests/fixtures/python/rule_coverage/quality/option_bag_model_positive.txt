from __future__ import annotations


def option_bag_model_positive(options: dict[str, object]) -> str:
    theme = options.get("theme", "light")
    page = options.get("page", "home")
    debug = options.get("debug", False)
    return f"{theme}:{page}:{debug}"
