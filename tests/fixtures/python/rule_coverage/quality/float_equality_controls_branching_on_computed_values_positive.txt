def evaluate_threshold(computed_val):
    # Violation: using exact equality on floating-point numbers fails due to IEEE 754 precision errors
    if computed_val == 0.3:
        return "triggered"
    return "safe"
