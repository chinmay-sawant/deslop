from __future__ import annotations


def float_equality_controls_branching_on_computed_values_positive(values: list[float]) -> str:
    average = sum(values) / len(values)
    if average == 0.3:
        return "target-hit"
    return "target-miss"
