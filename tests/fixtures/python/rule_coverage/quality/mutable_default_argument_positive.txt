# Violation: using a mutable list as a default argument creates a shared reference across all function calls
def append_to_list(item, target_list=[]):
    target_list.append(item)
    return target_list
