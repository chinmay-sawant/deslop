from __future__ import annotations

import asyncio


async def async_lock_held_across_await_negative(lock: asyncio.Lock) -> None:
    async with lock:
        should_sleep = True
    if should_sleep:
        await asyncio.sleep(0.1)
