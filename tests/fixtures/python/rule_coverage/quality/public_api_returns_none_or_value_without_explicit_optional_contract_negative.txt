from __future__ import annotations


def public_api_returns_none_or_value_without_explicit_optional_contract_negative(name: str) -> str | None:
    directory = {"admin": "found", "owner": "found"}
    normalized = name.strip().lower()
    if normalized in directory:
        return directory[normalized]
    return None
