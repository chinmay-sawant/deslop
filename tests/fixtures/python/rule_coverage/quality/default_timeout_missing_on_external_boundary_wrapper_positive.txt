from __future__ import annotations

import requests


def default_timeout_missing_on_external_boundary_wrapper_positive(url: str) -> int:
    response = requests.get(url)
    return response.status_code
