from __future__ import annotations


def partial_update_mutates_input_before_validation_succeeds_positive(user: dict[str, object], patch: dict[str, object]) -> dict[str, object]:
    user.update(patch)
    age = user.get("age")
    if not isinstance(age, int) or age < 0:
        raise ValueError("invalid age")
    return user
