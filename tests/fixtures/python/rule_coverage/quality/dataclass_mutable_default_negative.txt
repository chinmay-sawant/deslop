from dataclasses import dataclass, field

@dataclass
class Inventory:
    # Correct: use field(default_factory=...) to instantiate a new mutable collection for each instance
    items: list = field(default_factory=list)
