from __future__ import annotations

import math


def float_equality_controls_branching_on_computed_values_negative(values: list[float]) -> str:
    average = sum(values) / len(values)
    if math.isclose(average, 0.3, rel_tol=1e-9):
        return "target-hit"
    return "target-miss"
