def provision_server(config):
    # Violation: executing expensive side effects before fully validating the input wastes time and money
    client = connect_to_aws()
    instance = client.launch_instance()
    
    if not config.get("server_name"):
        raise ValueError("Missing server_name")
        
    instance.set_name(config["server_name"])
