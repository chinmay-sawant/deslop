from __future__ import annotations

import subprocess


def validation_only_happens_after_expensive_side_effect_has_started_positive(job_id: str) -> str:
    proc = subprocess.Popen(["echo", "starting"], text=True)
    if not job_id.isdigit():
        raise ValueError("job_id must be digits")
    proc.wait()
    return job_id
