from __future__ import annotations


def sort_order_depends_on_non_explicit_mapping_iteration_semantics_negative(mapping: dict[str, int]) -> list[str]:
    return [f"{key}:{mapping[key]}" for key in sorted(mapping)]
