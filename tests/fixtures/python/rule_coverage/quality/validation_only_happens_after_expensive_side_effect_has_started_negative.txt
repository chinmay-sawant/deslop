from __future__ import annotations

import subprocess


def validation_only_happens_after_expensive_side_effect_has_started_negative(job_id: str) -> str:
    if not job_id.isdigit():
        raise ValueError("job_id must be digits")
    subprocess.run(["echo", f"starting:{job_id}"], check=True)
    return job_id
