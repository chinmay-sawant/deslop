def upload_view(request: dict) -> dict:
    path = f"/tmp/{request['name']}.txt"
    with open(path, "w", encoding="utf-8") as handle:
        handle.write(request["content"])
    return {"saved_to": path}
