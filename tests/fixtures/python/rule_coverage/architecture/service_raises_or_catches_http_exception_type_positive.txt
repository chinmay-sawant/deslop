from fastapi import HTTPException

class UserService:
    def get_user(self, id):
        user = self.repo.get(id)
        if not user:
            # Violation: domain service raising a transport-specific exception
            raise HTTPException(status_code=404, detail="User not found")
        return user
