class UserService:
    def __init__(self, user_repo):
        self.user_repo = user_repo

    def register_user(self, email):
        # Correct: domain service uses an injected repository (abstraction)
        return self.user_repo.save(email)
