# domain/user.py
import boto3  # Violation: domain model depends on infrastructure library

class User:
    def upload_avatar(self, file_path):
        s3 = boto3.client('s3')
        s3.upload_file(file_path, 'my-bucket', f'avatars/{self.id}')
