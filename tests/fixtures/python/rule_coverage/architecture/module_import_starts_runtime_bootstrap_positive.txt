# Violation: module import starts runtime bootstrap immediately
import threading

def background_sync():
    while True:
        print("Syncing...")
        time.sleep(60)

# Dangerous: thread starts on module import
t = threading.Thread(target=background_sync, daemon=True)
t.start()
