from fastapi import FastAPI
from .database import engine

app = FastAPI()

@app.on_event("startup")
async def startup():
    # Pool initialized
    app.state.db_pool = await create_pool()

# Violation: missing shutdown handler to close the pool
