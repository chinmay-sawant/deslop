from fastapi import FastAPI
from .models import UserEntity

app = FastAPI()

@app.get("/users/{id}")
def get_user(id: int):
    # Violation: exposing database entity directly
    user = UserEntity.get(id)
    return user
