class User(Base):
    __tablename__ = "users"
    id = Column(Integer, primary_key=True)
    score = Column(Integer)

# Logic is in a separate domain service or pure domain object
def get_credit_limit(user_score: int):
    if user_score > 700:
        return 5000
    return 1000
