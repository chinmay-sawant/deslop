class User:
    def __init__(self, user_id):
        self.id = user_id
        self.events = []

    def notify(self):
        # Correct: domain object records an event instead of performing IO
        self.events.append("UserNotified")
