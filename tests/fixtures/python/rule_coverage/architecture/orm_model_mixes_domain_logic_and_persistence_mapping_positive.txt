class User(Base):
    __tablename__ = "users"
    id = Column(Integer, primary_key=True)
    score = Column(Integer)

    # Violation: ORM model mixing business logic with persistence
    def calculate_credit_limit(self):
        if self.score > 700:
            return 5000
        return 1000
