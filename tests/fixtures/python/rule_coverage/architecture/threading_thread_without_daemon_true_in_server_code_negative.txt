import threading

def run_worker():
    while True:
        process_queue()

# Correct: background threads in server code should be daemonic
t = threading.Thread(target=run_worker, daemon=True)
t.start()
