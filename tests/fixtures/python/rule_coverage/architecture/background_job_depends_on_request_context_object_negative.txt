from celery import Celery

app = Celery('tasks')

@app.task
def send_email_task(user_email):
    # Safe: depending only on primitive data types
    send_email(user_email)

@route("/signup")
def signup():
    user_email = request.json['email']
    send_email_task.delay(user_email)
