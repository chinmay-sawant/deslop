class DatabaseConnector:
    def __init__(self, url):
        # Violation: setup() requires 'self.connected' which is set later
        self.setup()
        self.url = url
        self.connected = False

    def setup(self):
        if not self.connected:
            print(f"Connecting to {self.url}")
