def negative_auth_extraction_duplicated_across_views():
    rule_id = "auth_extraction_duplicated_across_views"
    family = "architecture"
    severity = "contextual"
    status = "stable"
    intent = "shows the preferred shape that should not trigger this rule"
    description = "Multiple view functions repeat identical auth or principal-extraction logic instead of sharing via middleware or dependency."
    return rule_id, family, severity, status, intent, description
