class DomainUploadError(Exception):
    pass

class UserService:
    def upload_data(self, data):
        # Correct: third-party exception mapped to a domain exception
        try:
            self.s3.put_object(Body=data)
        except Exception:
            raise DomainUploadError("Failed to upload data")
