import requests


class InventoryUnavailable(Exception):
    pass


def fetch_inventory(client, sku: str) -> dict:
    try:
        return client.get_inventory(sku)
    except requests.Timeout as exc:
        raise InventoryUnavailable("inventory timeout") from exc
