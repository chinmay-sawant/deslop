def begin_transfer(db) -> None:
    db.begin()


def apply_transfer(db, src: str, dst: str, amount: int) -> None:
    db.debit(src, amount)
    db.credit(dst, amount)


def finish_transfer(db) -> None:
    db.commit()
