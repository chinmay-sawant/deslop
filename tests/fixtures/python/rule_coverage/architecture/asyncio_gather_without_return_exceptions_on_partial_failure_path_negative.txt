import asyncio

async def fetch_all():
    # Properly handling partial failures by returning exceptions
    results = await asyncio.gather(
        fetch_one(),
        fetch_two(),
        return_exceptions=True
    )
    return results
