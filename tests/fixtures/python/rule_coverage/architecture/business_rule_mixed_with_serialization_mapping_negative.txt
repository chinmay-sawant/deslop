class Order:
    def get_total_with_tax(self):
        # Business rule in a dedicated domain method
        return self.amount * 1.15

def order_to_json(order: Order):
    # Pure serialization mapping
    return {
        "amount": order.amount,
        "total": order.get_total_with_tax()
    }
