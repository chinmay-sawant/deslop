-- SQL View (Negative Scenario)
CREATE VIEW active_users AS
SELECT id, name, subscription_end, login_count
FROM users;

# Python Domain Logic
class User:
    @property
    def is_premium(self):
        # Correct: business logic stays in the domain layer
        return self.subscription_end > datetime.now() and self.login_count > 50
