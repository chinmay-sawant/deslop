from __future__ import annotations

from common import everything


def monolithic_common_package_becomes_transitive_dependency_for_most_modules_positive(user_id: str) -> str:
    return everything.slugify(user_id)
