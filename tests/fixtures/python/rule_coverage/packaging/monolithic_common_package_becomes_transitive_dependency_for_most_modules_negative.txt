from __future__ import annotations

from common.strings import slugify


def monolithic_common_package_becomes_transitive_dependency_for_most_modules_negative(user_id: str) -> str:
    return slugify(user_id)
