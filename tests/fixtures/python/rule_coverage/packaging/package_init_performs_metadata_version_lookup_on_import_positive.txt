# my_package/__init__.py
import importlib.metadata

# Violation: performing disk IO metadata lookups at the root level slows down every import of this package
__version__ = importlib.metadata.version("my_package")
