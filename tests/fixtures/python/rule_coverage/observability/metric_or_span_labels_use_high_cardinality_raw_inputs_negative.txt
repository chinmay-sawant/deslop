from __future__ import annotations


def metric_or_span_labels_use_high_cardinality_raw_inputs_negative(email: str, order_id: str) -> dict[str, str]:
    domain = email.split("@")[-1]
    bucket = "long" if len(order_id) > 10 else "short"
    return {"email_domain": domain, "order_bucket": bucket}
