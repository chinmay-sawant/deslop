def execute_query(raw_sql):
    with tracer.start_as_current_span("db_query") as span:
        # Correct: extract stable, low-cardinality categories to use as metric labels
        query_type = raw_sql.split()[0].upper()
        span.set_attribute("query_type", query_type)
        db.execute(raw_sql)
