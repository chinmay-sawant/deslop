from __future__ import annotations


def list_pop_zero_used_as_queue_operation_positive(items: list[str]) -> list[str]:
    queue = list(items)
    drained: list[str] = []
    while queue:
        drained.append(queue.pop(0))
    return drained
