from __future__ import annotations

TERMINAL_STATES = frozenset({"queued", "running", "done"})


def frozenset_not_used_for_constant_membership_set_rebuilt_per_call_negative(status: str) -> bool:
    return status in TERMINAL_STATES
