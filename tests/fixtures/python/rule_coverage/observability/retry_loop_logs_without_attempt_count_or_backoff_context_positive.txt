import logging
import time

def call_unstable_api():
    for _ in range(3):
        try:
            return do_api_call()
        except Exception:
            # Violation: retry logs without attempt counts or backoff durations are impossible to debug in prod
            logging.warning("API call failed, retrying...")
            time.sleep(1)
