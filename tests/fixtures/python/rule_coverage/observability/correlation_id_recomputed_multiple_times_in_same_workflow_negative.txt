from __future__ import annotations

import uuid


def correlation_id_recomputed_multiple_times_in_same_workflow_negative() -> list[str]:
    correlation_id = str(uuid.uuid4())
    return [correlation_id, correlation_id, correlation_id]
