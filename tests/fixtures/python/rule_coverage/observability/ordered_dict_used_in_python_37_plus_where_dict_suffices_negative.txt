from __future__ import annotations


def ordered_dict_used_in_python_37_plus_where_dict_suffices_negative(keys: list[str]) -> dict[str, int]:
    out: dict[str, int] = {}
    for index, key in enumerate(keys):
        out[key] = index
    return out
