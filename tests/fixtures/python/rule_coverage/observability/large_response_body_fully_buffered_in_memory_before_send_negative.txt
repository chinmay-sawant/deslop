from __future__ import annotations

from collections.abc import Iterator


def large_response_body_fully_buffered_in_memory_before_send_negative(chunks: list[str]) -> Iterator[bytes]:
    for chunk in chunks:
        yield chunk.encode("utf-8")
