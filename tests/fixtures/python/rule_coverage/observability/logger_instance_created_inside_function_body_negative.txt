import logging

# Correct: initialize loggers once globally at the module scope
logger = logging.getLogger(__name__)

def process_data(data):
    logger.info(f"Processing {len(data)} items")
    return [d * 2 for d in data]
