from importlib.metadata import version

def get_server_info():
    # Violation: reading package metadata (disk IO) dynamically inside a request handler
    pkg_version = version("my_package")
    return {"status": "ok", "version": pkg_version}
