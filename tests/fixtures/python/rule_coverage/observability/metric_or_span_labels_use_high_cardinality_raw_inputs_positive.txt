def execute_query(raw_sql):
    with tracer.start_as_current_span("db_query") as span:
        # Violation: storing raw SQL queries or high-cardinality data as labels breaks time-series metric databases
        span.set_attribute("query", raw_sql)
        db.execute(raw_sql)
