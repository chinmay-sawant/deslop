from __future__ import annotations

import logging

LOGGER = logging.getLogger(__name__)


def synchronous_log_handler_or_flush_called_on_fast_path_positive() -> None:
    LOGGER.info("processed request")
    for handler in LOGGER.handlers:
        handler.flush()
