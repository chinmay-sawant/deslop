from __future__ import annotations

import importlib.util


def optional_library_import_checked_on_hot_code_path_positive(events: list[int]) -> int:
    total = 0
    for value in events:
        if importlib.util.find_spec("orjson") is not None:
            total += value
    return total
