import time

def process_data_with_metric():
    db = connect()
    
    # Correct: tightly wrap only the core operation to get accurate, actionable latency metrics
    start = time.time()
    result = run_heavy_algorithm()
    latency = time.time() - start
    statsd.timing("algorithm.duration", latency)
    
    db.close()
    return result
