from __future__ import annotations


def observability_context_extracted_manually_at_many_call_sites_positive(context: dict[str, str]) -> tuple[str, str]:
    trace = context.get("trace_id", "missing")
    correlation = context.get("correlation_id", "missing")
    return trace, correlation
