from __future__ import annotations

import logging

LOGGER = logging.getLogger(__name__)


def exception_log_omits_operation_identifier_or_input_summary_positive(payload: dict[str, object]) -> None:
    try:
        int(payload["order_id"])
    except Exception:
        LOGGER.exception("request failed")
