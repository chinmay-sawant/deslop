from __future__ import annotations

from collections import Counter


def manual_dict_increment_instead_of_counter_or_defaultdict_negative(events: list[str]) -> dict[str, int]:
    return dict(Counter(events))
