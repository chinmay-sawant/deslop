from __future__ import annotations


def metric_name_contains_dynamic_user_or_data_values_negative(user_id: str) -> tuple[str, dict[str, str]]:
    return "login.count", {"user_tier": "standard" if user_id.startswith("u") else "other"}
