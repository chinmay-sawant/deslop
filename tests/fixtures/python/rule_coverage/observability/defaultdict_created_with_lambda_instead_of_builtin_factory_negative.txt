from __future__ import annotations

from collections import defaultdict


def defaultdict_created_with_lambda_instead_of_builtin_factory_negative(tokens: list[str]) -> dict[str, int]:
    counts: defaultdict[str, int] = defaultdict(int)
    for token in tokens:
        counts[token] += 1
    return dict(counts)
