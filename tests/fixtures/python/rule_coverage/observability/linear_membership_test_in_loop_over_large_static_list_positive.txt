from __future__ import annotations

DENYLIST = [f"user-{index}" for index in range(5000)]


def linear_membership_test_in_loop_over_large_static_list_positive(users: list[str]) -> int:
    blocked = 0
    for user in users:
        if user in DENYLIST:
            blocked += 1
    return blocked
