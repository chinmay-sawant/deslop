from __future__ import annotations


def zip_range_len_used_instead_of_enumerate_negative(items: list[str]) -> list[str]:
    out: list[str] = []
    for index, value in enumerate(items):
        out.append(f"{index}:{value}")
    return out
