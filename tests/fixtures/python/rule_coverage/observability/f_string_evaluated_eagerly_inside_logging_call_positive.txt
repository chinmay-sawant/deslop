from __future__ import annotations

import logging

LOGGER = logging.getLogger(__name__)


def f_string_evaluated_eagerly_inside_logging_call_positive(user_id: int, count: int) -> None:
    LOGGER.info(f"user={user_id} processed={count}")
