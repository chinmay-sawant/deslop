import logging

logger = logging.getLogger(__name__)

def compute_engine():
    # Correct: guard expensive logging operations with isEnabledFor to save CPU cycles
    if logger.isEnabledFor(logging.DEBUG):
        logger.debug(f"Current matrix state: {compute_massive_serialization_string()}")
    run_heavy_math()
