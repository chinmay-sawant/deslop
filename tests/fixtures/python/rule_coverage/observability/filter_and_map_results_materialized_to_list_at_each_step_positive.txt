from __future__ import annotations


def filter_and_map_results_materialized_to_list_at_each_step_positive(values: list[int]) -> list[int]:
    evens = list(filter(lambda value: value % 2 == 0, values))
    doubled = list(map(lambda value: value * 2, evens))
    return doubled
