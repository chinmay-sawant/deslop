from __future__ import annotations


def manual_dict_increment_instead_of_counter_or_defaultdict_positive(events: list[str]) -> dict[str, int]:
    counts: dict[str, int] = {}
    for event in events:
        if event in counts:
            counts[event] = counts[event] + 1
        else:
            counts[event] = 1
    return counts
