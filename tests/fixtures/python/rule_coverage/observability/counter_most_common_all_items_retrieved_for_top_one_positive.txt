from __future__ import annotations

from collections import Counter


def counter_most_common_all_items_retrieved_for_top_one_positive(values: list[str]) -> tuple[str, int]:
    pairs = Counter(values).most_common()
    return pairs[0]
