from __future__ import annotations


def bulk_endpoint_partial_failure_contract_ambiguous_positive(items: list[str]) -> dict[str, object]:
    failed = [item for item in items if item.startswith("bad")]
    return {"processed": len(items), "errors": failed}
