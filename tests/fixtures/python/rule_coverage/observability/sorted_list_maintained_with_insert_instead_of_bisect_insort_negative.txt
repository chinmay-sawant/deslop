from __future__ import annotations

import bisect


def sorted_list_maintained_with_insert_instead_of_bisect_insort_negative(values: list[int]) -> list[int]:
    ordered: list[int] = []
    for value in values:
        bisect.insort(ordered, value)
    return ordered
