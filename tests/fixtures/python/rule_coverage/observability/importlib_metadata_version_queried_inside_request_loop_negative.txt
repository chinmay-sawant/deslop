from __future__ import annotations

from importlib.metadata import version

PIP_VERSION = version("pip")


def importlib_metadata_version_queried_inside_request_loop_negative(request_ids: list[str]) -> list[str]:
    return [f"{request_id}:{PIP_VERSION}" for request_id in request_ids]
