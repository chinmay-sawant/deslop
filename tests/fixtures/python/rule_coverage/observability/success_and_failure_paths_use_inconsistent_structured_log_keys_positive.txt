from __future__ import annotations


def success_and_failure_paths_use_inconsistent_structured_log_keys_positive(ok: bool) -> dict[str, str]:
    if ok:
        return {"op": "invoice.send", "status": "success"}
    return {"operation_name": "invoice.send", "result": "failure"}
