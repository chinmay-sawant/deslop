import structlog

logger = structlog.get_logger()

def authenticate(user):
    if user.is_valid():
        # Violation: using completely different key names for the exact same entity across code paths
        logger.info("auth_success", user_email=user.email)
    else:
        logger.warning("auth_failed", account_id=user.email)
