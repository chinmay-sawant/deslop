from __future__ import annotations

from collections import Counter


def counter_most_common_all_items_retrieved_for_top_one_negative(values: list[str]) -> tuple[str, int]:
    return Counter(values).most_common(1)[0]
