from __future__ import annotations

import logging

LOGGER = logging.getLogger(__name__)


def high_frequency_code_path_logs_without_sampling_or_rate_limit_positive(request_ids: list[str]) -> None:
    for request_id in request_ids:
        LOGGER.info("handled request_id=%s", request_id)
