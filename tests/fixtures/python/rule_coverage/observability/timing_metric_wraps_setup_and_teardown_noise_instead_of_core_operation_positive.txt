import time

def process_data_with_metric():
    start = time.time()
    # Violation: measuring setup, DB connections, and teardown noise alongside the core algorithmic operation
    db = connect()
    result = run_heavy_algorithm()
    db.close()
    
    latency = time.time() - start
    statsd.timing("algorithm.duration", latency)
    return result
