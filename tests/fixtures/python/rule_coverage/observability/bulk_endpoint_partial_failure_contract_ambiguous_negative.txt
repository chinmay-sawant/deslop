from __future__ import annotations


def bulk_endpoint_partial_failure_contract_ambiguous_negative(items: list[str]) -> dict[str, object]:
    failed = [item for item in items if item.startswith("bad")]
    succeeded = [item for item in items if item not in failed]
    return {"status": "partial_failure", "succeeded": succeeded, "failed": failed}
