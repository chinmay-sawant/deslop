from __future__ import annotations

import json
import logging

LOGGER = logging.getLogger(__name__)


def expensive_log_argument_built_without_is_enabled_guard_positive(payload: dict[str, object]) -> None:
    rendered = json.dumps(payload, sort_keys=True, indent=2)
    LOGGER.debug("payload=%s", rendered)
