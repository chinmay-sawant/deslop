from __future__ import annotations

import logging

LOGGER = logging.getLogger(__name__)


def logger_error_inside_except_without_exc_info_negative(raw_id: str) -> None:
    try:
        int(raw_id)
    except ValueError:
        LOGGER.error("invalid id", exc_info=True)
