from __future__ import annotations

import logging

LOGGER = logging.getLogger(__name__)


def logger_error_inside_except_without_exc_info_positive(raw_id: str) -> None:
    try:
        int(raw_id)
    except ValueError as exc:
        LOGGER.error("invalid id: %s", exc)
