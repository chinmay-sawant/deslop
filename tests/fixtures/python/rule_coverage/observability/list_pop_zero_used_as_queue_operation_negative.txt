from __future__ import annotations

from collections import deque


def list_pop_zero_used_as_queue_operation_negative(items: list[str]) -> list[str]:
    queue = deque(items)
    drained: list[str] = []
    while queue:
        drained.append(queue.popleft())
    return drained
