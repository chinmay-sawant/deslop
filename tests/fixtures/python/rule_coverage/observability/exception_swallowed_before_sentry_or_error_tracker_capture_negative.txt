import logging
import sentry_sdk

def process_user(user_data):
    try:
        db.save(user_data)
    except Exception as e:
        # Correct: explicitly send the captured exception to your error monitoring service
        logging.error(f"Save failed: {e}")
        sentry_sdk.capture_exception(e)
        return False
