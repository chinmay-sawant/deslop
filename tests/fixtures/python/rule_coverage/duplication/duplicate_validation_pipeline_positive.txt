from __future__ import annotations


def _validate_email_one(email: str) -> bool:
    return "@" in email and "." in email and len(email) >= 5


def duplicate_validation_pipeline_positive(email: str) -> bool:
    return "@" in email and "." in email and len(email) >= 5
