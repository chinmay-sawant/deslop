from __future__ import annotations


def _is_valid_email(email: str) -> bool:
    return "@" in email and "." in email and len(email) >= 5


def duplicate_validation_pipeline_negative(email: str) -> bool:
    return _is_valid_email(email)
