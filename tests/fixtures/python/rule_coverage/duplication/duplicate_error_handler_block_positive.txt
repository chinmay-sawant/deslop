from __future__ import annotations


def _a(raw: str) -> int:
    try:
        return int(raw)
    except ValueError:
        return -1


def duplicate_error_handler_block_positive(raw: str) -> int:
    try:
        return int(raw) * 2
    except ValueError:
        return -1
