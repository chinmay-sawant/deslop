from __future__ import annotations

BASE_QUERY = "SELECT id FROM users WHERE active = 1 AND deleted = 0"


def duplicate_query_fragment_negative() -> tuple[str, str]:
    one = f"{BASE_QUERY} ORDER BY id DESC"
    two = f"{BASE_QUERY} ORDER BY id ASC"
    return one, two
