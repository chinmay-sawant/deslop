from __future__ import annotations


def _to_code_one(raw: str) -> str:
    return raw.strip().lower().replace(" ", "_")


def duplicate_transformation_pipeline_positive(raw: str) -> str:
    return raw.strip().lower().replace(" ", "_")
