from __future__ import annotations


def _normalize_user_one(name: str) -> str:
    return name.strip().lower()


def duplicate_test_utility_logic_positive(name: str) -> str:
    return name.strip().lower()
