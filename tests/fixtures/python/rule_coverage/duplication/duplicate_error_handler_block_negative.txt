from __future__ import annotations


def _parse_or_default(raw: str) -> int:
    try:
        return int(raw)
    except ValueError:
        return -1


def duplicate_error_handler_block_negative(raw: str) -> int:
    return _parse_or_default(raw) * 2
