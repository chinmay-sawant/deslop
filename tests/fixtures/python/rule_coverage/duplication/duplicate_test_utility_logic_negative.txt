# tests/utils.py
def create_mock_user(name):
    return {"id": random.randint(1, 100), "name": name, "roles": []}

# test_auth.py
from tests.utils import create_mock_user

# test_billing.py
# Correct: test utility logic is centralized in a shared module
from tests.utils import create_mock_user
