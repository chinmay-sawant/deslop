from __future__ import annotations


def _normalize_user(name: str) -> str:
    return name.strip().lower()


def duplicate_test_utility_logic_negative(name: str) -> str:
    return _normalize_user(name)
