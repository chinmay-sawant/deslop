from __future__ import annotations


def _to_code(raw: str) -> str:
    return raw.strip().lower().replace(" ", "_")


def duplicate_transformation_pipeline_negative(raw: str) -> str:
    return _to_code(raw)
