from __future__ import annotations

def python_perf_layer_logging_overhead_log_message_formatted_before_level_check_positive(logger, user):
    message = f"user={user['id']} roles={','.join(user['roles'])}"
    if logger.isEnabledFor(20):
        logger.info(message)
