import json

CACHE: dict[str, str] = {}


def get_settings(account: str) -> dict[str, object]:
    raw = CACHE.get(account)
    if raw is None:
        raw = json.dumps({"plan": "pro", "account": account})
        CACHE[account] = raw
    return json.loads(raw)
