from __future__ import annotations

def python_perf_layer_logging_overhead_log_message_formatted_before_level_check_negative(logger, user):
    if logger.isEnabledFor(20):
        logger.info("user=%s roles=%s", user["id"], ",".join(user["roles"]))
