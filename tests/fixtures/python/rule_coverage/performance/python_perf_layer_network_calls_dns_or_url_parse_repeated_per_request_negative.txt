from __future__ import annotations

from urllib.parse import urlsplit


def python_perf_layer_network_calls_dns_or_url_parse_repeated_per_request_negative(base_url, paths):
    host = urlsplit(base_url).netloc
    return [host for _ in paths]
