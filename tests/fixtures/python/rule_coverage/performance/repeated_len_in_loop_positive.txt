from __future__ import annotations

def repeated_len_in_loop_positive(items):
    last_index_hits = 0
    for index in range(len(items)):
        if index == len(items) - 1:
            last_index_hits += 1
    return last_index_hits
