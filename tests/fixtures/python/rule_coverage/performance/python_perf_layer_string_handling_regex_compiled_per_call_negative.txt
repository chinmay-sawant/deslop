from __future__ import annotations

import re

TICKET_PATTERN = re.compile(r"^[A-Z]{3}-\d+$")


def python_perf_layer_string_handling_regex_compiled_per_call_negative(lines):
    return [bool(TICKET_PATTERN.match(line)) for line in lines]
