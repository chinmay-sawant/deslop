from __future__ import annotations

import inspect


def python_perf_layer_hot_path_optimization_reflection_inspection_in_hot_path_positive(func, rows):
    count = 0
    for row in rows:
        if "debug" in inspect.signature(func).parameters:
            count += 1
    return count
