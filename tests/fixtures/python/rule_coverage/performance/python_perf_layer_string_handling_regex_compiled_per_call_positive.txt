from __future__ import annotations

import re


def python_perf_layer_string_handling_regex_compiled_per_call_positive(lines):
    matcher = re.compile(r"^[A-Z]{3}-\d+$")
    return [bool(matcher.match(line)) for line in lines]
