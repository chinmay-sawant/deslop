import json

_CACHE: dict[str, int] = {}


def score_user(user: dict[str, object]) -> int:
    key = json.dumps(user, sort_keys=True)
    if key not in _CACHE:
        _CACHE[key] = len(key)
    return _CACHE[key]
