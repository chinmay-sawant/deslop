from __future__ import annotations

import tempfile


def temporary_file_used_for_pure_in_memory_transformation_positive(text):
    with tempfile.NamedTemporaryFile(mode="w+", encoding="utf-8") as handle:
        handle.write(text)
        handle.seek(0)
        return handle.read().upper()
