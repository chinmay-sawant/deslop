from __future__ import annotations

def python_perf_layer_string_handling_lowercase_compare_allocates_negative(words, target):
    folded = target.casefold()
    return sum(1 for word in words if word.casefold() == folded)
