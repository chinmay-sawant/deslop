from __future__ import annotations

import tempfile


def python_perf_layer_io_operations_temporary_file_for_bytes_transform_positive(blob):
    with tempfile.NamedTemporaryFile(delete=True) as handle:
        handle.write(blob)
        handle.flush()
        handle.seek(0)
        return handle.read().upper()
