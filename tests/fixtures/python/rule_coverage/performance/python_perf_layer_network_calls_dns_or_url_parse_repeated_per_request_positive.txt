from __future__ import annotations

from urllib.parse import urlsplit


def python_perf_layer_network_calls_dns_or_url_parse_repeated_per_request_positive(base_url, paths):
    hosts = []
    for path in paths:
        parsed = urlsplit(base_url + path)
        hosts.append(parsed.netloc)
    return hosts
