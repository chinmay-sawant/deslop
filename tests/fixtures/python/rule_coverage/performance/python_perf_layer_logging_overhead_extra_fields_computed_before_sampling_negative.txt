from __future__ import annotations

import hashlib


def python_perf_layer_logging_overhead_extra_fields_computed_before_sampling_negative(logger, payload, sample):
    if not sample:
        return
    extra = {"digest": hashlib.sha256(payload.encode("utf-8")).hexdigest()}
    logger.info("request accepted", extra=extra)
