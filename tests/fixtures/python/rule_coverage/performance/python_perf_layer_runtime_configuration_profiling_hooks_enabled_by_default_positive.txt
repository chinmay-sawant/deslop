from __future__ import annotations

def python_perf_layer_runtime_configuration_profiling_hooks_enabled_by_default_positive(values, hook):
    profiled = True
    total = 0
    for value in values:
        if profiled:
            hook(value)
        total += value
    return total
