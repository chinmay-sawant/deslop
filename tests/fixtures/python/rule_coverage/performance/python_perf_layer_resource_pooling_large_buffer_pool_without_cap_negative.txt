from __future__ import annotations

CAPPED_POOL = []
POOL_LIMIT = 8


def python_perf_layer_resource_pooling_large_buffer_pool_without_cap_negative(size):
    if len(CAPPED_POOL) >= POOL_LIMIT:
        CAPPED_POOL.pop(0)
    CAPPED_POOL.append(bytearray(size))
    return len(CAPPED_POOL)
