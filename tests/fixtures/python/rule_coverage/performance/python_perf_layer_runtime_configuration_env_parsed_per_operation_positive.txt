from __future__ import annotations

import os


def python_perf_layer_runtime_configuration_env_parsed_per_operation_positive(keys):
    result = []
    for key in keys:
        value = int(os.getenv("SERVICE_TIMEOUT_MS", "1000"))
        result.append((key, value))
    return result
