from __future__ import annotations

from dataclasses import asdict


def python_perf_layer_memory_allocation_dataclass_asdict_in_hot_path_positive(rows):
    return [asdict(row)["score"] for row in rows]
