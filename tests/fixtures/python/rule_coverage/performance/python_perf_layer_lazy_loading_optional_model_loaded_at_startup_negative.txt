from __future__ import annotations

def python_perf_layer_lazy_loading_optional_model_loaded_at_startup_negative(enabled, model=None):
    if not enabled:
        return 0
    model = [index for index in range(20_000)] if model is None else model
    return len(model)
