from __future__ import annotations

def python_perf_layer_string_handling_lowercase_compare_allocates_positive(words, target):
    matches = 0
    for word in words:
        if word.lower() == target.lower():
            matches += 1
    return matches
