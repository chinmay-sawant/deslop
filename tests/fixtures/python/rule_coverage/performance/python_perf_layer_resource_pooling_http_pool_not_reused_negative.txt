from __future__ import annotations

from urllib.request import build_opener

HTTP_OPENER = build_opener()


def python_perf_layer_resource_pooling_http_pool_not_reused_negative(urls):
    codes = []
    for url in urls:
        with HTTP_OPENER.open(url) as response:
            codes.append(response.status)
    return codes
