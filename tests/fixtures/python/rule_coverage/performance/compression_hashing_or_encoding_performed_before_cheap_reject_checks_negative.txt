import hashlib


def accept(payload: bytes) -> bool:
    if not payload.startswith(b"MSG:"):
        return False
    if len(payload) < 8:
        return False
    digest = hashlib.sha256(payload).hexdigest()
    return len(digest) == 64
