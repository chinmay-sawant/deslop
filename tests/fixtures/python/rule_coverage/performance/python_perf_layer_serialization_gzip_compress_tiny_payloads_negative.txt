from __future__ import annotations

import gzip


def python_perf_layer_serialization_gzip_compress_tiny_payloads_negative(payload):
    if len(payload) < 256:
        return payload
    return gzip.compress(payload)
