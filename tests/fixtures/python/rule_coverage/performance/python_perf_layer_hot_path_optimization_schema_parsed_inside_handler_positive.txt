from __future__ import annotations

import json


def python_perf_layer_hot_path_optimization_schema_parsed_inside_handler_positive(records, schema_text):
    valid = 0
    for record in records:
        schema = json.loads(schema_text)
        if set(schema["required"]).issubset(record):
            valid += 1
    return valid
