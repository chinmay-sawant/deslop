from __future__ import annotations

def python_perf_layer_garbage_collection_cleanup_large_list_cleared_but_retained_positive(values):
    big = list(values)
    leaked_alias = big
    big.clear()
    return len(leaked_alias)
