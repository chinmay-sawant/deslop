from __future__ import annotations

def python_perf_layer_resource_pooling_db_engine_created_per_repository_negative(conn, ids):
    total = 0
    cursor = conn.cursor()
    for item_id in ids:
        total += cursor.execute("SELECT ? + 1", (item_id,)).fetchone()[0]
    return total
