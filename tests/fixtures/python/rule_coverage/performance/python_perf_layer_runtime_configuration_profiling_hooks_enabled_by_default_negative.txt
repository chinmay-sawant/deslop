from __future__ import annotations

def python_perf_layer_runtime_configuration_profiling_hooks_enabled_by_default_negative(values, hook, profiled=False):
    total = 0
    for value in values:
        if profiled:
            hook(value)
        total += value
    return total
