from __future__ import annotations

def python_perf_layer_io_operations_read_entire_file_for_line_processing_negative(path):
    count = 0
    with open(path, "r", encoding="utf-8") as handle:
        for line in handle:
            if line.strip():
                count += 1
    return count
