from __future__ import annotations

def repeated_small_writes_without_buffering_or_join_negative(path, chunks):
    content = "".join(chunks)
    with open(path, "w", encoding="utf-8") as handle:
        handle.write(content)
