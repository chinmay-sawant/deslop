from __future__ import annotations

import ssl


def python_perf_layer_network_calls_tls_context_built_per_request_positive(hosts):
    contexts = []
    for host in hosts:
        context = ssl.create_default_context()
        contexts.append((host, context.minimum_version))
    return contexts
