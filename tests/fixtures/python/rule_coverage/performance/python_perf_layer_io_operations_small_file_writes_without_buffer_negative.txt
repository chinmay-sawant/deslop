from __future__ import annotations

def python_perf_layer_io_operations_small_file_writes_without_buffer_negative(path, chunks):
    with open(path, "a", encoding="utf-8") as handle:
        handle.write("".join(chunks))
