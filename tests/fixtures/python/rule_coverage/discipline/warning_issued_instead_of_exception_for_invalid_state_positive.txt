from __future__ import annotations

import warnings


def warning_issued_instead_of_exception_for_invalid_state_positive(state: str) -> str:
    if state not in {"open", "closed"}:
        warnings.warn("invalid state")
        return "warned"
    return state
