from __future__ import annotations


def shipping_fee(country: str, member: bool, subtotal: float) -> int:
    if country != "US":
        return 15
    if subtotal > 100:
        return 0 if member else 5
    return 3 if member else 8
