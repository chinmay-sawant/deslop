class Processor:
    def stage_one(self):
        self.data = "raw"

    def stage_two(self):
        # Violation: correctness depends on calling stage_one() first
        self.data = self.data.upper()

p = Processor()
p.stage_two() # Fails with AttributeError
