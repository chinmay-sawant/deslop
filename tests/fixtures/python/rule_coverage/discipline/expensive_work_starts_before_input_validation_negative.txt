from __future__ import annotations


def expensive_work_starts_before_input_validation_negative(limit: int) -> int:
    if limit < 0:
        raise ValueError("limit must be non-negative")
    values = [n * n for n in range(limit)]
    return sum(values)
