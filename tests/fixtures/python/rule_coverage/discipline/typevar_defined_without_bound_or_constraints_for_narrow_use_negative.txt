from __future__ import annotations

from typing import TypeVar

TStr = TypeVar("TStr", bound=str)


def typevar_defined_without_bound_or_constraints_for_narrow_use_negative(value: TStr) -> str:
    return value.lower()
