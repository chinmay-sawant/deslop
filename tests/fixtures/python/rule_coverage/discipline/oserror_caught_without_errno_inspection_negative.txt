from __future__ import annotations

import errno


def oserror_caught_without_errno_inspection_negative(path: str) -> str:
    try:
        with open(path, "r", encoding="utf-8") as handle:
            return handle.read()
    except OSError as exc:
        if exc.errno == errno.ENOENT:
            return "missing"
        raise
