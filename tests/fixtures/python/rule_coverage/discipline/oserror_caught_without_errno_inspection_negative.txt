import errno

def read_config(path):
    try:
        with open(path) as f:
            return f.read()
    except OSError as e:
        # Correct: inspect errno to handle specific file-not-found cases
        if e.errno == errno.ENOENT:
            return "{}"
        raise
