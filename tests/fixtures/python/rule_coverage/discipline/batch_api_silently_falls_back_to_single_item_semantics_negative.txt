from __future__ import annotations


def format_users(users: list[str]) -> list[str]:
    if not isinstance(users, list):
        raise TypeError("users must be a list[str]")
    return [name.strip().title() for name in users]
