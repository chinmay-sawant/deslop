from __future__ import annotations

from unittest.mock import patch


def test_function_stacks_too_many_mock_patch_decorators_negative() -> None:
    with patch("service.a") as mock_a:
        assert mock_a is not None
