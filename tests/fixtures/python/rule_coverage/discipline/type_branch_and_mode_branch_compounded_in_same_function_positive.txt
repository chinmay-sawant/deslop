from __future__ import annotations


def type_branch_and_mode_branch_compounded_in_same_function_positive(value: int | str, mode: str) -> str:
    if isinstance(value, int):
        if mode == "hex":
            return hex(value)
        return str(value)
    if mode == "upper":
        return value.upper()
    return value
