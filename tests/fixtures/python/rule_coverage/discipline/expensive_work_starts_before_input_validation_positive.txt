from __future__ import annotations


def expensive_work_starts_before_input_validation_positive(limit: int) -> int:
    values = [n * n for n in range(20000)]
    if limit < 0:
        raise ValueError("limit must be non-negative")
    return sum(values[:limit])
