from __future__ import annotations

from unittest.mock import Mock


def _read_count() -> int:
    return 1


def mock_return_value_is_incompatible_type_with_real_signature_negative() -> int:
    mocked = Mock(spec=_read_count, return_value=10)
    return mocked()
