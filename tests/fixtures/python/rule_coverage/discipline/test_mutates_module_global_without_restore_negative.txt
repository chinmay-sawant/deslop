from __future__ import annotations

FEATURE_ENABLED = False


def test_mutates_module_global_without_restore_negative() -> bool:
    global FEATURE_ENABLED
    old_value = FEATURE_ENABLED
    try:
        FEATURE_ENABLED = True
        return FEATURE_ENABLED
    finally:
        FEATURE_ENABLED = old_value
