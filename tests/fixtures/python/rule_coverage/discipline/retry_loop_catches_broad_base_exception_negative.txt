from __future__ import annotations


def retry_loop_catches_broad_base_exception_negative(fn) -> int:
    for _ in range(3):
        try:
            return fn()
        except TimeoutError:
            continue
    raise TimeoutError("all retries failed")
