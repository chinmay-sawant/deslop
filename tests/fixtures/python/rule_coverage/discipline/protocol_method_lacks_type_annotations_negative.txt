from __future__ import annotations

from typing import Protocol


class Writer(Protocol):
    def write(self, payload: str) -> int:
        ...


def protocol_method_lacks_type_annotations_negative() -> type[Protocol]:
    return Writer
