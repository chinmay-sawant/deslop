from __future__ import annotations


def generator_close_exception_not_handled_when_cleanup_required_positive():
    class Resource:
        def close(self) -> None:
            raise RuntimeError("close failed")

    resource = Resource()
    try:
        yield "item"
    finally:
        resource.close()
