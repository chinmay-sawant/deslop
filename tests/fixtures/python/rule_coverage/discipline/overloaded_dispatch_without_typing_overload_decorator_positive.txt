from typing import Union

# Violation: overloaded dispatch without using the @overload decorator
def process(val: Union[int, str]):
    if isinstance(val, int):
        return val * 2
    return val.upper()
