def get_user_id(user: User) -> int:
    return user.id

# Violation: mock return value is incompatible type (str) with real signature (int)
with patch("app.get_user_id") as mock:
    mock.return_value = "123"
    result = get_user_id(mock)
