from __future__ import annotations
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from .models import User

# Correct: use annotations and TYPE_CHECKING to handle forward references safely
def set_owner(owner: User):
    print(owner.name)
