from unittest import TestCase

class BaseTestCase(TestCase):
    def setUp(self):
        self.client = APIClient()
        self.db = MockDB()

class TestAuth(BaseTestCase):
    # Correct: common setup logic is extracted into a shared base class
    pass

class TestBilling(BaseTestCase):
    pass
