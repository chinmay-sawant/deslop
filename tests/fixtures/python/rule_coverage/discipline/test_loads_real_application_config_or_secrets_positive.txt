import os

def test_app_init():
    # Violation: test loads real application config or secrets (unsafe for CI)
    config_path = "/etc/myapp/prod.yaml"
    if os.path.exists(config_path):
        app.load_config(config_path)
