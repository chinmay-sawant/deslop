from __future__ import annotations


def oserror_caught_without_errno_inspection_positive(path: str) -> str:
    try:
        with open(path, "r", encoding="utf-8") as handle:
            return handle.read()
    except OSError:
        return "failed"
