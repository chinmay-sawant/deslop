from __future__ import annotations

from typing import Protocol


class Writer(Protocol):
    def write(self, payload):
        ...


def protocol_method_lacks_type_annotations_positive() -> type[Protocol]:
    return Writer
