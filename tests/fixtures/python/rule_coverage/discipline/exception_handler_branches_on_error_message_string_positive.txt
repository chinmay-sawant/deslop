from __future__ import annotations


def classify_error(func) -> str:
    try:
        func()
        return "ok"
    except ValueError as exc:
        if "timeout" in str(exc).lower():
            return "retry"
        return "fail"
