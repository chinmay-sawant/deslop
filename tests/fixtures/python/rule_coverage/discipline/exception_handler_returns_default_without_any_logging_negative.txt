from __future__ import annotations


def read_count(fetcher, logger) -> int:
    try:
        return int(fetcher())
    except Exception as exc:
        logger.warning("failed to read count: %s", exc)
        return 0
