from __future__ import annotations

from typing import Protocol


class Runner(Protocol):
    def run(self) -> None:
        ...


def protocol_used_in_isinstance_without_runtime_checkable_positive(obj: object) -> bool:
    return isinstance(obj, Runner)
