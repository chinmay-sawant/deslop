from __future__ import annotations


def optional_parameter_used_without_none_guard_negative(name: str | None = None) -> str:
    if name is None:
        return "guest"
    return name.strip().lower()
