# Violation: string forward reference in annotation not under a TYPE_CHECKING guard
def set_owner(owner: "User"):
    print(owner.name)

class User:
    def __init__(self, name):
        self.name = name
