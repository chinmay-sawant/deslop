from __future__ import annotations

import tempfile
from pathlib import Path


def integration_test_writes_state_without_cleanup_negative() -> bool:
    with tempfile.TemporaryDirectory() as tmp:
        path = Path(tmp) / "state.txt"
        path.write_text("state=clean", encoding="utf-8")
        return path.exists()
