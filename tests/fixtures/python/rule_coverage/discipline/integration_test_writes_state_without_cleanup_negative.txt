def test_create_user(db_session):
    # Correct: use a transaction or explicit cleanup to ensure state is reset
    user = db_session.create_user(name="test")
    try:
        assert user.id is not None
    finally:
        db_session.delete(user)
