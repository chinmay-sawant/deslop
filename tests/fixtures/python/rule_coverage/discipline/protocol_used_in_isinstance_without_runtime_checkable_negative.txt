from typing import Protocol, runtime_checkable

@runtime_checkable
class Drawable(Protocol):
    def draw(self): ...

def render(obj):
    # Correct: protocol is marked @runtime_checkable, allowing isinstance checks
    if isinstance(obj, Drawable):
        obj.draw()
