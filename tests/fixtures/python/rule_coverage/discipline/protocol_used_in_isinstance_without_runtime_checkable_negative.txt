from __future__ import annotations

from typing import Protocol, runtime_checkable


@runtime_checkable
class Runner(Protocol):
    def run(self) -> None:
        ...


def protocol_used_in_isinstance_without_runtime_checkable_negative(obj: object) -> bool:
    return isinstance(obj, Runner)
