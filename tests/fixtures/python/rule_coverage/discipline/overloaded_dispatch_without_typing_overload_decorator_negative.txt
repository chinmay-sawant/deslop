from typing import overload, Union

@overload
def process(val: int) -> int: ...

@overload
def process(val: str) -> str: ...

def process(val: Union[int, str]):
    # Correct: use @overload to provide better type checking for dispatch
    if isinstance(val, int):
        return val * 2
    return val.upper()
