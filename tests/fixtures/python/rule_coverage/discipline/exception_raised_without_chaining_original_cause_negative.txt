from __future__ import annotations


def exception_raised_without_chaining_original_cause_negative(raw: str) -> float:
    try:
        return float(raw)
    except ValueError as exc:
        raise RuntimeError("failed to parse number") from exc
