from __future__ import annotations

from dataclasses import dataclass


@dataclass
class ParseResult:
    kind: str
    value: str


def function_returns_multiple_unlabeled_shape_variants_negative(raw: str) -> ParseResult:
    kind = "json" if raw.startswith("{") else "text"
    return ParseResult(kind=kind, value=raw)
