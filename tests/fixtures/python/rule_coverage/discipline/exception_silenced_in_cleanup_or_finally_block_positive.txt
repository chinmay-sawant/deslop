from __future__ import annotations


def exception_silenced_in_cleanup_or_finally_block_positive() -> None:
    try:
        raise RuntimeError("work failed")
    finally:
        try:
            raise OSError("cleanup failed")
        except OSError:
            pass
