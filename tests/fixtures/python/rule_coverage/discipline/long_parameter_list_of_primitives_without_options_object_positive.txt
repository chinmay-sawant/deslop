def configure_system(host, port, timeout, retry_count, buffer_size, enable_logging, log_level):
    # Violation: long parameter list of primitives without an options object
    print(f"Connecting to {host}:{port}")
