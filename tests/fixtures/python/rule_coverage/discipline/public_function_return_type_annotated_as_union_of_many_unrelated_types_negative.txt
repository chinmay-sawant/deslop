from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True)
class ScoreResult:
    score: int
    label: str


def public_function_return_type_annotated_as_union_of_many_unrelated_types_negative(flag: int) -> ScoreResult:
    return ScoreResult(score=flag, label=f"flag-{flag}")
