from __future__ import annotations

import tempfile
from pathlib import Path


def integration_test_writes_state_without_cleanup_positive() -> bool:
    path = Path(tempfile.gettempdir()) / "worker3_state.txt"
    path.write_text("state=dirty", encoding="utf-8")
    return path.exists()
