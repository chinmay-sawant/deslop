from __future__ import annotations

from typing import cast


def user_id(value: object) -> int:
    if not isinstance(value, int):
        raise TypeError("value must be int")
    return cast(int, value)
