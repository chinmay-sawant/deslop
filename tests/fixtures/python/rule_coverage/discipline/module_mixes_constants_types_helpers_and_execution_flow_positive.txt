from __future__ import annotations

API_VERSION = "v1"


class Payload:
    def __init__(self, name: str) -> None:
        self.name = name


def _format_name(value: str) -> str:
    return value.title()


DEFAULT_PAYLOAD = Payload(_format_name("alpha"))
