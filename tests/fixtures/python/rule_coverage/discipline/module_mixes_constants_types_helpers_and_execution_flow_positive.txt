import os
DEBUG = True
class User: pass
def log(m): print(m)
# Violation: module mixes constants, types, helpers, and execution flow at the top level
if DEBUG:
    log("Starting")
def run():
    print("Running")
run()
