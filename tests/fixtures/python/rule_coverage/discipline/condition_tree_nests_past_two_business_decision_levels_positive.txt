from __future__ import annotations


def shipping_fee(country: str, member: bool, subtotal: float) -> int:
    if country == "US":
        if member:
            if subtotal > 100:
                return 0
            return 3
        if subtotal > 100:
            return 5
        return 8
    return 15
