from __future__ import annotations


def warning_issued_instead_of_exception_for_invalid_state_negative(state: str) -> str:
    if state not in {"open", "closed"}:
        raise ValueError(f"invalid state: {state}")
    return state
