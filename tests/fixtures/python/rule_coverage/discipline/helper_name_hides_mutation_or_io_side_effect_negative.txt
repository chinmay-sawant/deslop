from __future__ import annotations


def helper_name_hides_mutation_or_io_side_effect_negative(cache: dict[str, str], user_id: str) -> str:
    cache[user_id] = "loaded"
    return f"persisted:{user_id}"
