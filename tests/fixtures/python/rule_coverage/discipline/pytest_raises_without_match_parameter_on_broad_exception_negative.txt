import pytest

def test_failure():
    # Correct: use 'match' to ensure the expected specific error message is raised
    with pytest.raises(ValueError, match="invalid user id"):
        risky_call()
