from __future__ import annotations

import pytest


def pytest_raises_without_match_parameter_on_broad_exception_negative() -> None:
    with pytest.raises(ValueError, match="invalid literal"):
        int("x")
