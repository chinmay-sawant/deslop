from __future__ import annotations


def commit_changes(unit_of_work, logger) -> None:
    try:
        unit_of_work.commit()
    except Exception:
        logger.exception("commit failed")
        raise
