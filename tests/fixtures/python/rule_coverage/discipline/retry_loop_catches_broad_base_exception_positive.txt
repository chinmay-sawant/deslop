from __future__ import annotations


def retry_loop_catches_broad_base_exception_positive(fn) -> int:
    for _ in range(3):
        try:
            return fn()
        except Exception:
            continue
    return -1
