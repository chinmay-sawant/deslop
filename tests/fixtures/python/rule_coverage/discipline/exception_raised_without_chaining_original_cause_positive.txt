from __future__ import annotations


def exception_raised_without_chaining_original_cause_positive(raw: str) -> float:
    try:
        return float(raw)
    except ValueError:
        raise RuntimeError("failed to parse number")
