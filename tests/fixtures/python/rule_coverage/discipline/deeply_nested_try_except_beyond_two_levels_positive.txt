from __future__ import annotations


def parse_payload(payload: str) -> int:
    try:
        try:
            try:
                return int(payload.strip())
            except ValueError:
                return -1
        except Exception:
            return -2
    except Exception:
        return -3
