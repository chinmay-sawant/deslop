from __future__ import annotations


def validation_or_parse_error_mapped_to_500_status_negative(raw: str) -> tuple[int, str]:
    try:
        int(raw)
    except ValueError:
        return (400, "invalid input")
    return (200, "ok")
