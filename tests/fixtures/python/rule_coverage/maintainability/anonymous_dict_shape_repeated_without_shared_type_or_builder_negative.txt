from __future__ import annotations

from dataclasses import dataclass

@dataclass(frozen=True)
class PersonState:
    id: int
    name: str
    active: bool

def anonymous_dict_shape_repeated_without_shared_type_or_builder_negative():
    return [PersonState(7, "Ari", True), PersonState(8, "Bo", False)]
