def is_ready_for_shipping(order):
    return order.status == OrderStatus.PROCESSED and order.payment_cleared and not order.is_backordered

def process_order(order):
    # Correct: encapsulate complex conditions in a well-named helper function
    if is_ready_for_shipping(order):
        ship_order(order)
