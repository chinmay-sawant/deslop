from __future__ import annotations

def comment_required_to_explain_opaque_branching_that_code_could_express_negative(balance: int, payment: int):
    adjusted = balance - payment
    has_exact_settlement = adjusted == 0
    return "ok" if has_exact_settlement else "skip"
