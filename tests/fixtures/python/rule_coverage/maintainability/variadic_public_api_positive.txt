def initialize_service(*args, **kwargs):
    # Violation: using variadic arguments in a public API hides the actual signature and dependencies
    host = kwargs.get("host", "localhost")
    port = kwargs.get("port", 8080)
    setup_connection(host, port)
