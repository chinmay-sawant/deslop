def process_value(val):
    # Violation: comparing to None using equality operators instead of identity operators
    if val == None:
        return "Empty"
    if val != None:
        return val * 2
