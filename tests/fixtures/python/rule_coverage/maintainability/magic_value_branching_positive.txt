from __future__ import annotations

def magic_value_branching_positive(code: str):
    if code == "A":
        return "approved"
    if code == "R":
        return "rejected"
    if code == "H":
        return "on_hold"
    return "unknown"
