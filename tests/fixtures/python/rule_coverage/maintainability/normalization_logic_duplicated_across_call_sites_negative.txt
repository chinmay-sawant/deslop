from __future__ import annotations


def normalize_text(raw: str) -> str:
    cleaned = raw.strip().lower()
    return " ".join(cleaned.split())


def format_username(raw: str) -> str:
    return normalize_text(raw)


def format_city(raw: str) -> str:
    return normalize_text(raw)
