from pydantic import BaseModel, EmailStr

class UserCreate(BaseModel):
    email: EmailStr
    name: str

def create_user(request_data: dict):
    # Correct: validate external input using a robust schema library before processing
    user = UserCreate(**request_data)
    db.execute("INSERT INTO users (email, name) VALUES (?, ?)", (user.email, user.name))
