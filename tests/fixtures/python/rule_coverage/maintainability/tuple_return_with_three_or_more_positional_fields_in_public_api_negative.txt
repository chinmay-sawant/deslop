from __future__ import annotations

from dataclasses import dataclass


@dataclass
class UserProfile:
    user_id: int
    name: str
    role: str


def get_user_profile(user_id: int) -> UserProfile:
    return UserProfile(user_id=user_id, name="Ana", role="admin")
