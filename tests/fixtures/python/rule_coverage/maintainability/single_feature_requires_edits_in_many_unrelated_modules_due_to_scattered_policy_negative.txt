from __future__ import annotations


def has_priority_access(plan: str) -> bool:
    return plan == "premium"


def checkout_message(plan: str) -> str:
    return "priority checkout" if has_priority_access(plan) else "standard checkout"


def support_message(plan: str) -> str:
    return "priority support" if has_priority_access(plan) else "normal support"
