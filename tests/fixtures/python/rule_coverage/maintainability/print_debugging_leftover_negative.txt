import logging

logger = logging.getLogger(__name__)

def calculate_pricing(base, tax):
    # Correct: use the logging module with appropriate severity levels
    logger.debug("Calculating pricing with base: %s, tax: %s", base, tax)
    total = base + tax
    return total
