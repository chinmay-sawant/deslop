from __future__ import annotations

def exception_swallowed_negative(raw_value: str):
    try:
        return int(raw_value)
    except ValueError as exc:
        raise ValueError(f"invalid integer: {raw_value}") from exc
