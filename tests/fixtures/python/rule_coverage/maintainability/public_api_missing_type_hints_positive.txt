def fetch_user_record(user_id, include_deleted):
    # Violation: public API function lacking type hints, reducing maintainability and IDE support
    record = db.query(user_id)
    if include_deleted or not record.is_deleted:
        return record
    return None
