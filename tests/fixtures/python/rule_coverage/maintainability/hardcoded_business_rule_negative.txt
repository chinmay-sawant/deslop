DISCOUNT_THRESHOLD = 1000
PREMIUM_DISCOUNT_RATE = 0.15

def calculate_discount(order):
    # Correct: extract business rules into configurable constants or settings
    if order.total > DISCOUNT_THRESHOLD:
        return order.total * PREMIUM_DISCOUNT_RATE
    return 0
