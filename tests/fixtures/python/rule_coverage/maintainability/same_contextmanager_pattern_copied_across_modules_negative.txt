from contextlib import contextmanager

@contextmanager
def db_transaction():
    # Correct: encapsulate repeated setup/teardown logic in a reusable context manager
    db.connect()
    try:
        yield
    finally:
        db.disconnect()

# module_a.py
def process_data():
    with db_transaction():
        db.execute("...")
