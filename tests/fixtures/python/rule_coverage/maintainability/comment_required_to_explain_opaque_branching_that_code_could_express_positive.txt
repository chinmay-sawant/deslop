from __future__ import annotations

def comment_required_to_explain_opaque_branching_that_code_could_express_positive(x: int, y: int):
    if (x * 3 - y * 2) % 5 == 0:
        return "ok"
    return "skip"
