from __future__ import annotations

SETTINGS = {"mode": "dev"}

def hidden_dependency_arrives_via_import_time_side_effect_positive():
    import bootstrap_runtime_settings  # expected to mutate SETTINGS at import time
    return SETTINGS.get("mode", "dev")
