import socket

def connect_to_server(host, port):
    # Correct: always set a timeout before initiating a network connection
    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    s.settimeout(5.0)
    s.connect((host, port))
    return s
