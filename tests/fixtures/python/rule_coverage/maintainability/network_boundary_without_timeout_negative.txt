from __future__ import annotations

import urllib.request


def fetch_bytes(url: str, timeout_seconds: float = 2.0) -> bytes:
    with urllib.request.urlopen(url, timeout=timeout_seconds) as response:
        return response.read()
