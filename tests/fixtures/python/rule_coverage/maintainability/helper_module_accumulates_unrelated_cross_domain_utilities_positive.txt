from __future__ import annotations

def helper_module_accumulates_unrelated_cross_domain_utilities_positive(email: str, amount: float):
    local, domain = email.split("@", 1)
    invoice_total = round(amount * 1.2, 2)
    return {"email_domain": domain, "invoice_total": invoice_total, "name": local}
