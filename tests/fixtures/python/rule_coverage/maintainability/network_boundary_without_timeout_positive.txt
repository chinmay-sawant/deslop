import socket

def connect_to_server(host, port):
    # Violation: crossing a network boundary without setting a timeout (can hang indefinitely)
    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    s.connect((host, port))
    return s
