# app/main.py
# Violation: importing from a massive, monolithic utils package that drags in unrelated dependencies
from app.utils import parse_date, calculate_md5, init_db_connection

def process():
    parse_date("2023-01-01")
    calculate_md5("data")
