from __future__ import annotations

UNKNOWN_REGION = "UNKNOWN"


def parse_region(raw: str) -> str:
    region = raw.strip() or UNKNOWN_REGION
    if region == UNKNOWN_REGION:
        return UNKNOWN_REGION
    return region
