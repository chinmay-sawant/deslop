from __future__ import annotations


def checkout_message(plan: str) -> str:
    if plan == "premium":
        return "priority checkout"
    return "standard checkout"


def support_message(plan: str) -> str:
    if plan == "premium":
        return "priority support"
    return "normal support"
