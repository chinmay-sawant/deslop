def check_all_active(users):
    # VIOLATION: Using a manual flag and loop for a boolean reduction.
    # This should be replaced with the built-in all() function.
    is_all_active = True
    for user in users:
        if not user.is_active:
            is_all_active = False
            break
    return is_all_active