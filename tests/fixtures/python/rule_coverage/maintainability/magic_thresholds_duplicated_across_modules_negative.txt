from __future__ import annotations

WARNING_THRESHOLD = 70
CRITICAL_THRESHOLD = 90

def magic_thresholds_duplicated_across_modules_negative(score: int):
    is_warning = score >= WARNING_THRESHOLD
    is_critical = score >= CRITICAL_THRESHOLD
    return is_warning, is_critical
