from __future__ import annotations

def lambda_or_closure_allocated_per_item_when_static_helper_suffices_negative():
    results = []
    for n in [1, 2, 3]:
        results.append(add_offset(10, n))
    return results
