from __future__ import annotations

def repeated_pure_helper_call_on_same_input_without_local_cache_negative():
    def slugify(text: str) -> str:
        return "-".join(text.lower().split())
    value = "Product Name"
    slug = slugify(value)
    return [f"{slug}:{slug}" for _ in range(3)]
