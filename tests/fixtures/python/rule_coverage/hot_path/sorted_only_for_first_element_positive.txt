def get_minimum_value(items):
    if not items:
        return None
    # Violation: sorting the entire list just to retrieve the first element (O(N log N) instead of O(N))
    return sorted(items)[0]
