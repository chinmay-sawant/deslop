def process_stream(items):
    data = []
    for item in items:
        # Violation: appending to a list and then sorting it in every iteration (O(N^2 log N))
        data.append(item)
        data.sort()
    return data
