from __future__ import annotations

def pathlib_path_reconstructed_from_same_base_in_loop_negative():
    base = Path("/var/data/reports")
    targets = []
    for name in ["a.txt", "b.txt", "c.txt"]:
        targets.append(base / name)
    return targets
