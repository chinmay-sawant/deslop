from __future__ import annotations

def iterator_materialized_to_list_before_single_pass_loop_positive():
    rows = list(generate_rows())
    total = 0
    for row in rows:
        total += row
    return total
