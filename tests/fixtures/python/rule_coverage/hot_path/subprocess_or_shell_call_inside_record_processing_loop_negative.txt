from __future__ import annotations

def subprocess_or_shell_call_inside_record_processing_loop_negative():
    import subprocess
    records = ["alice", "bob", "carol"]
    joined = "\n".join(records)
    output = subprocess.check_output(["echo", joined], text=True)
    return output.splitlines()
