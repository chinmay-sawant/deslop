from __future__ import annotations

def environment_lookup_repeated_in_hot_path_negative():
    region = os.getenv("APP_REGION", "local")
    labels = []
    for _ in range(4):
        labels.append(region)
    return labels
