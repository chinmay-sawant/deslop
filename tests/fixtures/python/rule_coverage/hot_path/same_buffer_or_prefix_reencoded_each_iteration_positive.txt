from __future__ import annotations

def same_buffer_or_prefix_reencoded_each_iteration_positive():
    names = ["a", "b", "c"]
    packets = []
    for name in names:
        prefix = "user:".encode("utf-8")
        packets.append(prefix + name.encode("utf-8"))
    return packets
