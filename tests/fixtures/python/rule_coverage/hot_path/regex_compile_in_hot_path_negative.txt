from __future__ import annotations

def regex_compile_in_hot_path_negative():
    pattern = re.compile(r"id=(\d+)")
    matches = []
    for text in ["id=1", "x=na", "id=2"]:
        matches.append(pattern.findall(text))
    return matches
