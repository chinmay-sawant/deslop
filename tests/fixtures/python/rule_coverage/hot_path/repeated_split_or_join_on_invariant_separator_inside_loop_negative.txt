from __future__ import annotations

def repeated_split_or_join_on_invariant_separator_inside_loop_negative():
    text = "red,green,blue"
    parts = text.split(",")
    return [list(parts) for _ in range(4)]
