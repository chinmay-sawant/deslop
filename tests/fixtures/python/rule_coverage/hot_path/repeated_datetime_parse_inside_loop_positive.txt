from datetime import datetime

def parse_events(events):
    parsed = []
    for event in events:
        # Violation: repeated datetime parsing inside a loop using the slow strptime
        dt = datetime.strptime(event["timestamp"], "%Y-%m-%d %H:%M:%S")
        parsed.append((dt, event["data"]))
    return parsed
