import os

def write_data(fd, records):
    for record in records:
        # Violation: writing to a file descriptor inside a loop without user-space buffering
        os.write(fd, record.encode('utf-8'))
