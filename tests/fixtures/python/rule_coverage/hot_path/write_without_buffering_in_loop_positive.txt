from __future__ import annotations

def write_without_buffering_in_loop_positive():
    path = "events.log"
    for event in ("open", "save", "close"):
        with open(path, "a", encoding="utf-8") as handle:
            handle.write(event + "\n")
    return path
