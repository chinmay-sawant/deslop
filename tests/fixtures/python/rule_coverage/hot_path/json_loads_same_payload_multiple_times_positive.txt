from __future__ import annotations

def json_loads_same_payload_multiple_times_positive():
    raw = '{"enabled": true, "name": "api"}'
    enabled = json.loads(raw)["enabled"]
    name = json.loads(raw)["name"]
    return enabled, name
