from __future__ import annotations

def lambda_or_closure_allocated_per_item_when_static_helper_suffices_positive():
    funcs = []
    for n in [1, 2, 3]:
        funcs.append(lambda x, n=n: x + n)
    return [fn(10) for fn in funcs]
