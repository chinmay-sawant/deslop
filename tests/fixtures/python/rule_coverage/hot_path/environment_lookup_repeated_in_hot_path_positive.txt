import os

def log_event(msg):
    # Violation: environment variable lookup repeated inside a high-frequency function
    log_level = os.getenv("LOG_LEVEL", "INFO")
    if log_level == "DEBUG":
        print(f"DEBUG: {msg}")
