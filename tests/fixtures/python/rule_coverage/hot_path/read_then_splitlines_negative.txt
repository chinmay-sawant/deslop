from __future__ import annotations

def read_then_splitlines_negative():
    rows = []
    with open("events.log", "r", encoding="utf-8") as fh:
        for line in fh:
            rows.append(line.rstrip("\n"))
    return rows
