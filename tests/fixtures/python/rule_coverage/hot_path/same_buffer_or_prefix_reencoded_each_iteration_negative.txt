from __future__ import annotations

def same_buffer_or_prefix_reencoded_each_iteration_negative():
    names = ["a", "b", "c"]
    prefix = "user:".encode("utf-8")
    return [prefix + name.encode("utf-8") for name in names]
