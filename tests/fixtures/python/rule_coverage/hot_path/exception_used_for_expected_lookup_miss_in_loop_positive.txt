def map_values(keys, mapping):
    results = []
    for key in keys:
        # Violation: using try-except for an expected/common lookup miss in a loop (slow)
        try:
            results.append(mapping[key])
        except KeyError:
            results.append(None)
    return results
