from __future__ import annotations

def exception_used_for_expected_lookup_miss_in_loop_negative():
    counts = {}
    for key in ["a", "b", "a", "c"]:
        counts[key] = counts.get(key, 0) + 1
    return counts
