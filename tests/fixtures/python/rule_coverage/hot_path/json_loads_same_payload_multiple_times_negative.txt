from __future__ import annotations

def json_loads_same_payload_multiple_times_negative():
    raw = '{"enabled": true, "name": "api"}'
    payload = json.loads(raw)
    return payload["enabled"], payload["name"]
