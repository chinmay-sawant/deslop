def format_records(base_parts, records):
    formatted = []
    for record in records:
        # Violation: repeated join on an invariant prefix inside a loop
        prefix = "-".join(base_parts)
        formatted.append(f"{prefix}-{record.id}")
    return formatted
