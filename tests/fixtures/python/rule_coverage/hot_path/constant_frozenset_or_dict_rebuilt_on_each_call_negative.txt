# Correct: define constant collections at the module level for optimal performance
VALID_TOKENS = frozenset({"ALPHA", "BETA", "GAMMA", "DELTA", "EPSILON"})

def is_valid_token(token):
    return token in VALID_TOKENS
