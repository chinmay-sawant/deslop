def process_path(path):
    # Correct: pass a tuple of strings to startswith() for a single, optimized C-level check
    if path.startswith(("/api/v1", "/api/v2", "/admin")):
        return True
    return False
