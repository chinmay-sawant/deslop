from __future__ import annotations

def write_without_buffering_in_loop_negative():
    path = "events.log"
    lines = [event + "\n" for event in ("open", "save", "close")]
    with open(path, "a", encoding="utf-8") as handle:
        handle.writelines(lines)
    return path
