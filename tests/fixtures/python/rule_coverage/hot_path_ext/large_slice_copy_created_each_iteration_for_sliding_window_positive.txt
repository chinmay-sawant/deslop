from __future__ import annotations

def large_slice_copy_created_each_iteration_for_sliding_window_positive():
    values = list(range(120))
    totals = []
    for i in range(0, 70, 10):
        window = values[i:i + 40]
        totals.append(sum(window))
    return totals
