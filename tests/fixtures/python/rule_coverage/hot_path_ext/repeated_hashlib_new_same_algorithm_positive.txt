from __future__ import annotations

def repeated_hashlib_new_same_algorithm_positive():
    import hashlib
    values = ["a", "b", "c"]
    digests = []
    for value in values:
        h = hashlib.new("sha256")
        h.update(value.encode("utf-8"))
        digests.append(h.hexdigest())
    return digests
