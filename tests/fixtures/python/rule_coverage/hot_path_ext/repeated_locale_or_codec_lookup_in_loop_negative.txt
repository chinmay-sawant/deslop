from __future__ import annotations

def repeated_locale_or_codec_lookup_in_loop_negative():
    import codecs
    payloads = ["ok", "go", "up"]
    codec = codecs.lookup("utf-8")
    return [codec.encode(text)[0] for text in payloads]
