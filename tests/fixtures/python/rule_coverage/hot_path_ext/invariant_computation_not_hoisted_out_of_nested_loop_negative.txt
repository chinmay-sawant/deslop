from __future__ import annotations

def invariant_computation_not_hoisted_out_of_nested_loop_negative():
    reference = ["us", "eu", "apac", "us"]
    distinct_count = len(set(reference))
    total = 0
    for outer in range(3):
        for inner in range(2):
            total += distinct_count + outer + inner
    return total
