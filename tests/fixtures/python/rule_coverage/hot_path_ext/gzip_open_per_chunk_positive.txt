from __future__ import annotations

def gzip_open_per_chunk_positive():
    import gzip
    chunks = ["a", "b", "c"]
    for chunk in chunks:
        with gzip.open("data.gz", "at", encoding="utf-8") as stream:
            stream.write(chunk)
    return len(chunks)
