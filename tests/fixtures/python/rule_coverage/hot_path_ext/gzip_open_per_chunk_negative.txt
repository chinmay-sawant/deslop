from __future__ import annotations

def gzip_open_per_chunk_negative():
    import gzip
    chunks = ["a", "b", "c"]
    with gzip.open("data.gz", "at", encoding="utf-8") as stream:
        for chunk in chunks:
            stream.write(chunk)
    return len(chunks)
