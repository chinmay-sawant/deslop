from __future__ import annotations

def repeated_directory_scan_inside_nested_loop_positive():
    import os
    folders = ["logs", "cache"]
    counts = []
    for folder in folders:
        for _ in range(2):
            counts.append(len(os.listdir(folder)))
    return counts
