from __future__ import annotations

def repeated_path_exists_check_before_open_or_replace_in_loop_positive():
    from pathlib import Path
    names = ["a.tmp", "b.tmp"]
    archived = []
    for name in names:
        path = Path(name)
        if path.exists():
            archived.append(path.with_suffix(".bak"))
    return archived
