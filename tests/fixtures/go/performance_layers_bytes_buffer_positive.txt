package perflayers

import "bytes"

func BytesBufferPerRecordPositive(records []string) int {
    total := 0
    for _, record := range records {
        var buf bytes.Buffer
        buf.WriteString(record)
        total += buf.Len()
    }
    return total
}