package main

import "gorm.io/gorm"

type User struct {
	ID   int
	Name string
}

func UpdateEach(db *gorm.DB, users []User) {
	for _, user := range users { changes := map[string]any{"name": user.Name}; db.Model(&User{}).Updates(changes) }
}
