// should trigger likely_n_squared_string_concat because += appears inside nested loops without a builder.
package sample

func Render(rows [][]string) string {
	out := ""
	for _, row := range rows {
		for _, cell := range row {
			out += cell
		}
	}
	return out
}