package main

import "gorm.io/gorm"

type User struct {
	ID   int
	Name string
}

func UpdateEach(db *gorm.DB, users []User) {
	for _, user := range users { db.Model(&User{}).Updates(map[string]any{"name": user.Name}) }
}
