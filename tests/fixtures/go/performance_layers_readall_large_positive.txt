package perflayers

import "os"

func ReadAllLargePositive() (int, error) {
    data, err := os.ReadFile("large.dat")
    if err != nil {
        return 0, err
    }

    total := 0
    for _, value := range data {
        total += int(value)
    }
    return total, nil
}