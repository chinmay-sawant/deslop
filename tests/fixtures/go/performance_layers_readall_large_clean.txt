package perflayers

import (
    "bufio"
    "io"
)

func ReadAllLargeClean(reader io.Reader) (int, error) {
    scanner := bufio.NewScanner(reader)
    total := 0
    for scanner.Scan() {
        total += len(scanner.Bytes())
    }
    return total, scanner.Err()
}