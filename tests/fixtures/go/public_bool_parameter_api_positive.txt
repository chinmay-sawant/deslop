package sample

// PublicBoolParameterAPI demonstrates a function with a bool parameter
// that controls internal branching — an API design smell.
func PublicBoolParameterAPI(name string, includeDeleted bool) []string {
    if includeDeleted {
        return []string{name, "deleted-item"}
    }
    return []string{name}
}