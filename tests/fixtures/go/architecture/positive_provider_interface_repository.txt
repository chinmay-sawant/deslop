package repository

import "context"

type User struct {
	ID string
}

type UserRepository interface {
	ListActive(ctx context.Context) ([]User, error)
}

type GormUserRepository struct{}

func (r *GormUserRepository) ListActive(ctx context.Context) ([]User, error) {
	return nil, nil
}
