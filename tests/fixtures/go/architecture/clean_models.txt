package models

import "gorm.io/gorm"

type User struct {
    ID   int    `gorm:"column:id"`
    Name string `gorm:"column:name"`
}

func (u *User) BeforeCreate(tx *gorm.DB) error {
    _ = tx
    return nil
}
