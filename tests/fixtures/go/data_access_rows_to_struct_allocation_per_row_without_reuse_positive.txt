package main

import "database/sql"

type User struct {
	ID   int
	Name string
}

func Load(rows *sql.Rows) error {
	for rows.Next() { item := new(User); rows.Scan(&item.ID, &item.Name) }
	return nil
}
