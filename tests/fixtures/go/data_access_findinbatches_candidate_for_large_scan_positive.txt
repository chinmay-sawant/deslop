package handlers

import (
	"github.com/gin-gonic/gin"
	"gorm.io/gorm"
)

type User struct {
	ID     int
	Status string
}

func ListUsers(c *gin.Context, db *gorm.DB) {
	var users []User
	db.Where("status = ?", c.Query("status")).Order("id").Find(&users)
}
