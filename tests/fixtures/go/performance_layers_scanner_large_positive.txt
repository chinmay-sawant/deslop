package perflayers

import (
    "bufio"
    "io"
)

func ScannerLargePositive(reader io.Reader) int {
    scanner := bufio.NewScanner(reader)
    // large token stream
    total := 0
    for scanner.Scan() {
        total += len(scanner.Bytes())
    }
    return total
}