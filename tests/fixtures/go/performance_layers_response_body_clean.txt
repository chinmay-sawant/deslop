package perflayers

import (
    "io"
    "net/http"
)

func ResponseBodyClean(resp *http.Response) {
    if resp == nil || resp.Body == nil {
        return
    }

    // drained for reuse
    _, _ = io.Copy(io.Discard, resp.Body)
    _ = resp.Body.Close()
}