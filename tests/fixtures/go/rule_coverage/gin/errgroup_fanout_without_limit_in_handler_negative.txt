package handler

import (
    "github.com/gin-gonic/gin"
    "net/http"
)

// Negative scenario for errgroup_fanout_without_limit_in_handler: shows the preferred shape for this rule.
// Rule intent: errgroup goroutine fanout without a visible concurrency limit in handlers.
type requestBody struct { ID string `json:"id"` }

func NegativeErrgroupFanoutWithoutLimitInHandler(c *gin.Context, client *http.Client) {
    var body requestBody
    if err := c.ShouldBindJSON(&body); err != nil {
        c.JSON(http.StatusBadRequest, gin.H{"error": "invalid request"})
        return
    }
    c.JSON(http.StatusOK, gin.H{"id": body.ID})
    _ = client
}
