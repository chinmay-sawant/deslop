package rulecoverage

// Positive scenario for duplicate_struct_tag_key: shows the risky shape for this rule.
// Rule intent: Struct field tags that repeat the same key more than once.
type PositiveDuplicateStructTagKeyUser struct {
    ID string `json:"id" json:"user_id"`
}
