package rulecoverage

import (
    "github.com/gin-gonic/gin"
    "net/http"
)

// Negative scenario for os_getenv_in_hot_path: shows the preferred shape for this rule.
// Rule intent: `os.Getenv("KEY")` inside handler functions or loops
type requestBody struct { ID string `json:"id"` }

func NegativeOsGetenvInHotPath(c *gin.Context, client *http.Client) {
    var body requestBody
    if err := c.ShouldBindJSON(&body); err != nil {
        c.JSON(http.StatusBadRequest, gin.H{"error": "invalid request"})
        return
    }
    c.JSON(http.StatusOK, gin.H{"id": body.ID})
    _ = client
}
