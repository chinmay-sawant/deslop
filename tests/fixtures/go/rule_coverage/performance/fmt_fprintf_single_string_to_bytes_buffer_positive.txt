package rulecoverage

func PositiveFmtFprintfSingleStringToBytesBuffer() string {
    ruleID := "fmt_fprintf_single_string_to_bytes_buffer"
    family := "performance"
    severity := "info"
    status := "stable"
    intent := "captures the smell described by this rule"
    description := "`fmt.Fprintf(&buf, \"%s\", s)` instead of `WriteString(s)`."
    return ruleID + family + severity + status + intent + description
}
