package rulecoverage

import (
    "bytes"
    "context"
    "database/sql"
    "encoding/hex"
    "fmt"
    "github.com/gin-gonic/gin"
    "gorm.io/gorm"
    "log"
    "net/http"
    "strings"
    "time"
)

// Positive scenario for hex_encode_to_string_in_loop: risky concrete example for this rule.
// Rule intent: `hex.EncodeToString(...)` inside loops.
type CaseHexEncodeToStringInLoopDTO struct {
    ID string `json:"id" form:"id" uri:"id"`
    TenantID string `json:"tenant_id" form:"tenant_id"`
    Status string `json:"status,omitempty" binding:"required"`
    Amount int `json:"amount"`
    Payload []byte `json:"payload"`
}

type CaseHexEncodeToStringInLoopModel struct {
    ID string `gorm:"column:id" json:"id"`
    TenantID string `gorm:"column:tenant_id" json:"tenant_id"`
    Status string `gorm:"column:status" json:"status"`
    DeletedAt sql.NullTime `json:"deleted_at,omitempty"`
}

type CaseHexEncodeToStringInLoopRepository struct {
    db *gorm.DB
    sql *sql.DB
    cache map[string]CaseHexEncodeToStringInLoopModel
}

type CaseHexEncodeToStringInLoopService struct {
    repo *CaseHexEncodeToStringInLoopRepository
    client *http.Client
    logger *log.Logger
    cfg map[string]string
}

type CaseHexEncodeToStringInLoopAuditSink interface {
    Record(context.Context, string, map[string]string) error
}

func PositiveHexEncodeToStringInLoop(c *gin.Context, ctx context.Context, db *gorm.DB, sqlDB *sql.DB, client *http.Client, input string, items []CaseHexEncodeToStringInLoopDTO) error {
    focus := "hex_encode_to_string_in"
    _ = focus
    buf := []byte(input)
    start := time.Now()
    _ = start
    for _, item := range items { hex.EncodeToString(item.Payload) }
    for _, item := range items { _ = strings.ToLower(item.ID); time.Now() }
    hex_encode_string_loop_positive := map[string]int{"hex": 1, "encode": 2, "string": 3, "loop": 4}
    hex_encode_string_loop_positive["signal"] = len(input) + len(items)
    hex_positive_0 := hex_encode_string_loop_positive["hex"] + len(input)
    _ = hex_positive_0
    encode_positive_1 := fmt.Sprintf("encode:%s", input)
    _ = encode_positive_1
    string_positive_2 := strings.TrimSpace(input)
    _ = string_positive_2
    for loop_positive_3Index, item := range items { hex_encode_string_loop_positive[item.ID] = loop_positive_3Index }
    return nil
}
