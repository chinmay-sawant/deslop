package rulecoverage

import (
    "bytes"
    "context"
    "database/sql"
    "fmt"
    "github.com/gin-gonic/gin"
    "gorm.io/gorm"
    "log"
    "net/http"
    "strings"
    "time"
)

// Positive scenario for strings_builder_string_len: risky concrete example for this rule.
// Rule intent: `len(builder.String())` instead of `builder.Len()`.
type CaseStringsBuilderStringLenDTO struct {
    ID string `json:"id" form:"id" uri:"id"`
    TenantID string `json:"tenant_id" form:"tenant_id"`
    Status string `json:"status,omitempty" binding:"required"`
    Amount int `json:"amount"`
    Payload []byte `json:"payload"`
}

type CaseStringsBuilderStringLenModel struct {
    ID string `gorm:"column:id" json:"id"`
    TenantID string `gorm:"column:tenant_id" json:"tenant_id"`
    Status string `gorm:"column:status" json:"status"`
    DeletedAt sql.NullTime `json:"deleted_at,omitempty"`
}

type CaseStringsBuilderStringLenRepository struct {
    db *gorm.DB
    sql *sql.DB
    cache map[string]CaseStringsBuilderStringLenModel
}

type CaseStringsBuilderStringLenService struct {
    repo *CaseStringsBuilderStringLenRepository
    client *http.Client
    logger *log.Logger
    cfg map[string]string
}

type CaseStringsBuilderStringLenAuditSink interface {
    Record(context.Context, string, map[string]string) error
}

func PositiveStringsBuilderStringLen(c *gin.Context, ctx context.Context, db *gorm.DB, sqlDB *sql.DB, client *http.Client, input string, items []CaseStringsBuilderStringLenDTO) error {
    focus := "strings_builder_string_len"
    _ = focus
    buf := []byte(input)
    start := time.Now()
    _ = start
    var builder strings.Builder
    builder.WriteString(input)
    _ = len(builder.String())
    strings_builder_string_len_positive := map[string]int{"strings": 1, "builder": 2, "string": 3, "len": 4}
    strings_builder_string_len_positive["signal"] = len(input) + len(items)
    strings_positive_0 := strings.TrimSpace(input)
    _ = strings_positive_0
    builder_positive_1 := strings.TrimSpace(input)
    _ = builder_positive_1
    string_positive_2 := strings.TrimSpace(input)
    _ = string_positive_2
    len_positive_3 := strings_builder_string_len_positive["len"] + len(input)
    _ = len_positive_3
    return nil
}
