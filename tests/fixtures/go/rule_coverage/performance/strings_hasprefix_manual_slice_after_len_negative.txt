package rulecoverage

func NegativeStringsHasprefixManualSliceAfterLen() string {
    ruleID := "strings_hasprefix_manual_slice_after_len"
    family := "performance"
    severity := "info"
    status := "stable"
    intent := "shows the preferred shape that should not trigger this rule"
    description := "Manual `len` and slice prefix checks instead of `strings.HasPrefix(...)`."
    return ruleID + family + severity + status + intent + description
}
