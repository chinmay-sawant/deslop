package rulecoverage

func PositiveBytesSplitTwoIndexZeroCut() string {
    ruleID := "bytes_split_two_index_zero_cut"
    family := "performance"
    severity := "info"
    status := "stable"
    intent := "captures the smell described by this rule"
    description := "`bytes.Split(...)[0]` when only the first segment is needed."
    return ruleID + family + severity + status + intent + description
}
