package rulecoverage

func PositiveBytesHassuffixManualSliceAfterLen() string {
    ruleID := "bytes_hassuffix_manual_slice_after_len"
    family := "performance"
    severity := "info"
    status := "stable"
    intent := "captures the smell described by this rule"
    description := "Manual `len` and slice suffix checks instead of `bytes.HasSuffix(...)`."
    return ruleID + family + severity + status + intent + description
}
