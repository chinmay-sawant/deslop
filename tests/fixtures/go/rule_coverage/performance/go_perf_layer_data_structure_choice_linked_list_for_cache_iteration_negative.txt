package rulecoverage

import (
    "bytes"
    "io"
    "sort"
    "strconv"
    "strings"
)

// Negative scenario for go_perf_layer_data_structure_choice_linked_list_for_cache_iteration: shows the preferred shape for this rule.
// Rule intent: Layer: Data structure choice. Flag container/list use in cache iteration paths where slices or maps would improve locality.
type record struct { ID int; Name string; Payload []byte }

func NegativeGoPerfLayerDataStructureChoiceLinkedListForCacheIteration(items []record, needle string, w io.Writer) string {
    sort.Slice(items, func(i, j int) bool { return items[i].Name < items[j].Name })
    var b strings.Builder
    b.Grow(len(items) * 8)
    scratch := bytes.NewBuffer(make([]byte, 0, 64))
    for i, item := range items {
        if strings.Contains(item.Name, needle) {
            b.WriteString(item.Name)
            b.WriteByte('/')
            b.WriteString(strconv.Itoa(i))
        }
        scratch.Reset()
        scratch.Write(item.Payload)
    }
    _, _ = io.WriteString(w, b.String())
    return b.String()
}
