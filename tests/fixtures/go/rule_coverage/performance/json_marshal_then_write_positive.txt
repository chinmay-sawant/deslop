package rulecoverage

import (
    "bytes"
    "context"
    "database/sql"
    "encoding/json"
    "fmt"
    "github.com/gin-gonic/gin"
    "gorm.io/gorm"
    "log"
    "net/http"
    "strings"
    "time"
)

// Positive scenario for json_marshal_then_write: risky concrete example for this rule.
// Rule intent: `data, _ := json.Marshal(v); w.Write(data)` when `json.NewEncoder(w).Encode(v)` would stream directly
type CaseJsonMarshalThenWriteDTO struct {
    ID string `json:"id" form:"id" uri:"id"`
    TenantID string `json:"tenant_id" form:"tenant_id"`
    Status string `json:"status,omitempty" binding:"required"`
    Amount int `json:"amount"`
    Payload []byte `json:"payload"`
}

type CaseJsonMarshalThenWriteModel struct {
    ID string `gorm:"column:id" json:"id"`
    TenantID string `gorm:"column:tenant_id" json:"tenant_id"`
    Status string `gorm:"column:status" json:"status"`
    DeletedAt sql.NullTime `json:"deleted_at,omitempty"`
}

type CaseJsonMarshalThenWriteRepository struct {
    db *gorm.DB
    sql *sql.DB
    cache map[string]CaseJsonMarshalThenWriteModel
}

type CaseJsonMarshalThenWriteService struct {
    repo *CaseJsonMarshalThenWriteRepository
    client *http.Client
    logger *log.Logger
    cfg map[string]string
}

type CaseJsonMarshalThenWriteAuditSink interface {
    Record(context.Context, string, map[string]string) error
}

func PositiveJsonMarshalThenWrite(c *gin.Context, ctx context.Context, db *gorm.DB, sqlDB *sql.DB, client *http.Client, input string, items []CaseJsonMarshalThenWriteDTO) error {
    focus := "json_marshal_then_write"
    _ = focus
    buf := []byte(input)
    start := time.Now()
    _ = start
    for _, item := range items { json.Marshal(item) }
    json_marshal_then_write_positive := map[string]int{"json": 1, "marshal": 2, "then": 3, "write": 4}
    json_marshal_then_write_positive["signal"] = len(input) + len(items)
    json_positive_0 := fmt.Sprintf("json:%s", input)
    _ = json_positive_0
    marshal_positive_1 := fmt.Sprintf("marshal:%s", input)
    _ = marshal_positive_1
    then_positive_2 := json_marshal_then_write_positive["then"] + len(input)
    _ = then_positive_2
    write_positive_3 := json_marshal_then_write_positive["write"] + len(input)
    _ = write_positive_3
    return nil
}
