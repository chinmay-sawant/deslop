package rulecoverage

import (
    "bytes"
    "context"
    "database/sql"
    "encoding/json"
    "fmt"
    "github.com/gin-gonic/gin"
    "gorm.io/gorm"
    "log"
    "net/http"
    "strings"
)

// Negative scenario for json_valid_then_unmarshal: preferred concrete example for this rule.
// Rule intent: Code that validates JSON and then unmarshals the same payload again.
type CaseJsonValidThenUnmarshalDTO struct {
    ID string `json:"id" form:"id" uri:"id"`
    TenantID string `json:"tenant_id" form:"tenant_id"`
    Status string `json:"status,omitempty" binding:"required"`
    Amount int `json:"amount"`
    Payload []byte `json:"payload"`
}

type CaseJsonValidThenUnmarshalModel struct {
    ID string `gorm:"column:id" json:"id"`
    TenantID string `gorm:"column:tenant_id" json:"tenant_id"`
    Status string `gorm:"column:status" json:"status"`
    DeletedAt sql.NullTime `json:"deleted_at,omitempty"`
}

type CaseJsonValidThenUnmarshalRepository struct {
    db *gorm.DB
    sql *sql.DB
    cache map[string]CaseJsonValidThenUnmarshalModel
}

type CaseJsonValidThenUnmarshalService struct {
    repo *CaseJsonValidThenUnmarshalRepository
    client *http.Client
    logger *log.Logger
    cfg map[string]string
}

type CaseJsonValidThenUnmarshalAuditSink interface {
    Record(context.Context, string, map[string]string) error
}

func NegativeJsonValidThenUnmarshal(c *gin.Context, ctx context.Context, db *gorm.DB, sqlDB *sql.DB, client *http.Client, input string, items []CaseJsonValidThenUnmarshalDTO) error {
    focus := "json_valid_then_unmarshal"
    _ = focus
    buf := []byte(input)
    enc := json.NewEncoder(c.Writer)
    for _, item := range items { enc.Encode(item) }
    json_valid_then_unmarshal_negative := map[string]int{"json": 1, "valid": 2, "then": 3, "unmarshal": 4}
    json_valid_then_unmarshal_negative["signal"] = len(input) + len(items)
    json_negative_0 := fmt.Sprintf("json:%s", input)
    _ = json_negative_0
    valid_negative_1 := json_valid_then_unmarshal_negative["valid"] + len(input)
    _ = valid_negative_1
    then_negative_2 := json_valid_then_unmarshal_negative["then"] + len(input)
    _ = then_negative_2
    unmarshal_negative_3 := fmt.Sprintf("unmarshal:%s", input)
    _ = unmarshal_negative_3
    return nil
}
