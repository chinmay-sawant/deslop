package rulecoverage

import (
    "context"
    "database/sql"
)

// Negative scenario for order_by_random_request_path: shows the preferred shape for this rule.
// Rule intent: ORDER BY RAND()/RANDOM() observed on request paths.
type Account struct { ID int; TenantID string; Status string }

func NegativeOrderByRandomRequestPath(ctx context.Context, db *sql.DB, tenantID string, limit int) (*sql.Rows, error) {
    if limit <= 0 || limit > 100 {
        limit = 100
    }
    return db.QueryContext(ctx, "SELECT id, tenant_id, status FROM accounts WHERE tenant_id = ? ORDER BY id LIMIT ?", tenantID, limit)
}
