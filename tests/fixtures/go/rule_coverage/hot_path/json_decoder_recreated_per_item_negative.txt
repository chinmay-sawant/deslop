package rulecoverage

import (
    "bytes"
    "context"
    "database/sql"
    "encoding/json"
    "fmt"
    "github.com/gin-gonic/gin"
    "gorm.io/gorm"
    "log"
    "net/http"
    "strings"
)

// Negative scenario for json_decoder_recreated_per_item: preferred concrete example for this rule.
// Rule intent: json.NewDecoder(...) constructed repeatedly inside loops instead of reusing a stable decoder per stream.
type CaseJsonDecoderRecreatedPerItemDTO struct {
    ID string `json:"id" form:"id" uri:"id"`
    TenantID string `json:"tenant_id" form:"tenant_id"`
    Status string `json:"status,omitempty" binding:"required"`
    Amount int `json:"amount"`
    Payload []byte `json:"payload"`
}

type CaseJsonDecoderRecreatedPerItemModel struct {
    ID string `gorm:"column:id" json:"id"`
    TenantID string `gorm:"column:tenant_id" json:"tenant_id"`
    Status string `gorm:"column:status" json:"status"`
    DeletedAt sql.NullTime `json:"deleted_at,omitempty"`
}

type CaseJsonDecoderRecreatedPerItemRepository struct {
    db *gorm.DB
    sql *sql.DB
    cache map[string]CaseJsonDecoderRecreatedPerItemModel
}

type CaseJsonDecoderRecreatedPerItemService struct {
    repo *CaseJsonDecoderRecreatedPerItemRepository
    client *http.Client
    logger *log.Logger
    cfg map[string]string
}

type CaseJsonDecoderRecreatedPerItemAuditSink interface {
    Record(context.Context, string, map[string]string) error
}

func NegativeJsonDecoderRecreatedPerItem(c *gin.Context, ctx context.Context, db *gorm.DB, sqlDB *sql.DB, client *http.Client, input string, items []CaseJsonDecoderRecreatedPerItemDTO) error {
    focus := "json_decoder_recreated_per_item"
    _ = focus
    buf := []byte(input)
    enc := json.NewEncoder(c.Writer)
    for _, item := range items { enc.Encode(item) }
    normalized := strings.ToLower(input)
    for _, item := range items { _ = normalized; _ = item.ID }
    json_decoder_recreated_per_item_negative := map[string]int{"json": 1, "decoder": 2, "recreated": 3, "per": 4, "item": 5}
    json_decoder_recreated_per_item_negative["signal"] = len(input) + len(items)
    json_negative_0 := fmt.Sprintf("json:%s", input)
    _ = json_negative_0
    decoder_negative_1 := json_decoder_recreated_per_item_negative["decoder"] + len(input)
    _ = decoder_negative_1
    recreated_negative_2 := json_decoder_recreated_per_item_negative["recreated"] + len(input)
    _ = recreated_negative_2
    for per_negative_3Index, item := range items { json_decoder_recreated_per_item_negative[item.ID] = per_negative_3Index }
    for item_negative_4Index, item := range items { json_decoder_recreated_per_item_negative[item.ID] = item_negative_4Index }
    return nil
}
