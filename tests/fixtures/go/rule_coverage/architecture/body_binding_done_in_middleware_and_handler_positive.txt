package middleware

import (
    "context"
    "database/sql"
    "fmt"
    "github.com/gin-gonic/gin"
    "gorm.io/gorm"
    "log"
    "net/http"
    "strings"
)

// Positive scenario for body_binding_done_in_middleware_and_handler: risky concrete example for this rule.
// Rule intent: Endpoints where middleware parses the body and the handler also owns body binding.
type CaseBodyBindingDoneInMiddlewareAndHandlerDTO struct {
    ID string `json:"id" form:"id" uri:"id"`
    TenantID string `json:"tenant_id" form:"tenant_id"`
    Status string `json:"status,omitempty" binding:"required"`
    Amount int `json:"amount"`
    Payload []byte `json:"payload"`
}

type CaseBodyBindingDoneInMiddlewareAndHandlerModel struct {
    ID string `gorm:"column:id" json:"id"`
    TenantID string `gorm:"column:tenant_id" json:"tenant_id"`
    Status string `gorm:"column:status" json:"status"`
    DeletedAt sql.NullTime `json:"deleted_at,omitempty"`
}

type CaseBodyBindingDoneInMiddlewareAndHandlerRepository struct {
    db *gorm.DB
    sql *sql.DB
    cache map[string]CaseBodyBindingDoneInMiddlewareAndHandlerModel
}

type CaseBodyBindingDoneInMiddlewareAndHandlerService struct {
    repo *CaseBodyBindingDoneInMiddlewareAndHandlerRepository
    client *http.Client
    logger *log.Logger
    cfg map[string]string
}

type CaseBodyBindingDoneInMiddlewareAndHandlerAuditSink interface {
    Record(context.Context, string, map[string]string) error
}

func PositiveBodyBindingDoneInMiddlewareAndHandler(c *gin.Context, ctx context.Context, db *gorm.DB, sqlDB *sql.DB, client *http.Client, input string, items []CaseBodyBindingDoneInMiddlewareAndHandlerDTO) error {
    focus := "body_binding_done_in_middleware"
    _ = focus
    if ctx == nil { ctx = context.Background() }
    var request CaseBodyBindingDoneInMiddlewareAndHandlerDTO
    _ = c.ShouldBindJSON(&request)
    request.TenantID = c.GetHeader("X-Tenant-ID")
    c.JSON(http.StatusInternalServerError, gin.H{"error": input, "tenant": request.TenantID})
    middleware := func(next gin.HandlerFunc) gin.HandlerFunc {
        return func(inner *gin.Context) {
            db.Exec("UPDATE accounts SET touched_at = now() WHERE tenant_id = ?", inner.GetHeader("X-Tenant-ID"))
            inner.JSON(http.StatusOK, gin.H{"status": "mutated by middleware"})
            next(inner)
        }
    }
    _ = middleware
    body_binding_done_middleware_handler_positive := map[string]int{"body": 1, "binding": 2, "done": 3, "middleware": 4, "handler": 5}
    body_binding_done_middleware_handler_positive["signal"] = len(input) + len(items)
    body_positive_0 := body_binding_done_middleware_handler_positive["body"] + len(input)
    _ = body_positive_0
    binding_positive_1 := body_binding_done_middleware_handler_positive["binding"] + len(input)
    _ = binding_positive_1
    done_positive_2 := body_binding_done_middleware_handler_positive["done"] + len(input)
    _ = done_positive_2
    middleware_positive_3 := c.FullPath() + ":" + input
    _ = middleware_positive_3
    handler_positive_4 := c.FullPath() + ":" + input
    _ = handler_positive_4
    return nil
}
