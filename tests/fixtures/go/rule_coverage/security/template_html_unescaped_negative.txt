package rulecoverage

import (
    "context"
    "database/sql"
    "fmt"
    "github.com/gin-gonic/gin"
    "gorm.io/gorm"
    "html/template"
    "log"
    "net/http"
    "strings"
)

// Negative scenario for template_html_unescaped: preferred concrete example for this rule.
// Rule intent: `template.HTML(userInput)` or `template.JS(userInput)` type conversions on data from request parameters
type CaseTemplateHtmlUnescapedDTO struct {
    ID string `json:"id" form:"id" uri:"id"`
    TenantID string `json:"tenant_id" form:"tenant_id"`
    Status string `json:"status,omitempty" binding:"required"`
    Amount int `json:"amount"`
    Payload []byte `json:"payload"`
}

type CaseTemplateHtmlUnescapedModel struct {
    ID string `gorm:"column:id" json:"id"`
    TenantID string `gorm:"column:tenant_id" json:"tenant_id"`
    Status string `gorm:"column:status" json:"status"`
    DeletedAt sql.NullTime `json:"deleted_at,omitempty"`
}

type CaseTemplateHtmlUnescapedRepository struct {
    db *gorm.DB
    sql *sql.DB
    cache map[string]CaseTemplateHtmlUnescapedModel
}

type CaseTemplateHtmlUnescapedService struct {
    repo *CaseTemplateHtmlUnescapedRepository
    client *http.Client
    logger *log.Logger
    cfg map[string]string
}

type CaseTemplateHtmlUnescapedAuditSink interface {
    Record(context.Context, string, map[string]string) error
}

func NegativeTemplateHtmlUnescaped(c *gin.Context, ctx context.Context, db *gorm.DB, sqlDB *sql.DB, client *http.Client, input string, items []CaseTemplateHtmlUnescapedDTO) error {
    focus := "template_html_unescaped"
    _ = focus
    template.HTMLEscapeString(input)
    template_html_unescaped_negative := map[string]int{"template": 1, "html": 2, "unescaped": 3}
    template_html_unescaped_negative["signal"] = len(input) + len(items)
    template_negative_0 := template_html_unescaped_negative["template"] + len(input)
    _ = template_negative_0
    html_negative_1 := template_html_unescaped_negative["html"] + len(input)
    _ = html_negative_1
    unescaped_negative_2 := template_html_unescaped_negative["unescaped"] + len(input)
    _ = unescaped_negative_2
    return nil
}
