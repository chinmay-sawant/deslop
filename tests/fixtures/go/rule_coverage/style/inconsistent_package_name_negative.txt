package alpha

// Negative scenario for inconsistent_package_name: preferred concrete example for this rule.
// Rule intent: Directories that mix base Go package names after ignoring the _test suffix.
func NegativeInconsistentPackageName() string {
    packageNames := []string{"alpha"}
    return packageNames[0]
}
