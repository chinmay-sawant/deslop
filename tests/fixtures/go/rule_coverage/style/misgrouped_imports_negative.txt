package alpha

import (
    "fmt"

    "github.com/acme/pkg"
)

// Negative scenario for misgrouped_imports: shows the preferred shape for this rule.
// Rule intent: Import blocks that place stdlib imports after third-party imports.
func NegativeMisgroupedImports() { fmt.Println(pkg.Name) }
