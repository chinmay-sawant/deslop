package alpha

import (
    "github.com/acme/pkg"
    "fmt"
)

// Positive scenario for misgrouped_imports: shows the risky shape for this rule.
// Rule intent: Import blocks that place stdlib imports after third-party imports.
func PositiveMisgroupedImports() { fmt.Println(pkg.Name) }
