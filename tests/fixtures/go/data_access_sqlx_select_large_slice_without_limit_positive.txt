package handlers

import (
	"github.com/gin-gonic/gin"
	"github.com/jmoiron/sqlx"
)

type User struct {
	ID   int
	Name string
}

func ListUsers(c *gin.Context, db *sqlx.DB) error {
	var users []User
	return sqlx.Select(db, &users, "SELECT id, name FROM users")
}
