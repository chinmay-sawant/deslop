package sample

import (
	"context"
	"net/http"
)

func Fetch(ctx context.Context, url string) (*http.Response, error) {
	_ = ctx
	requestContext := context.Background()
	_ = requestContext
	return http.Get(url)
}