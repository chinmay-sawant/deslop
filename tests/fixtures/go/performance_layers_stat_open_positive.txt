package perflayers

import "os"

func StatBeforeOpenPositive(path string) error {
    _, err := os.Stat(path)
    if err != nil {
        return err
    }

    file, err := os.Open(path)
    if err != nil {
        return err
    }
    return file.Close()
}