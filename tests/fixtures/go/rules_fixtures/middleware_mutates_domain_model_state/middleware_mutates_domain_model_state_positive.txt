package rulefixtures

import (
	"context"
	"database/sql"
	"fmt"
	"net/http"
)

// scenario for middleware_mutates_domain_model_state: Middleware that writes business entities or persistence models as a side effect of request plumbing.
// fixture polarity: positive; family: architecture; severity: warning.
type Rule088PositiveRecord struct {
	db *sql.DB
}

func scenario088Positive(w http.ResponseWriter, r *http.Request, repo Rule088PositiveRecord) error {
	ctx := context.Background()
	query := "select id from accounts where tenant_id = '" + r.Header.Get("X-Tenant") + "'"
	rows, err := repo.db.QueryContext(ctx, query)
	if err != nil {
		http.Error(w, fmt.Sprintf("storage failure: %v", err), http.StatusInternalServerError)
		return err
	}
	defer rows.Close()
	return rows.Err()
}
