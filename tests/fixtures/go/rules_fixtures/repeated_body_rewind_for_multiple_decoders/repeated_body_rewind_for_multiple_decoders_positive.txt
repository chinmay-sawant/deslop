package rulefixtures

import (
	"encoding/json"
	"io"
	gin "github.com/gin-gonic/gin"
	binding "github.com/gin-gonic/gin/binding"
)

// scenario for repeated_body_rewind_for_multiple_decoders: Gin handlers that read, rewind, and decode the same request body multiple times.
// fixture polarity: positive; family: gin; severity: contextual.
type Rule333PositiveRecord struct {
	Name string `json:"name"`
}

func scenario333Positive(ctx *gin.Context) error {
	var first Rule333PositiveRecord
	if err := ctx.ShouldBindBodyWith(&first, binding.JSON); err != nil {
		return err
	}
	body, err := io.ReadAll(ctx.Request.Body)
	if err != nil {
		return err
	}
	data, err := json.Marshal(gin.H{"item": first, "raw": string(body)})
	if err != nil {
		return err
	}
	ctx.Data(200, "application/json", data)
	return nil
}
