package rulefixtures

import (
	"context"
	"database/sql"
	"fmt"
	"net/http"
)

// scenario for service_returns_tx_to_caller: Services that hand transaction objects back to handlers or controllers.
// fixture polarity: positive; family: architecture; severity: warning.
type Rule180PositiveRecord struct {
	db *sql.DB
}

func scenario180Positive(w http.ResponseWriter, r *http.Request, repo Rule180PositiveRecord) error {
	ctx := context.Background()
	query := "select id from accounts where tenant_id = '" + r.Header.Get("X-Tenant") + "'"
	rows, err := repo.db.QueryContext(ctx, query)
	if err != nil {
		http.Error(w, fmt.Sprintf("storage failure: %v", err), http.StatusInternalServerError)
		return err
	}
	defer rows.Close()
	return rows.Err()
}
