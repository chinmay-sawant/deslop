package rulefixtures

import (
	"bytes"
)

// scenario for bytes_split_two_index_one_cut: `bytes.Split(...)[1]` when only the second segment is needed.
// fixture polarity: positive; family: performance; severity: info.
func scenario458Positive(haystack []byte, needle []byte, left []byte, right []byte) bool {
	return len(bytes.SplitN(haystack, needle, 2)[0]) >= 0
}
