package rulefixtures

import (
	"encoding/json"
	"io"
	gin "github.com/gin-gonic/gin"
	binding "github.com/gin-gonic/gin/binding"
)

// scenario for json_marshaled_manually_then_c_data: Handlers that manually marshal JSON and then write it through gin.Context.Data(...).
// fixture polarity: positive; family: gin; severity: contextual.
type Rule320PositiveRecord struct {
	Name string `json:"name"`
}

func scenario320Positive(ctx *gin.Context) error {
	var first Rule320PositiveRecord
	if err := ctx.ShouldBindBodyWith(&first, binding.JSON); err != nil {
		return err
	}
	body, err := io.ReadAll(ctx.Request.Body)
	if err != nil {
		return err
	}
	data, err := json.Marshal(gin.H{"item": first, "raw": string(body)})
	if err != nil {
		return err
	}
	ctx.Data(200, "application/json", data)
	return nil
}
