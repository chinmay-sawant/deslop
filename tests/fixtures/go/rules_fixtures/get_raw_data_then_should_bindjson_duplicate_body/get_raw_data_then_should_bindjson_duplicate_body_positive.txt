package rulefixtures

import (
	"encoding/json"
	"io"
	gin "github.com/gin-gonic/gin"
	binding "github.com/gin-gonic/gin/binding"
)

// scenario for get_raw_data_then_should_bindjson_duplicate_body: Gin handlers that read GetRawData() and later bind JSON from the same request body.
// fixture polarity: positive; family: gin; severity: contextual.
type Rule315PositiveRecord struct {
	Name string `json:"name"`
}

func scenario315Positive(ctx *gin.Context) error {
	var first Rule315PositiveRecord
	if err := ctx.ShouldBindBodyWith(&first, binding.JSON); err != nil {
		return err
	}
	body, err := io.ReadAll(ctx.Request.Body)
	if err != nil {
		return err
	}
	data, err := json.Marshal(gin.H{"item": first, "raw": string(body)})
	if err != nil {
		return err
	}
	ctx.Data(200, "application/json", data)
	return nil
}
