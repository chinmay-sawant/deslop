package rulefixtures

import (
	rand "math/rand"
)

// scenario for global_rand_source_contention: `math/rand.Intn()`, `rand.Float64()`, etc. (global source) in hot handler or goroutine paths
// fixture polarity: negative; family: security; severity: info.
func scenario712Negative(rng *rand.Rand) int {
	return rng.Intn(1000000)
}
