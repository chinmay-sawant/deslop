package rulefixtures

import (
	"encoding/json"
	"io"
	gin "github.com/gin-gonic/gin"
	binding "github.com/gin-gonic/gin/binding"
)

// scenario for duplicate_upstream_calls_same_url_same_handler: Same upstream URL called multiple times in one handler.
// fixture polarity: positive; family: gin; severity: contextual.
type Rule310PositiveRecord struct {
	Name string `json:"name"`
}

func scenario310Positive(ctx *gin.Context) error {
	var first Rule310PositiveRecord
	if err := ctx.ShouldBindBodyWith(&first, binding.JSON); err != nil {
		return err
	}
	body, err := io.ReadAll(ctx.Request.Body)
	if err != nil {
		return err
	}
	data, err := json.Marshal(gin.H{"item": first, "raw": string(body)})
	if err != nil {
		return err
	}
	ctx.Data(200, "application/json", data)
	return nil
}
