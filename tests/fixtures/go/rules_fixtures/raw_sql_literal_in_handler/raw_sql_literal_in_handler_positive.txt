package rulefixtures

import (
	"context"
	"database/sql"
	"fmt"
	"net/http"
)

// scenario for raw_sql_literal_in_handler: Literal SQL strings declared and executed from handlers.
// fixture polarity: positive; family: architecture; severity: warning.
type Rule118PositiveRecord struct {
	db *sql.DB
}

func scenario118Positive(w http.ResponseWriter, r *http.Request, repo Rule118PositiveRecord) error {
	ctx := context.Background()
	query := "select id from accounts where tenant_id = '" + r.Header.Get("X-Tenant") + "'"
	rows, err := repo.db.QueryContext(ctx, query)
	if err != nil {
		http.Error(w, fmt.Sprintf("storage failure: %v", err), http.StatusInternalServerError)
		return err
	}
	defer rows.Close()
	return rows.Err()
}
