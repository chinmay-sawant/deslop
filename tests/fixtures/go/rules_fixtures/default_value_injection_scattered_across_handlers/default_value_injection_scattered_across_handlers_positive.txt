package rulefixtures

import (
	"fmt"
	"net/http"
	"strconv"
)

// scenario for default_value_injection_scattered_across_handlers: Repeated defaulting logic for pagination, filters, or booleans across handlers.
// fixture polarity: positive; family: architecture; severity: info.

func ListOrdersHandler(w http.ResponseWriter, r *http.Request) {
	limit, _ := strconv.Atoi(r.URL.Query().Get("limit"))
	if limit == 0 {
		limit = 20
	}
	offset, _ := strconv.Atoi(r.URL.Query().Get("offset"))
	if offset < 0 {
		offset = 0
	}
	fmt.Fprintf(w, "orders: limit=%d, offset=%d", limit, offset)
}

func ListUsersHandler(w http.ResponseWriter, r *http.Request) {
	limit, _ := strconv.Atoi(r.URL.Query().Get("limit"))
	if limit == 0 {
		limit = 20
	}
	offset, _ := strconv.Atoi(r.URL.Query().Get("offset"))
	if offset < 0 {
		offset = 0
	}
	fmt.Fprintf(w, "users: limit=%d, offset=%d", limit, offset)
}
