package rulefixtures

import (
	"encoding/json"
	"io"
	gin "github.com/gin-gonic/gin"
	binding "github.com/gin-gonic/gin/binding"
)

// scenario for env_or_config_lookup_per_request: Environment variable reads observed on request paths instead of cached configuration.
// fixture polarity: positive; family: gin; severity: contextual.
type Rule311PositiveRecord struct {
	Name string `json:"name"`
}

func scenario311Positive(ctx *gin.Context) error {
	var first Rule311PositiveRecord
	if err := ctx.ShouldBindBodyWith(&first, binding.JSON); err != nil {
		return err
	}
	body, err := io.ReadAll(ctx.Request.Body)
	if err != nil {
		return err
	}
	data, err := json.Marshal(gin.H{"item": first, "raw": string(body)})
	if err != nil {
		return err
	}
	ctx.Data(200, "application/json", data)
	return nil
}
