package rulefixtures

// scenario for clear_map_go121: `for k := range m { delete(m, k) }` in Go 1.21+ codebases
// fixture polarity: positive; family: performance; severity: info.

func scenario470Positive(cache map[string]*Item) {
	for k := range cache {
		delete(cache, k)
	}
}

type Item struct {
	ID    int
	Value string
}
