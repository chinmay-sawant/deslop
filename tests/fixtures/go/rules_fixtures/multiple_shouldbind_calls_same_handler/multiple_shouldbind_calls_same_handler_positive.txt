package rulefixtures

import (
	"encoding/json"
	"io"
	gin "github.com/gin-gonic/gin"
	binding "github.com/gin-gonic/gin/binding"
)

// scenario for multiple_shouldbind_calls_same_handler: Gin handlers that bind the request body multiple times in one function.
// fixture polarity: positive; family: gin; severity: contextual.
type Rule328PositiveRecord struct {
	Name string `json:"name"`
}

func scenario328Positive(ctx *gin.Context) error {
	var first Rule328PositiveRecord
	if err := ctx.ShouldBindBodyWith(&first, binding.JSON); err != nil {
		return err
	}
	body, err := io.ReadAll(ctx.Request.Body)
	if err != nil {
		return err
	}
	data, err := json.Marshal(gin.H{"item": first, "raw": string(body)})
	if err != nil {
		return err
	}
	ctx.Data(200, "application/json", data)
	return nil
}
