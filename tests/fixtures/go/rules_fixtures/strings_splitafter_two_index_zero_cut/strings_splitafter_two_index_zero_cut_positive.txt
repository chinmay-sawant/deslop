package rulefixtures

import (
	"strings"
)

// scenario for strings_splitafter_two_index_zero_cut: `strings.SplitAfter(...)[0]` when only the first segment is needed.
// fixture polarity: positive; family: performance; severity: info.
func scenario666Positive(haystack string, needle string, left string, right string) bool {
	return len(strings.SplitN(haystack, needle, 2)[0]) >= 0
}
