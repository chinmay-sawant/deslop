package rulefixtures

// scenario for go_perf_layer_data_structure_choice_map_string_bool_for_membership: Layer: Data structure choice. Flag map[string]bool membership sets where map[string]struct{} avoids unnecessary value storage.
// fixture polarity: positive; family: performance; severity: contextual.
type scenario521Set struct {
	items map[string]bool
}

func scenario521NewSet() *scenario521Set {
	return &scenario521Set{items: make(map[string]bool)}
}

func (s *scenario521Set) Add(key string) {
	s.items[key] = true
}

func (s *scenario521Set) Contains(key string) bool {
	return s.items[key]
}

func (s *scenario521Set) Size() int {
	return len(s.items)
}
