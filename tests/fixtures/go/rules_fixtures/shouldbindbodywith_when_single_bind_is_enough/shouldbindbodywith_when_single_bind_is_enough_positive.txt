package rulefixtures

import (
	"encoding/json"
	"io"
	gin "github.com/gin-gonic/gin"
	binding "github.com/gin-gonic/gin/binding"
)

// scenario for shouldbindbodywith_when_single_bind_is_enough: Gin handlers that use ShouldBindBodyWith(...) even though only one body bind is observed.
// fixture polarity: positive; family: gin; severity: contextual.
type Rule337PositiveRecord struct {
	Name string `json:"name"`
}

func scenario337Positive(ctx *gin.Context) error {
	var first Rule337PositiveRecord
	if err := ctx.ShouldBindBodyWith(&first, binding.JSON); err != nil {
		return err
	}
	body, err := io.ReadAll(ctx.Request.Body)
	if err != nil {
		return err
	}
	data, err := json.Marshal(gin.H{"item": first, "raw": string(body)})
	if err != nil {
		return err
	}
	ctx.Data(200, "application/json", data)
	return nil
}
