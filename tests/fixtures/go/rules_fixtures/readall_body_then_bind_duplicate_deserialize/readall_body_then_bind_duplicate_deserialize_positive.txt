package rulefixtures

import (
	"encoding/json"
	"io"
	gin "github.com/gin-gonic/gin"
	binding "github.com/gin-gonic/gin/binding"
)

// scenario for readall_body_then_bind_duplicate_deserialize: Gin handlers that materialize c.Request.Body with io.ReadAll(...) and then bind the same body again.
// fixture polarity: positive; family: gin; severity: contextual.
type Rule332PositiveRecord struct {
	Name string `json:"name"`
}

func scenario332Positive(ctx *gin.Context) error {
	var first Rule332PositiveRecord
	if err := ctx.ShouldBindBodyWith(&first, binding.JSON); err != nil {
		return err
	}
	body, err := io.ReadAll(ctx.Request.Body)
	if err != nil {
		return err
	}
	data, err := json.Marshal(gin.H{"item": first, "raw": string(body)})
	if err != nil {
		return err
	}
	ctx.Data(200, "application/json", data)
	return nil
}
