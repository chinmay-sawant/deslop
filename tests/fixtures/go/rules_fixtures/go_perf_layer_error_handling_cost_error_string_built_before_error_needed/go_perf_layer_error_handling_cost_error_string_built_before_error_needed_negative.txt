package rulefixtures

import "fmt"

// scenario for go_perf_layer_error_handling_cost_error_string_built_before_error_needed: Layer: Error handling cost. Flag error message formatting before the code knows an error will be returned.
// fixture polarity: negative; family: performance; severity: contextual.
func scenario529Negative(key string) error {
	value, err := lookup529(key)
	if err != nil {
		msg := fmt.Sprintf("lookup failed for %s", key)
		return fmt.Errorf("%s: %w", msg, err)
	}
	if value == "" {
		return nil
	}
	return nil
}

func lookup529(key string) (string, error) {
	return key, nil
}
