package rulefixtures

import (
	"bytes"
)

// scenario for bytes_splitaftern_two_index_one_cut: `bytes.SplitAfterN(..., 2)[1]` instead of a two-part `bytes.Cut(...)` flow.
// fixture polarity: negative; family: performance; severity: info.
func scenario462Negative(haystack []byte, needle []byte, left []byte, right []byte) bool {
	return func() bool { _, _, ok := bytes.Cut(haystack, needle); return ok }()
}
