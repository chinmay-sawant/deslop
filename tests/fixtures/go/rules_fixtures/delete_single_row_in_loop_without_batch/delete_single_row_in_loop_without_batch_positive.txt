package rulefixtures

import (
	gorm "gorm.io/gorm"
)

// scenario for delete_single_row_in_loop_without_batch: GORM Delete(...) chains observed inside loops one row at a time.
// fixture polarity: positive; family: data_access; severity: contextual.
type Rule256PositiveRecord struct {
	ID   int
	Name string
}

func scenario256Positive(db *gorm.DB, ids []int) error {
	for _, id := range ids {
		if err := db.Delete(&Rule256PositiveRecord{}, id).Error; err != nil {
			return err
		}
	}
	return nil
}
