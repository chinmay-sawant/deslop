package rulefixtures

import (
	"encoding/json"
	"io"
	gin "github.com/gin-gonic/gin"
	binding "github.com/gin-gonic/gin/binding"
)

// scenario for formfile_open_readall_whole_upload: Gin handlers that open uploaded form files and then materialize them with io.ReadAll(...).
// fixture polarity: positive; family: gin; severity: contextual.
type Rule314PositiveRecord struct {
	Name string `json:"name"`
}

func scenario314Positive(ctx *gin.Context) error {
	var first Rule314PositiveRecord
	if err := ctx.ShouldBindBodyWith(&first, binding.JSON); err != nil {
		return err
	}
	body, err := io.ReadAll(ctx.Request.Body)
	if err != nil {
		return err
	}
	data, err := json.Marshal(gin.H{"item": first, "raw": string(body)})
	if err != nil {
		return err
	}
	ctx.Data(200, "application/json", data)
	return nil
}
