package rulefixtures

import (
	"encoding/json"
	"io"
	gin "github.com/gin-gonic/gin"
	binding "github.com/gin-gonic/gin/binding"
)

// scenario for dumprequest_or_dumpresponse_in_hot_path: Request-path handlers that dump full HTTP requests or responses with httputil.
// fixture polarity: positive; family: gin; severity: contextual.
type Rule309PositiveRecord struct {
	Name string `json:"name"`
}

func scenario309Positive(ctx *gin.Context) error {
	var first Rule309PositiveRecord
	if err := ctx.ShouldBindBodyWith(&first, binding.JSON); err != nil {
		return err
	}
	body, err := io.ReadAll(ctx.Request.Body)
	if err != nil {
		return err
	}
	data, err := json.Marshal(gin.H{"item": first, "raw": string(body)})
	if err != nil {
		return err
	}
	ctx.Data(200, "application/json", data)
	return nil
}
