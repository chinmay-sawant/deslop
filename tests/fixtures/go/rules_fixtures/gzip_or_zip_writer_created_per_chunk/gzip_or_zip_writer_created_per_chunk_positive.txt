package rulefixtures

import (
	"encoding/json"
	"io"
	gin "github.com/gin-gonic/gin"
	binding "github.com/gin-gonic/gin/binding"
)

// scenario for gzip_or_zip_writer_created_per_chunk: Gzip or zip writers recreated per chunk inside handler loops instead of reusing per stream.
// fixture polarity: positive; family: gin; severity: contextual.
type Rule318PositiveRecord struct {
	Name string `json:"name"`
}

func scenario318Positive(ctx *gin.Context) error {
	var first Rule318PositiveRecord
	if err := ctx.ShouldBindBodyWith(&first, binding.JSON); err != nil {
		return err
	}
	body, err := io.ReadAll(ctx.Request.Body)
	if err != nil {
		return err
	}
	data, err := json.Marshal(gin.H{"item": first, "raw": string(body)})
	if err != nil {
		return err
	}
	ctx.Data(200, "application/json", data)
	return nil
}
