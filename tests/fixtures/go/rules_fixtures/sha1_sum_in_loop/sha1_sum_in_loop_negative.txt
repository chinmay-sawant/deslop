package rulefixtures

// scenario for sha1_sum_in_loop: `sha1.Sum(...)` inside loops.
// fixture polarity: negative; family: performance; severity: info.
func scenario636Negative(chunks [][]byte) int {
	total := 0
	for _, payload := range chunks {
		total += len(payload)
	}
	return total
}
