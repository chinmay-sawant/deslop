package rulefixtures

import (
	"encoding/json"
	"io"
	gin "github.com/gin-gonic/gin"
	binding "github.com/gin-gonic/gin/binding"
)

// scenario for template_parse_in_handler: Template construction or parsing inside Gin handlers instead of startup-time caching.
// fixture polarity: positive; family: gin; severity: contextual.
type Rule338PositiveRecord struct {
	Name string `json:"name"`
}

func scenario338Positive(ctx *gin.Context) error {
	var first Rule338PositiveRecord
	if err := ctx.ShouldBindBodyWith(&first, binding.JSON); err != nil {
		return err
	}
	body, err := io.ReadAll(ctx.Request.Body)
	if err != nil {
		return err
	}
	data, err := json.Marshal(gin.H{"item": first, "raw": string(body)})
	if err != nil {
		return err
	}
	ctx.Data(200, "application/json", data)
	return nil
}
