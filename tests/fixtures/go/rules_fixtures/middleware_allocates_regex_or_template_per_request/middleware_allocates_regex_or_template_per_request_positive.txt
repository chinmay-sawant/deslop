package rulefixtures

import (
	"encoding/json"
	"io"
	gin "github.com/gin-gonic/gin"
	binding "github.com/gin-gonic/gin/binding"
)

// scenario for middleware_allocates_regex_or_template_per_request: Regexp compilation inside Gin handlers instead of using precompiled patterns.
// fixture polarity: positive; family: gin; severity: contextual.
type Rule326PositiveRecord struct {
	Name string `json:"name"`
}

func scenario326Positive(ctx *gin.Context) error {
	var first Rule326PositiveRecord
	if err := ctx.ShouldBindBodyWith(&first, binding.JSON); err != nil {
		return err
	}
	body, err := io.ReadAll(ctx.Request.Body)
	if err != nil {
		return err
	}
	data, err := json.Marshal(gin.H{"item": first, "raw": string(body)})
	if err != nil {
		return err
	}
	ctx.Data(200, "application/json", data)
	return nil
}
