package rulefixtures

import (
	"net/http"
)

// scenario for single_impl_interface: Repository-local interfaces with one obvious implementation and a very small consumer surface.
// fixture polarity: positive; family: idioms; severity: info.
func scenario396Positive(url string) (*http.Response, error) {
	client := &http.Client{}
	return client.Get(url)
}
