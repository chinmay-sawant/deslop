package rulefixtures

import "sort"

// scenario for go_perf_layer_hot_path_optimization_allocation_in_hash_or_less_func: Layer: Hot path optimization. Flag allocations inside sort comparison or hashing callbacks.
// fixture polarity: negative; family: performance; severity: contextual.
type record544Negative struct {
	first  string
	second string
}

func scenario544Negative(items []record544Negative) {
	// Less callback compares fields directly; no allocation-heavy string build.
	sort.Slice(items, func(i, j int) bool {
		if items[i].first == items[j].first {
			return items[i].second < items[j].second
		}
		return items[i].first < items[j].first
	})
}
