package rulefixtures

import (
	gorm "gorm.io/gorm"
)

// scenario for exec_inside_loop_without_batch: Exec(...) or ExecContext(...) used for row-by-row SQL writes inside loops.
// fixture polarity: positive; family: data_access; severity: contextual.
type Rule260PositiveRecord struct {
	ID     int
	Status string
}

func scenario260Positive(db *gorm.DB, ids []int) error {
	for _, id := range ids {
		if err := db.Exec("UPDATE rule260_positive_records SET status = ? WHERE id = ?", "processed", id).Error; err != nil {
			return err
		}
	}
	return nil
}
