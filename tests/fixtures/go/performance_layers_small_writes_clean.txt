package perflayers

import (
    "bufio"
    "os"
)

func SmallWritesClean(file *os.File, lines []string) {
    writer := bufio.NewWriter(file)
    for _, line := range lines {
        _, _ = writer.WriteString(line)
    }
    _ = writer.Flush()
}