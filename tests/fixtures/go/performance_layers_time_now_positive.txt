package perflayers

import "time"

func ProcessBatchPositive(items []int) []time.Time {
    stamps := make([]time.Time, 0, len(items))
    for range items {
        started := time.Now()
        finished := time.Now()
        stamps = append(stamps, started, finished)
    }
    return stamps
}