package perflayers

func MultiplePassesClean(values []int) int {
    total := 0
    count := 0
    for _, value := range values {
        total += value
        count++
    }
    return total + count
}