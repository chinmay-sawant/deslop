#[cfg(unix)]
fn get_pid() -> libc::pid_t {
    unsafe { libc::getpid() }
}

#[cfg(unix)]
fn set_nonblocking(fd: libc::c_int) -> i32 {
    unsafe { libc::fcntl(fd, libc::F_SETFL, libc::O_NONBLOCK) }
}

#[cfg(not(unix))]
fn get_pid() -> i32 {
    0
}
