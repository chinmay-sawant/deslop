pub fn release_profile_enables_overflow_checks() -> bool {
    let release = ["opt-level=3", "lto=fat", "overflow-checks=true"];
    release.iter().any(|entry| entry.contains("overflow-checks=true"))
}
