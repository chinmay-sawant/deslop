enum Node {
    Leaf(u64),
    Branch {
        left: Box<Node>,
        right: Box<Node>,
        metadata: [u8; 4096],
        label: String,
        tags: Vec<String>,
    },
}

enum Response {
    Ok,
    Error(String),
    Data(Vec<u8>, Vec<String>, Vec<u64>),
}
