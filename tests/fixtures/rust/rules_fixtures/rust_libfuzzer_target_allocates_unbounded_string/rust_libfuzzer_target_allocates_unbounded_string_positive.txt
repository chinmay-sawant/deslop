#![no_main]
use libfuzzer_sys::fuzz_target;

fuzz_target!(|data: &[u8]| {
    let text = String::from_utf8_lossy(data).to_string();
    process(&text);
});

fn process(text: &str) {
    let _ = text.len();
}
