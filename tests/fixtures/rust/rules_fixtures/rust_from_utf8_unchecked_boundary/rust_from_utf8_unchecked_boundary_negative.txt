use std::str;

fn parse_ffi_string(raw: &[u8]) -> Result<&str, std::str::Utf8Error> {
    str::from_utf8(raw)
}

fn handle_request(body: &[u8]) -> Result<String, String> {
    let text = str::from_utf8(body).map_err(|e| format!("invalid UTF-8: {}", e))?;
    Ok(text.to_uppercase())
}
