use std::ffi::CString;

fn open_no_follow(path: &str) -> i32 {
    let c_path = CString::new(path).unwrap();
    unsafe {
        libc::open(c_path.as_ptr(), libc::O_RDONLY | libc::O_NOFOLLOW)
    }
}
