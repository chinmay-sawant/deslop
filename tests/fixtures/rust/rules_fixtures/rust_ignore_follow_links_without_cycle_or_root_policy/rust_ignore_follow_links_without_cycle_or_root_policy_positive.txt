use walkdir::WalkDir;

fn scan_following_links(root: &str) -> Vec<String> {
    WalkDir::new(root)
        .follow_links(true)
        .into_iter()
        .filter_map(|e| e.ok())
        .map(|e| e.path().to_string_lossy().to_string())
        .collect()
}
