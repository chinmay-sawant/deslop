[workspace]
members = ["app", "tools"]
default-members = ["app"]

[workspace.dependencies]
serde_json = "1.0.149"
clap = { version = "4.6.0", default-features = false, features = ["derive"] }

[dependencies]
serde_json = { workspace = true, default-features = false }
clap = { workspace = true, default-features = false }

[profile.release]
lto = "fat"
