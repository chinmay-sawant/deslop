var e=`python`,t=`hot_path`,n=`Hot Path`,r=`Hot-path loops and repeated work in frequently executed code.`,i=[{id:`append_then_sort_each_iteration`,label:`Append Then Sort Each Iteration`,description:`A collection is appended to and then sorted on each iteration instead of sorting once after accumulation.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, Append Then Sort Each Iteration matters because the pattern here, A collection is appended to and then sorted on each iteration instead of sorting once after accumulation, shows that the code is bypassing a clearer or safer default that readers usually expect to see.`,fix:`For Append Then Sort Each Iteration in Python, prefer sorting once after accumulation when the semantics match, and keep the expected or cheaper API choice as the default.`},{id:`csv_writer_flush_per_row`,label:`Csv Writer Flush Per Row`,description:`csv.Writer flushes on each row instead of buffering a larger batch.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, Csv Writer Flush Per Row matters because the pattern here, csv.Writer flushes on each row instead of buffering a larger batch, shows that the code is bypassing a clearer or safer default that readers usually expect to see.`,fix:`For Csv Writer Flush Per Row in Python, prefer buffering a larger batch when the semantics match, and keep the expected or cheaper API choice as the default.`},{id:`dict_items_or_keys_materialized_in_loop`,label:`Dict Items or Keys Materialized in Loop`,description:`dict.items(), keys(), or values() are repeatedly materialized inside loops.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, Dict Items or Keys Materialized in Loop matters because the pattern here, dict.items(), keys(), or values() are repeatedly materialized inside loops, pushes avoidable work into a repeated path where the cost compounds quickly.`,fix:`For Dict Items or Keys Materialized in Loop in Python, move the one-time work out of the repeated path, cache reusable state, or batch the operation so the loop only does per-item work.`},{id:`enumerate_on_range_len`,label:`Enumerate on Range Len`,description:`enumerate(range(len(...))) style loops that add indexing ceremony without extra value.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, Enumerate on Range Len matters because the pattern here, enumerate(range(len(...))) style loops that add indexing ceremony without extra value, removes a guardrail or signal that reviewers expect before trusting the surrounding code.`,fix:`For Enumerate on Range Len in Python, add extra value or move the code to a path where that safeguard is guaranteed before the risky work happens.`},{id:`filter_then_count_then_iterate`,label:`Filter Then Count Then Iterate`,description:`The same collection is traversed repeatedly for filtering, counting, and later iteration.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, Filter Then Count Then Iterate matters because the pattern here, The same collection is traversed repeatedly for filtering, counting, and later iteration, is worth review because repeated work in Python gets expensive fast when the path runs for every request, batch, or record.`,fix:`For Filter Then Count Then Iterate in Python, precompute what you can once, reuse objects or compiled state, and avoid rebuilding the same work inside the loop.`},{id:`in_check_on_list_literal`,label:`In Check on List Literal`,description:`Membership tests against list literals where a tuple or set would be clearer or cheaper.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, In Check on List Literal matters because the pattern here, Membership tests against list literals where a tuple or set would be clearer or cheaper, is worth review because repeated work in Python gets expensive fast when the path runs for every request, batch, or record.`,fix:`For In Check on List Literal in Python, precompute what you can once, reuse objects or compiled state, and avoid rebuilding the same work inside the loop.`},{id:`json_encoder_recreated_per_item`,label:`JSON Encoder Recreated Per Item`,description:`A JSON encoder object is recreated per item instead of being reused for the stream.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, JSON Encoder Recreated Per Item matters because the pattern here, A JSON encoder object is recreated per item instead of being reused for the stream, shows that the code is bypassing a clearer or safer default that readers usually expect to see.`,fix:`For JSON Encoder Recreated Per Item in Python, prefer being reused for the stream when the semantics match, and keep the expected or cheaper API choice as the default.`},{id:`json_loads_same_payload_multiple_times`,label:`JSON Loads Same Payload Multiple Times`,description:`The same JSON payload is decoded multiple times inside one function instead of caching the parsed value.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, JSON Loads Same Payload Multiple Times matters because the pattern here, The same JSON payload is decoded multiple times inside one function instead of caching the parsed value, shows that the code is bypassing a clearer or safer default that readers usually expect to see.`,fix:`For JSON Loads Same Payload Multiple Times in Python, prefer caching the parsed value when the semantics match, and keep the expected or cheaper API choice as the default.`},{id:`list_comprehension_only_for_length`,label:`List Comprehension Only for Length`,description:`A list comprehension is built only so len(...) can be called on it.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, List Comprehension Only for Length matters because the pattern here, A list comprehension is built only so len(...) can be called on it, is worth review because repeated work in Python gets expensive fast when the path runs for every request, batch, or record.`,fix:`For List Comprehension Only for Length in Python, precompute what you can once, reuse objects or compiled state, and avoid rebuilding the same work inside the loop.`},{id:`read_then_splitlines`,label:`Read Then Splitlines`,description:`File contents are fully read and then splitlines() is called instead of streaming lines.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, Read Then Splitlines matters because the pattern here, File contents are fully read and then splitlines() is called instead of streaming lines, shows that the code is bypassing a clearer or safer default that readers usually expect to see.`,fix:`For Read Then Splitlines in Python, prefer streaming lines when the semantics match, and keep the expected or cheaper API choice as the default.`},{id:`readlines_then_iterate`,label:`Readlines Then Iterate`,description:`readlines() materializes the whole file before line-by-line iteration.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, Readlines Then Iterate matters because the pattern here, readlines() materializes the whole file before line-by-line iteration, is worth review because repeated work in Python gets expensive fast when the path runs for every request, batch, or record.`,fix:`For Readlines Then Iterate in Python, precompute what you can once, reuse objects or compiled state, and avoid rebuilding the same work inside the loop.`},{id:`regex_compile_in_hot_path`,label:`Regex Compile in Hot Path`,description:`re.compile(...) or similar regex compilation repeated inside hot code paths.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, Regex Compile in Hot Path matters because the pattern here, re.compile(...) or similar regex compilation repeated inside hot code paths, turns local work into repeated cost that spreads with traffic or input size.`,fix:`For Regex Compile in Hot Path in Python, move one-time initialization, parsing, or allocation outside the loop when the per-iteration state does not actually change.`},{id:`repeated_json_dumps_same_object`,label:`Repeated JSON Dumps Same Object`,description:`json.dumps(...) is repeated for the same object instead of caching the serialized value.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, Repeated JSON Dumps Same Object matters because the pattern here, json.dumps(...) is repeated for the same object instead of caching the serialized value, shows that the code is bypassing a clearer or safer default that readers usually expect to see.`,fix:`For Repeated JSON Dumps Same Object in Python, prefer caching the serialized value when the semantics match, and keep the expected or cheaper API choice as the default.`},{id:`repeated_open_same_file_in_function`,label:`Repeated Open Same File in Function`,description:`The same file appears to be opened multiple times within one function.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, Repeated Open Same File in Function matters because the pattern here, The same file appears to be opened multiple times within one function, turns local work into repeated cost that spreads with traffic or input size.`,fix:`For Repeated Open Same File in Function in Python, move one-time initialization, parsing, or allocation outside the loop when the per-iteration state does not actually change.`},{id:`sorted_only_for_first_element`,label:`Sorted Only for First Element`,description:`A sequence is fully sorted even though only the first or smallest element is used.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, Sorted Only for First Element matters because the pattern here, A sequence is fully sorted even though only the first or smallest element is used, is worth review because repeated work in Python gets expensive fast when the path runs for every request, batch, or record.`,fix:`For Sorted Only for First Element in Python, precompute what you can once, reuse objects or compiled state, and avoid rebuilding the same work inside the loop.`},{id:`string_startswith_endswith_chain`,label:`String Startswith Endswith Chain`,description:`Repeated startswith(...) or endswith(...) checks that can often be combined into tuple-based calls.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, String Startswith Endswith Chain matters because the pattern here, Repeated startswith(...) or endswith(...) checks that can often be combined into tuple-based calls, turns local work into repeated cost that spreads with traffic or input size.`,fix:`For String Startswith Endswith Chain in Python, prefer a builder, buffer, or one-time composition step instead of growing the same value incrementally.`},{id:`write_without_buffering_in_loop`,label:`Write without Buffering in Loop`,description:`Repeated writes inside loops with no visible buffering or batching.`,defaultSeverity:`info`,status:`stable`,configurability:[`disable`,`ignore`,`severity_override`],explanation:`In Python, Write without Buffering in Loop matters because the pattern here, Repeated writes inside loops with no visible buffering or batching, pushes avoidable work into a repeated path where the cost compounds quickly.`,fix:`For Write without Buffering in Loop in Python, move the one-time work out of the repeated path, cache reusable state, or batch the operation so the loop only does per-item work.`}],a={language:e,family:t,label:n,summary:r,rules:i};export{a as default,t as family,n as label,e as language,i as rules,r as summary};