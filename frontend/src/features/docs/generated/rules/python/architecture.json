{
  "language": "python",
  "family": "architecture",
  "label": "Architecture",
  "summary": "Repository, service, DTO, and package ownership drift.",
  "rules": [
    {
      "id": "adapter_boundary_missing_for_external_payload_shape",
      "label": "Adapter Boundary Missing for External Payload Shape",
      "description": "Flag code that lets raw third-party dicts, tuples, or response objects spread through unrelated modules without normalization.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Adapter Boundary Missing for External Payload Shape matters because the pattern here, Flag code that lets raw third-party dicts, tuples, or response objects spread through unrelated modules without normalization, removes a guardrail or signal that reviewers expect before trusting the surrounding code.",
      "fix": "For Adapter Boundary Missing for External Payload Shape in Python, add normalization or move the code to a path where that safeguard is guaranteed before the risky work happens."
    },
    {
      "id": "asyncio_gather_without_return_exceptions_on_partial_failure_path",
      "label": "Asyncio Gather without Return Exceptions on Partial Failure Path",
      "description": "asyncio.gather called without return_exceptions=True on a code path where partial failure should be recoverable.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Asyncio Gather without Return Exceptions on Partial Failure Path matters because the pattern here, asyncio.gather called without return_exceptions=True on a code path where partial failure should be recoverable, removes a guardrail or signal that reviewers expect before trusting the surrounding code.",
      "fix": "For Asyncio Gather without Return Exceptions on Partial Failure Path in Python, add return_exceptions=True on a code path where partial failure should be recoverable or move the code to a path where that safeguard is guaranteed before the risky work happens."
    },
    {
      "id": "asyncio_get_event_loop_at_module_scope",
      "label": "Asyncio Get Event Loop at Module Scope",
      "description": "asyncio.get_event_loop() or get_running_loop() called at module import scope outside any function.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Asyncio Get Event Loop at Module Scope matters because the pattern here, asyncio.get_event_loop() or get_running_loop() called at module import scope outside any function, makes ownership and navigation harder when someone has to trace behavior across modules or services.",
      "fix": "For Asyncio Get Event Loop at Module Scope in Python, move the code to the package, module, router, or entrypoint layer that already owns this boundary so navigation and responsibility stay aligned."
    },
    {
      "id": "asyncio_queue_created_without_maxsize_in_producer_path",
      "label": "Asyncio Queue Created without Maxsize in Producer Path",
      "description": "asyncio.Queue created without maxsize on a producer code path, allowing unbounded memory growth.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Asyncio Queue Created without Maxsize in Producer Path matters because the pattern here, asyncio.Queue created without maxsize on a producer code path, allowing unbounded memory growth, removes a guardrail or signal that reviewers expect before trusting the surrounding code.",
      "fix": "For Asyncio Queue Created without Maxsize in Producer Path in Python, add maxsize on a producer code path, allowing unbounded memory growth or move the code to a path where that safeguard is guaranteed before the risky work happens."
    },
    {
      "id": "asyncio_sleep_zero_busy_wait_pattern",
      "label": "Asyncio Sleep Zero Busy Wait Pattern",
      "description": "asyncio.sleep(0) used in a tight loop as a busy-wait yield pattern instead of a proper event-driven wait.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Asyncio Sleep Zero Busy Wait Pattern matters because the pattern here, asyncio.sleep(0) used in a tight loop as a busy-wait yield pattern instead of a proper event-driven wait, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For Asyncio Sleep Zero Busy Wait Pattern in Python, prefer a proper event-driven wait when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "auth_extraction_duplicated_across_views",
      "label": "Auth Extraction Duplicated Across Views",
      "description": "Multiple view functions repeat identical auth or principal-extraction logic instead of sharing via middleware or dependency.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Auth Extraction Duplicated Across Views matters because the pattern here, Multiple view functions repeat identical auth or principal-extraction logic instead of sharing via middleware or dependency, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For Auth Extraction Duplicated Across Views in Python, prefer sharing via middleware or dependency when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "background_job_depends_on_request_context_object",
      "label": "Background Job Depends on Request Context Object",
      "description": "Celery, RQ, or background task function holds a reference to an HTTP request context object across the task boundary.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Background Job Depends on Request Context Object matters because the pattern here, Celery, RQ, or background task function holds a reference to an HTTP request context object across the task boundary, creates lifecycle behavior that is easy to miss until the system is under load or shutting down.",
      "fix": "For Background Job Depends on Request Context Object in Python, give the work an explicit owner, pass the right context or handle, and make cancellation or shutdown visible in the API."
    },
    {
      "id": "business_logic_inside_middleware",
      "label": "Business Logic Inside Middleware",
      "description": "Middleware component encodes domain decision logic beyond cross-cutting concerns such as auth and logging.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Business Logic Inside Middleware matters because the pattern here, Middleware component encodes domain decision logic beyond cross-cutting concerns such as auth and logging, blurs the boundary between request plumbing and the rest of the application.",
      "fix": "For Business Logic Inside Middleware in Python, move the non-transport work into middleware, services, bootstrap code, or shared helpers so the request edge stays narrow."
    },
    {
      "id": "business_rule_mixed_with_serialization_mapping",
      "label": "Business Rule Mixed with Serialization Mapping",
      "description": "Flag functions that both enforce domain rules and convert to wire or storage payloads in the same block.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Business Rule Mixed with Serialization Mapping matters because the pattern here, Flag functions that both enforce domain rules and convert to wire or storage payloads in the same block, is worth review because soft module boundaries make it easy for transport, persistence, and business logic to blur together over time.",
      "fix": "For Business Rule Mixed with Serialization Mapping in Python, choose one owner for mapping, orchestration, and data access, then keep each package aligned with that role."
    },
    {
      "id": "celery_or_rq_task_imports_web_framework_app",
      "label": "Celery or Rq Task Imports Web Framework App",
      "description": "Celery or RQ task module imports the Flask app object or FastAPI application at the top level.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Celery or Rq Task Imports Web Framework App matters because the pattern here, Celery or RQ task module imports the Flask app object or FastAPI application at the top level, makes ownership and navigation harder when someone has to trace behavior across modules or services.",
      "fix": "For Celery or Rq Task Imports Web Framework App in Python, move the code to the package, module, router, or entrypoint layer that already owns this boundary so navigation and responsibility stay aligned."
    },
    {
      "id": "command_or_task_mutates_shared_process_state_directly",
      "label": "Command or Task Mutates Shared Process State Directly",
      "description": "Flag command handlers or task functions that directly rewrite module-level caches or registries used elsewhere.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Command or Task Mutates Shared Process State Directly matters because the pattern here, Flag command handlers or task functions that directly rewrite module-level caches or registries used elsewhere, makes ownership and navigation harder when someone has to trace behavior across modules or services.",
      "fix": "For Command or Task Mutates Shared Process State Directly in Python, move the code to the package, module, router, or entrypoint layer that already owns this boundary so navigation and responsibility stay aligned."
    },
    {
      "id": "concurrent_futures_executor_not_shut_down",
      "label": "Concurrent Futures Executor Not Shut Down",
      "description": "ThreadPoolExecutor or ProcessPoolExecutor created without shutdown() or a context manager, leaking threads.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Concurrent Futures Executor Not Shut Down matters because the pattern here, ThreadPoolExecutor or ProcessPoolExecutor created without shutdown() or a context manager, leaking threads, removes a guardrail or signal that reviewers expect before trusting the surrounding code.",
      "fix": "For Concurrent Futures Executor Not Shut Down in Python, add shutdown() or a context manager, leaking threads or move the code to a path where that safeguard is guaranteed before the risky work happens."
    },
    {
      "id": "constructor_reads_global_config_inline",
      "label": "Constructor Reads Global Config Inline",
      "description": "Flag classes whose constructors pull environment variables or global config directly instead of receiving normalized dependencies.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Constructor Reads Global Config Inline matters because the pattern here, Flag classes whose constructors pull environment variables or global config directly instead of receiving normalized dependencies, pushes configuration or startup ownership into the wrong phase of the program.",
      "fix": "For Constructor Reads Global Config Inline in Python, load, validate, and wire this configuration at startup so request paths and imports only consume already-owned state."
    },
    {
      "id": "core_model_reads_process_environment",
      "label": "Core Model Reads Process Environment",
      "description": "Flag core classes or pure helper layers that directly access os.environ, os.getenv, or global settings.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Core Model Reads Process Environment matters because the pattern here, Flag core classes or pure helper layers that directly access os.environ, os.getenv, or global settings, pushes configuration or startup ownership into the wrong phase of the program.",
      "fix": "For Core Model Reads Process Environment in Python, load, validate, and wire this configuration at startup so request paths and imports only consume already-owned state."
    },
    {
      "id": "coroutine_result_discarded_without_await",
      "label": "Coroutine Result Discarded without Await",
      "description": "Coroutine object assigned or returned without await, so the coroutine body never executes.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Coroutine Result Discarded without Await matters because the pattern here, Coroutine object assigned or returned without await, so the coroutine body never executes, removes a guardrail or signal that reviewers expect before trusting the surrounding code.",
      "fix": "For Coroutine Result Discarded without Await in Python, add await, so the coroutine body never executes or move the code to a path where that safeguard is guaranteed before the risky work happens."
    },
    {
      "id": "data_mapper_contains_business_decision_tree",
      "label": "Data Mapper Contains Business Decision Tree",
      "description": "Flag mappers or adapters that choose policy outcomes while converting data.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Data Mapper Contains Business Decision Tree matters because the pattern here, Flag mappers or adapters that choose policy outcomes while converting data, lets layer-specific contracts leak into places that should stay simpler and easier to test.",
      "fix": "For Data Mapper Contains Business Decision Tree in Python, choose one layer to own the contract shape and perform the mapping there instead of passing boundary-specific types through the stack."
    },
    {
      "id": "dependency_injection_bypassed_via_global_singleton",
      "label": "Dependency Injection Bypassed Via Global Singleton",
      "description": "Function bypasses the DI container and retrieves a collaborator via a global variable or module-level singleton.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Dependency Injection Bypassed Via Global Singleton matters because the pattern here, Function bypasses the DI container and retrieves a collaborator via a global variable or module-level singleton, makes ownership and navigation harder when someone has to trace behavior across modules or services.",
      "fix": "For Dependency Injection Bypassed Via Global Singleton in Python, move the code to the package, module, router, or entrypoint layer that already owns this boundary so navigation and responsibility stay aligned."
    },
    {
      "id": "domain_model_class_imports_http_library",
      "label": "Domain Model Class Imports HTTP Library",
      "description": "Domain model or entity file imports an HTTP framework module such as Flask, Django, or FastAPI.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Domain Model Class Imports HTTP Library matters because the pattern here, Domain model or entity file imports an HTTP framework module such as Flask, Django, or FastAPI, makes ownership and navigation harder when someone has to trace behavior across modules or services.",
      "fix": "For Domain Model Class Imports HTTP Library in Python, move the code to the package, module, router, or entrypoint layer that already owns this boundary so navigation and responsibility stay aligned."
    },
    {
      "id": "domain_object_performs_external_io",
      "label": "Domain Object Performs External I/O",
      "description": "Flag domain or value-like objects that open files, spawn subprocesses, or call network clients during core operations.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Domain Object Performs External I/O matters because the pattern here, Flag domain or value-like objects that open files, spawn subprocesses, or call network clients during core operations, creates lifecycle behavior that is easy to miss until the system is under load or shutting down.",
      "fix": "For Domain Object Performs External I/O in Python, give the work an explicit owner, pass the right context or handle, and make cancellation or shutdown visible in the API."
    },
    {
      "id": "entrypoint_builds_dependency_graph_inside_hot_function",
      "label": "Entrypoint Builds Dependency Graph Inside Hot Function",
      "description": "Flag request, batch, or loop entrypoints that reconstruct clients, caches, or providers on each call instead of wiring them once.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Entrypoint Builds Dependency Graph Inside Hot Function matters because the pattern here, Flag request, batch, or loop entrypoints that reconstruct clients, caches, or providers on each call instead of wiring them once, makes ownership and navigation harder when someone has to trace behavior across modules or services.",
      "fix": "For Entrypoint Builds Dependency Graph Inside Hot Function in Python, move the code to the package, module, router, or entrypoint layer that already owns this boundary so navigation and responsibility stay aligned."
    },
    {
      "id": "feature_logic_embedded_in_process_entrypoint",
      "label": "Feature Logic Embedded in Process Entrypoint",
      "description": "Flag CLI, worker, or service entrypoints that own business branching instead of delegating to focused application services.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Feature Logic Embedded in Process Entrypoint matters because the pattern here, Flag CLI, worker, or service entrypoints that own business branching instead of delegating to focused application services, makes ownership and navigation harder when someone has to trace behavior across modules or services.",
      "fix": "For Feature Logic Embedded in Process Entrypoint in Python, move the code to the package, module, router, or entrypoint layer that already owns this boundary so navigation and responsibility stay aligned."
    },
    {
      "id": "function_accepts_too_many_cross_cutting_dependencies",
      "label": "Function Accepts Too Many Cross Cutting Dependencies",
      "description": "Flag functions or constructors that accept a broad mix of logging, config, cache, clock, auth, and persistence dependencies with no cohesive abstraction.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Function Accepts Too Many Cross Cutting Dependencies matters because the pattern here, Flag functions or constructors that accept a broad mix of logging, config, cache, clock, auth, and persistence dependencies with no cohesive abstraction, pushes configuration or startup ownership into the wrong phase of the program.",
      "fix": "For Function Accepts Too Many Cross Cutting Dependencies in Python, load, validate, and wire this configuration at startup so request paths and imports only consume already-owned state."
    },
    {
      "id": "function_returns_domain_value_and_transport_metadata",
      "label": "Function Returns Domain Value and Transport Metadata",
      "description": "Flag APIs that mix domain results with transport metadata such as status-like fields or side-channel details in one return shape.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Function Returns Domain Value and Transport Metadata matters because the pattern here, Flag APIs that mix domain results with transport metadata such as status-like fields or side-channel details in one return shape, creates lifecycle behavior that is easy to miss until the system is under load or shutting down.",
      "fix": "For Function Returns Domain Value and Transport Metadata in Python, give the work an explicit owner, pass the right context or handle, and make cancellation or shutdown visible in the API."
    },
    {
      "id": "handler_or_view_builds_raw_sql",
      "label": "Handler or View Builds Raw SQL",
      "description": "Route handler or view constructs a raw SQL string instead of delegating to a repository layer.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Handler or View Builds Raw SQL matters because the pattern here, Route handler or view constructs a raw SQL string instead of delegating to a repository layer, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For Handler or View Builds Raw SQL in Python, prefer delegating to a repository layer when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "handler_or_view_owns_transaction_lifecycle",
      "label": "Handler or View Owns Transaction Lifecycle",
      "description": "Route handler or view manually manages database transaction boundaries (begin/commit/rollback).",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Handler or View Owns Transaction Lifecycle matters because the pattern here, Route handler or view manually manages database transaction boundaries (begin/commit/rollback), stretches the write boundary and makes failure ownership harder to reason about.",
      "fix": "For Handler or View Owns Transaction Lifecycle in Python, keep the transaction focused on the minimum database work and move unrelated orchestration outside the open transaction."
    },
    {
      "id": "initializer_requires_half_built_instance_state",
      "label": "Initializer Requires Half Built Instance State",
      "description": "Flag methods that depend on object fields being patched in after construction before the object becomes usable.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Initializer Requires Half Built Instance State matters because the pattern here, Flag methods that depend on object fields being patched in after construction before the object becomes usable, is worth review because soft module boundaries make it easy for transport, persistence, and business logic to blur together over time.",
      "fix": "For Initializer Requires Half Built Instance State in Python, choose one owner for mapping, orchestration, and data access, then keep each package aligned with that role."
    },
    {
      "id": "loop_run_until_complete_inside_running_loop",
      "label": "Loop Run Until Complete Inside Running Loop",
      "description": "loop.run_until_complete called from within a running event loop, which raises RuntimeError.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Loop Run Until Complete Inside Running Loop matters because the pattern here, loop.run_until_complete called from within a running event loop, which raises RuntimeError, turns local work into repeated cost that spreads with traffic or input size.",
      "fix": "For Loop Run Until Complete Inside Running Loop in Python, move one-time initialization, parsing, or allocation outside the loop when the per-iteration state does not actually change."
    },
    {
      "id": "module_exposes_mutable_singleton_client",
      "label": "Module Exposes Mutable Singleton Client",
      "description": "Flag modules that export long-lived mutable client instances for direct cross-module mutation.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Module Exposes Mutable Singleton Client matters because the pattern here, Flag modules that export long-lived mutable client instances for direct cross-module mutation, makes ownership and navigation harder when someone has to trace behavior across modules or services.",
      "fix": "For Module Exposes Mutable Singleton Client in Python, move the code to the package, module, router, or entrypoint layer that already owns this boundary so navigation and responsibility stay aligned."
    },
    {
      "id": "module_import_starts_runtime_bootstrap",
      "label": "Module Import Starts Runtime Bootstrap",
      "description": "Flag imports that eagerly start threads, background loops, watchers, or connection attempts at import time.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Module Import Starts Runtime Bootstrap matters because the pattern here, Flag imports that eagerly start threads, background loops, watchers, or connection attempts at import time, makes ownership and navigation harder when someone has to trace behavior across modules or services.",
      "fix": "For Module Import Starts Runtime Bootstrap in Python, move the code to the package, module, router, or entrypoint layer that already owns this boundary so navigation and responsibility stay aligned."
    },
    {
      "id": "multiprocessing_pool_created_without_context_manager_or_terminate",
      "label": "Multiprocessing Pool Created without Context Manager or Terminate",
      "description": "multiprocessing.Pool created without a context manager or explicit .terminate() call, leaking worker processes.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Multiprocessing Pool Created without Context Manager or Terminate matters because the pattern here, multiprocessing.Pool created without a context manager or explicit .terminate() call, leaking worker processes, removes a guardrail or signal that reviewers expect before trusting the surrounding code.",
      "fix": "For Multiprocessing Pool Created without Context Manager or Terminate in Python, add a context manager or explicit .terminate() call, leaking worker processes or move the code to a path where that safeguard is guaranteed before the risky work happens."
    },
    {
      "id": "object_construction_triggers_network_or_disk_side_effect",
      "label": "Object Construction Triggers Network or Disk Side Effect",
      "description": "Flag constructors that perform expensive I/O instead of separating configuration from execution.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Object Construction Triggers Network or Disk Side Effect matters because the pattern here, Flag constructors that perform expensive I/O instead of separating configuration from execution, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For Object Construction Triggers Network or Disk Side Effect in Python, prefer separating configuration from execution when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "orchestrator_performs_low_level_tokenization_or_parsing",
      "label": "Orchestrator Performs Low Level Tokenization or Parsing",
      "description": "Flag high-level workflow functions that also own low-level parsing logic better isolated in helpers.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Orchestrator Performs Low Level Tokenization or Parsing matters because the pattern here, Flag high-level workflow functions that also own low-level parsing logic better isolated in helpers, is worth review because soft module boundaries make it easy for transport, persistence, and business logic to blur together over time.",
      "fix": "For Orchestrator Performs Low Level Tokenization or Parsing in Python, choose one owner for mapping, orchestration, and data access, then keep each package aligned with that role."
    },
    {
      "id": "orm_model_mixes_domain_logic_and_persistence_mapping",
      "label": "ORM Model Mixes Domain Logic and Persistence Mapping",
      "description": "ORM model class contains domain business logic methods alongside column mapping, coupling persistence and domain concerns.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, ORM Model Mixes Domain Logic and Persistence Mapping matters because the pattern here, ORM model class contains domain business logic methods alongside column mapping, coupling persistence and domain concerns, concentrates too much responsibility into one shape and makes future change riskier.",
      "fix": "For ORM Model Mixes Domain Logic and Persistence Mapping in Python, split the class or constructor work so one object owns one role and collaborators are injected or composed deliberately."
    },
    {
      "id": "persistent_model_field_encodes_transport_concern",
      "label": "Persistent Model Field Encodes Transport Concern",
      "description": "SQLAlchemy or Django ORM model carries a field that encodes HTTP or transport metadata such as status codes.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Persistent Model Field Encodes Transport Concern matters because the pattern here, SQLAlchemy or Django ORM model carries a field that encodes HTTP or transport metadata such as status codes, lets layer-specific contracts leak into places that should stay simpler and easier to test.",
      "fix": "For Persistent Model Field Encodes Transport Concern in Python, choose one layer to own the contract shape and perform the mapping there instead of passing boundary-specific types through the stack."
    },
    {
      "id": "repository_method_accepts_pydantic_request_schema",
      "label": "Repository Method Accepts Pydantic Request Schema",
      "description": "Repository method parameter type is a Pydantic request schema that belongs to the presentation layer.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Repository Method Accepts Pydantic Request Schema matters because the pattern here, Repository method parameter type is a Pydantic request schema that belongs to the presentation layer, weakens the contract at the exact place callers need the shape or boundary to be explicit.",
      "fix": "For Repository Method Accepts Pydantic Request Schema in Python, tighten the type, annotation, schema, or response contract so callers can see the expected shape at the boundary."
    },
    {
      "id": "repository_returns_unexecuted_orm_query",
      "label": "Repository Returns Unexecuted ORM Query",
      "description": "Repository method returns a raw ORM query object instead of executing it and returning domain values.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Repository Returns Unexecuted ORM Query matters because the pattern here, Repository method returns a raw ORM query object instead of executing it and returning domain values, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For Repository Returns Unexecuted ORM Query in Python, prefer executing it and returning domain values when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "retry_policy_scattered_across_multiple_callers",
      "label": "Retry Policy Scattered Across Multiple Callers",
      "description": "Flag repeated retry or backoff loops around the same dependency instead of one owner boundary.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Retry Policy Scattered Across Multiple Callers matters because the pattern here, Flag repeated retry or backoff loops around the same dependency instead of one owner boundary, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For Retry Policy Scattered Across Multiple Callers in Python, prefer one owner boundary when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "semaphore_acquired_without_async_with_context_manager",
      "label": "Semaphore Acquired without Async with Context Manager",
      "description": "asyncio.Semaphore acquired via .acquire() without async with, risking a missed release on exception.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Semaphore Acquired without Async with Context Manager matters because the pattern here, asyncio.Semaphore acquired via .acquire() without async with, risking a missed release on exception, removes a guardrail or signal that reviewers expect before trusting the surrounding code.",
      "fix": "For Semaphore Acquired without Async with Context Manager in Python, add async with, risking a missed release on exception or move the code to a path where that safeguard is guaranteed before the risky work happens."
    },
    {
      "id": "service_method_accepts_http_request_object",
      "label": "Service Method Accepts HTTP Request Object",
      "description": "Service-layer method parameter list includes an HTTP request type from Flask, Django, or FastAPI.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Service Method Accepts HTTP Request Object matters because the pattern here, Service-layer method parameter list includes an HTTP request type from Flask, Django, or FastAPI, lets layer-specific contracts leak into places that should stay simpler and easier to test.",
      "fix": "For Service Method Accepts HTTP Request Object in Python, choose one layer to own the contract shape and perform the mapping there instead of passing boundary-specific types through the stack."
    },
    {
      "id": "service_method_returns_http_response_object",
      "label": "Service Method Returns HTTP Response Object",
      "description": "Service-layer method returns an HTTP response object that belongs to the transport layer.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Service Method Returns HTTP Response Object matters because the pattern here, Service-layer method returns an HTTP response object that belongs to the transport layer, lets layer-specific contracts leak into places that should stay simpler and easier to test.",
      "fix": "For Service Method Returns HTTP Response Object in Python, choose one layer to own the contract shape and perform the mapping there instead of passing boundary-specific types through the stack."
    },
    {
      "id": "service_raises_or_catches_http_exception_type",
      "label": "Service Raises or Catches HTTP Exception Type",
      "description": "Service-layer function raises or catches HTTP exception types that belong to the transport layer.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Service Raises or Catches HTTP Exception Type matters because the pattern here, Service-layer function raises or catches HTTP exception types that belong to the transport layer, lets layer-specific contracts leak into places that should stay simpler and easier to test.",
      "fix": "For Service Raises or Catches HTTP Exception Type in Python, choose one layer to own the contract shape and perform the mapping there instead of passing boundary-specific types through the stack."
    },
    {
      "id": "service_reads_settings_inline_instead_of_injected",
      "label": "Service Reads Settings Inline Instead of Injected",
      "description": "Service reads configuration from os.getenv or settings module inline rather than receiving it via injection.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Service Reads Settings Inline Instead of Injected matters because the pattern here, Service reads configuration from os.getenv or settings module inline rather than receiving it via injection, makes ownership and navigation harder when someone has to trace behavior across modules or services.",
      "fix": "For Service Reads Settings Inline Instead of Injected in Python, move the code to the package, module, router, or entrypoint layer that already owns this boundary so navigation and responsibility stay aligned."
    },
    {
      "id": "shared_mutable_collection_mutated_across_threads_without_lock",
      "label": "Shared Mutable Collection Mutated Across Threads without Lock",
      "description": "Shared mutable collection such as a list or dict is mutated from multiple threads without a lock.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Shared Mutable Collection Mutated Across Threads without Lock matters because the pattern here, Shared mutable collection such as a list or dict is mutated from multiple threads without a lock, removes a guardrail or signal that reviewers expect before trusting the surrounding code.",
      "fix": "For Shared Mutable Collection Mutated Across Threads without Lock in Python, add a lock or move the code to a path where that safeguard is guaranteed before the risky work happens."
    },
    {
      "id": "storage_write_returns_driver_specific_object",
      "label": "Storage Write Returns Driver Specific Object",
      "description": "Flag write helpers that return raw driver, session, or cursor objects instead of stable application data.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Storage Write Returns Driver Specific Object matters because the pattern here, Flag write helpers that return raw driver, session, or cursor objects instead of stable application data, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For Storage Write Returns Driver Specific Object in Python, prefer stable application data when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "sync_function_called_from_async_without_executor",
      "label": "Sync Function Called From Async without Executor",
      "description": "Blocking I/O or CPU-bound function called directly inside async def without loop.run_in_executor.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Sync Function Called From Async without Executor matters because the pattern here, Blocking I/O or CPU-bound function called directly inside async def without loop.run_in_executor, removes a guardrail or signal that reviewers expect before trusting the surrounding code.",
      "fix": "For Sync Function Called From Async without Executor in Python, add loop.run_in_executor or move the code to a path where that safeguard is guaranteed before the risky work happens."
    },
    {
      "id": "third_party_exception_type_leaks_across_architecture_boundary",
      "label": "Third Party Exception Type Leaks Across Architecture Boundary",
      "description": "Flag public or upper-layer APIs that expose low-level library exception types instead of stable local contracts.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Third Party Exception Type Leaks Across Architecture Boundary matters because the pattern here, Flag public or upper-layer APIs that expose low-level library exception types instead of stable local contracts, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For Third Party Exception Type Leaks Across Architecture Boundary in Python, prefer stable local contracts when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "thread_local_storage_read_from_async_function",
      "label": "Thread Local Storage Read From Async Function",
      "description": "Async function reads from threading.local() storage, which is per-thread not per-coroutine.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Thread Local Storage Read From Async Function matters because the pattern here, Async function reads from threading.local() storage, which is per-thread not per-coroutine, creates lifecycle behavior that is easy to miss until the system is under load or shutting down.",
      "fix": "For Thread Local Storage Read From Async Function in Python, give the work an explicit owner, pass the right context or handle, and make cancellation or shutdown visible in the API."
    },
    {
      "id": "threading_lock_acquired_blocking_inside_async_def",
      "label": "Threading Lock Acquired Blocking Inside Async Def",
      "description": "threading.Lock.acquire() called with blocking=True inside an async function, blocking the event loop thread.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Threading Lock Acquired Blocking Inside Async Def matters because the pattern here, threading.Lock.acquire() called with blocking=True inside an async function, blocking the event loop thread, turns local work into repeated cost that spreads with traffic or input size.",
      "fix": "For Threading Lock Acquired Blocking Inside Async Def in Python, move one-time initialization, parsing, or allocation outside the loop when the per-iteration state does not actually change."
    },
    {
      "id": "threading_thread_without_daemon_true_in_server_code",
      "label": "Threading Thread without Daemon True in Server Code",
      "description": "threading.Thread created in server-side code without daemon=True, which can block graceful shutdown.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Threading Thread without Daemon True in Server Code matters because the pattern here, threading.Thread created in server-side code without daemon=True, which can block graceful shutdown, removes a guardrail or signal that reviewers expect before trusting the surrounding code.",
      "fix": "For Threading Thread without Daemon True in Server Code in Python, add daemon=True, which can block graceful shutdown or move the code to a path where that safeguard is guaranteed before the risky work happens."
    },
    {
      "id": "transaction_scope_split_across_unrelated_helpers",
      "label": "Transaction Scope Split Across Unrelated Helpers",
      "description": "Flag flows where begin, commit, and rollback responsibilities are scattered across helpers with no single owner.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Transaction Scope Split Across Unrelated Helpers matters because the pattern here, Flag flows where begin, commit, and rollback responsibilities are scattered across helpers with no single owner, stretches the write boundary and makes failure ownership harder to reason about.",
      "fix": "For Transaction Scope Split Across Unrelated Helpers in Python, keep the transaction focused on the minimum database work and move unrelated orchestration outside the open transaction."
    },
    {
      "id": "untracked_create_task_result_may_hide_exception",
      "label": "Untracked Create Task Result May Hide Exception",
      "description": "asyncio.create_task result discarded without storing a reference; exceptions raised in the task are silenced.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Untracked Create Task Result May Hide Exception matters because the pattern here, asyncio.create_task result discarded without storing a reference; exceptions raised in the task are silenced, removes a guardrail or signal that reviewers expect before trusting the surrounding code.",
      "fix": "For Untracked Create Task Result May Hide Exception in Python, add storing a reference; exceptions raised in the task are silenced or move the code to a path where that safeguard is guaranteed before the risky work happens."
    },
    {
      "id": "validation_rules_duplicated_at_dto_and_domain_layer",
      "label": "Validation Rules Duplicated at DTO and Domain Layer",
      "description": "Identical validation constraint is applied at both the DTO/schema layer and the domain entity layer.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, Validation Rules Duplicated at DTO and Domain Layer matters because the pattern here, Identical validation constraint is applied at both the DTO/schema layer and the domain entity layer, weakens the contract at the exact place callers need the shape or boundary to be explicit.",
      "fix": "For Validation Rules Duplicated at DTO and Domain Layer in Python, tighten the type, annotation, schema, or response contract so callers can see the expected shape at the boundary."
    },
    {
      "id": "view_or_handler_constructs_orm_query_directly",
      "label": "View or Handler Constructs ORM Query Directly",
      "description": "View or route handler builds an ORM query rather than delegating to a repository or service.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, View or Handler Constructs ORM Query Directly matters because the pattern here, View or route handler builds an ORM query rather than delegating to a repository or service, blurs the boundary between request plumbing and the rest of the application.",
      "fix": "For View or Handler Constructs ORM Query Directly in Python, move the non-transport work into middleware, services, bootstrap code, or shared helpers so the request edge stays narrow."
    },
    {
      "id": "view_or_handler_performs_direct_file_system_io",
      "label": "View or Handler Performs Direct File System I/O",
      "description": "Route handler or view calls open(), os.path, or pathlib directly on the request path.",
      "defaultSeverity": "contextual",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Python, View or Handler Performs Direct File System I/O matters because the pattern here, Route handler or view calls open(), os.path, or pathlib directly on the request path, blurs the boundary between request plumbing and the rest of the application.",
      "fix": "For View or Handler Performs Direct File System I/O in Python, move the non-transport work into middleware, services, bootstrap code, or shared helpers so the request edge stays narrow."
    }
  ]
}
