{
  "language": "go",
  "family": "hot_path",
  "label": "Hot Path",
  "summary": "High-frequency request-path work that repeats avoidable computation.",
  "rules": [
    {
      "id": "append_then_sort_each_iteration",
      "label": "Append Then Sort Each Iteration",
      "description": "Slice sorting observed inside loops \u2014 often cheaper to sort once after the loop.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Append Then Sort Each Iteration matters because the pattern here, Slice sorting observed inside loops \u2014 often cheaper to sort once after the loop, pushes avoidable work into a repeated path where the cost compounds quickly.",
      "fix": "For Append Then Sort Each Iteration in Go, move the one-time work out of the repeated path, cache reusable state, or batch the operation so the loop only does per-item work."
    },
    {
      "id": "append_then_trim_each_iteration",
      "label": "Append Then Trim Each Iteration",
      "description": "Slice append followed by reslice each iteration instead of batching.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Append Then Trim Each Iteration matters because the pattern here, Slice append followed by reslice each iteration instead of batching, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For Append Then Trim Each Iteration in Go, prefer batching when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "bufio_reader_missing_for_small_read_loop",
      "label": "Bufio Reader Missing for Small Read Loop",
      "description": "File or socket reads inside loops without visible bufio buffering.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Bufio Reader Missing for Small Read Loop matters because the pattern here, File or socket reads inside loops without visible bufio buffering, pushes avoidable work into a repeated path where the cost compounds quickly.",
      "fix": "For Bufio Reader Missing for Small Read Loop in Go, move the one-time work out of the repeated path, cache reusable state, or batch the operation so the loop only does per-item work."
    },
    {
      "id": "bufio_writer_missing_in_bulk_export",
      "label": "Bufio Writer Missing in Bulk Export",
      "description": "File or socket writes inside loops without visible bufio buffering.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Bufio Writer Missing in Bulk Export matters because the pattern here, File or socket writes inside loops without visible bufio buffering, pushes avoidable work into a repeated path where the cost compounds quickly.",
      "fix": "For Bufio Writer Missing in Bulk Export in Go, move the one-time work out of the repeated path, cache reusable state, or batch the operation so the loop only does per-item work."
    },
    {
      "id": "builder_or_buffer_recreated_per_iteration",
      "label": "Builder or Buffer Recreated Per Iteration",
      "description": "strings.Builder, bytes.Buffer, or bytes.NewBuffer(...) constructions observed inside loops instead of being reset or reused.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Builder or Buffer Recreated Per Iteration matters because the pattern here, strings.Builder, bytes.Buffer, or bytes.NewBuffer(...) constructions observed inside loops instead of being reset or reused, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For Builder or Buffer Recreated Per Iteration in Go, prefer being reset or reused when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "byte_string_conversion_in_loop",
      "label": "Byte String Conversion in Loop",
      "description": "Byte-to-string or string-to-byte conversion observed inside loops in short-lived lookup or append paths.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Byte String Conversion in Loop matters because the pattern here, Byte-to-string or string-to-byte conversion observed inside loops in short-lived lookup or append paths, pushes avoidable work into a repeated path where the cost compounds quickly.",
      "fix": "For Byte String Conversion in Loop in Go, move the one-time work out of the repeated path, cache reusable state, or batch the operation so the loop only does per-item work."
    },
    {
      "id": "bytes_buffer_without_grow_known_bound",
      "label": "Bytes Buffer without Grow Known Bound",
      "description": "bytes.Buffer used without Grow when approximate output size is locally visible.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Bytes Buffer without Grow Known Bound matters because the pattern here, bytes.Buffer used without Grow when approximate output size is locally visible, removes a guardrail or signal that reviewers expect before trusting the surrounding code.",
      "fix": "For Bytes Buffer without Grow Known Bound in Go, add Grow when approximate output size is locally visible or move the code to a path where that safeguard is guaranteed before the risky work happens."
    },
    {
      "id": "bytes_split_same_input_multiple_times",
      "label": "Bytes Split Same Input Multiple Times",
      "description": "The same byte-slice input is passed through bytes.Split* or bytes.Fields* helpers multiple times in one function.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Bytes Split Same Input Multiple Times matters because the pattern here, The same byte-slice input is passed through bytes.Split* or bytes.Fields* helpers multiple times in one function, is worth review because tiny costs inside frequently executed code paths turn into real CPU and allocation bills at traffic scale.",
      "fix": "For Bytes Split Same Input Multiple Times in Go, move invariant work out of the hot path, reuse buffers or parsed state, and only do per-request work that must actually happen there."
    },
    {
      "id": "csv_writer_flush_per_row",
      "label": "Csv Writer Flush Per Row",
      "description": "csv.Writer.Flush() called inside per-row loops, reducing buffering effectiveness.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Csv Writer Flush Per Row matters because the pattern here, csv.Writer.Flush() called inside per-row loops, reducing buffering effectiveness, turns local work into repeated cost that spreads with traffic or input size.",
      "fix": "For Csv Writer Flush Per Row in Go, move one-time initialization, parsing, or allocation outside the loop when the per-iteration state does not actually change."
    },
    {
      "id": "filter_then_count_then_iterate",
      "label": "Filter Then Count Then Iterate",
      "description": "Same collection traversed multiple times for filter, count, and process steps.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Filter Then Count Then Iterate matters because the pattern here, Same collection traversed multiple times for filter, count, and process steps, is worth review because tiny costs inside frequently executed code paths turn into real CPU and allocation bills at traffic scale.",
      "fix": "For Filter Then Count Then Iterate in Go, move invariant work out of the hot path, reuse buffers or parsed state, and only do per-request work that must actually happen there."
    },
    {
      "id": "gzip_reader_writer_recreated_per_item",
      "label": "Gzip Reader Writer Recreated Per Item",
      "description": "gzip.NewReader(...) or gzip.NewWriter(...) recreated inside iterative paths instead of per stream.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Gzip Reader Writer Recreated Per Item matters because the pattern here, gzip.NewReader(...) or gzip.NewWriter(...) recreated inside iterative paths instead of per stream, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For Gzip Reader Writer Recreated Per Item in Go, prefer per stream when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "json_decoder_recreated_per_item",
      "label": "JSON Decoder Recreated Per Item",
      "description": "json.NewDecoder(...) constructed repeatedly inside loops instead of reusing a stable decoder per stream.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, JSON Decoder Recreated Per Item matters because the pattern here, json.NewDecoder(...) constructed repeatedly inside loops instead of reusing a stable decoder per stream, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For JSON Decoder Recreated Per Item in Go, prefer reusing a stable decoder per stream when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "json_encoder_recreated_per_item",
      "label": "JSON Encoder Recreated Per Item",
      "description": "json.NewEncoder(...) constructed repeatedly inside loops instead of reusing a stable encoder per stream.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, JSON Encoder Recreated Per Item matters because the pattern here, json.NewEncoder(...) constructed repeatedly inside loops instead of reusing a stable encoder per stream, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For JSON Encoder Recreated Per Item in Go, prefer reusing a stable encoder per stream when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "make_map_inside_hot_loop_same_shape",
      "label": "Make Map Inside Hot Loop Same Shape",
      "description": "make(map[K]V, ...) scratch maps recreated inside loops instead of being reused or prebuilt.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Make Map Inside Hot Loop Same Shape matters because the pattern here, make(map[K]V, ...) scratch maps recreated inside loops instead of being reused or prebuilt, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For Make Map Inside Hot Loop Same Shape in Go, prefer being reused or prebuilt when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "make_slice_inside_hot_loop_same_shape",
      "label": "Make Slice Inside Hot Loop Same Shape",
      "description": "make([]T, ...) scratch slices recreated inside loops instead of being reused.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Make Slice Inside Hot Loop Same Shape matters because the pattern here, make([]T, ...) scratch slices recreated inside loops instead of being reused, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For Make Slice Inside Hot Loop Same Shape in Go, prefer being reused when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "map_growth_without_size_hint",
      "label": "Map Growth without Size Hint",
      "description": "Map insertions inside loops without a visible size hint on the initial make call.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Map Growth without Size Hint matters because the pattern here, Map insertions inside loops without a visible size hint on the initial make call, pushes avoidable work into a repeated path where the cost compounds quickly.",
      "fix": "For Map Growth without Size Hint in Go, move the one-time work out of the repeated path, cache reusable state, or batch the operation so the loop only does per-item work."
    },
    {
      "id": "nested_append_without_outer_capacity",
      "label": "Nested Append without Outer Capacity",
      "description": "Append calls inside nested loops without visible preallocation on the outer slice.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Nested Append without Outer Capacity matters because the pattern here, Append calls inside nested loops without visible preallocation on the outer slice, removes a guardrail or signal that reviewers expect before trusting the surrounding code.",
      "fix": "For Nested Append without Outer Capacity in Go, add visible preallocation on the outer slice or move the code to a path where that safeguard is guaranteed before the risky work happens."
    },
    {
      "id": "nested_linear_join_map_candidate",
      "label": "Nested Linear Join Map Candidate",
      "description": "Nested-loop lookups or joins that could use a map index for O(1) access.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Nested Linear Join Map Candidate matters because the pattern here, Nested-loop lookups or joins that could use a map index for O(1) access, turns local work into repeated cost that spreads with traffic or input size.",
      "fix": "For Nested Linear Join Map Candidate in Go, move one-time initialization, parsing, or allocation outside the loop when the per-iteration state does not actually change."
    },
    {
      "id": "read_then_decode_duplicate_materialization",
      "label": "Read Then Decode Duplicate Materialization",
      "description": "io.ReadAll(...) materializes a payload and the same binding is then unmarshaled again instead of using a streaming decode path.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Read Then Decode Duplicate Materialization matters because the pattern here, io.ReadAll(...) materializes a payload and the same binding is then unmarshaled again instead of using a streaming decode path, shows that the code is bypassing a clearer or safer default that readers usually expect to see.",
      "fix": "For Read Then Decode Duplicate Materialization in Go, prefer using a streaming decode path when the semantics match, and keep the expected or cheaper API choice as the default."
    },
    {
      "id": "regexp_compile_in_hot_path",
      "label": "Regexp Compile in Hot Path",
      "description": "regexp.Compile or regexp.MustCompile observed inside obvious iterative paths.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Regexp Compile in Hot Path matters because the pattern here, regexp.Compile or regexp.MustCompile observed inside obvious iterative paths, is worth review because tiny costs inside frequently executed code paths turn into real CPU and allocation bills at traffic scale.",
      "fix": "For Regexp Compile in Hot Path in Go, move invariant work out of the hot path, reuse buffers or parsed state, and only do per-request work that must actually happen there."
    },
    {
      "id": "repeated_map_clone_in_loop",
      "label": "Repeated Map Clone in Loop",
      "description": "maps.Clone or equivalent map-copy calls observed inside loops.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Repeated Map Clone in Loop matters because the pattern here, maps.Clone or equivalent map-copy calls observed inside loops, pushes avoidable work into a repeated path where the cost compounds quickly.",
      "fix": "For Repeated Map Clone in Loop in Go, move the one-time work out of the repeated path, cache reusable state, or batch the operation so the loop only does per-item work."
    },
    {
      "id": "repeated_slice_clone_in_loop",
      "label": "Repeated Slice Clone in Loop",
      "description": "slices.Clone(...) or similar whole-slice cloning observed inside loops.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Repeated Slice Clone in Loop matters because the pattern here, slices.Clone(...) or similar whole-slice cloning observed inside loops, pushes avoidable work into a repeated path where the cost compounds quickly.",
      "fix": "For Repeated Slice Clone in Loop in Go, move the one-time work out of the repeated path, cache reusable state, or batch the operation so the loop only does per-item work."
    },
    {
      "id": "slice_append_without_prealloc_known_bound",
      "label": "Slice Append without Prealloc Known Bound",
      "description": "Slice append inside a range loop without visible preallocation when the bound is locally known.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Slice Append without Prealloc Known Bound matters because the pattern here, Slice append inside a range loop without visible preallocation when the bound is locally known, removes a guardrail or signal that reviewers expect before trusting the surrounding code.",
      "fix": "For Slice Append without Prealloc Known Bound in Go, add visible preallocation when the bound is locally known or move the code to a path where that safeguard is guaranteed before the risky work happens."
    },
    {
      "id": "slice_membership_in_loop_map_candidate",
      "label": "Slice Membership in Loop Map Candidate",
      "description": "slices.Contains(...) or slices.Index(...) used inside loops against a stable-looking slice binding.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Slice Membership in Loop Map Candidate matters because the pattern here, slices.Contains(...) or slices.Index(...) used inside loops against a stable-looking slice binding, pushes avoidable work into a repeated path where the cost compounds quickly.",
      "fix": "For Slice Membership in Loop Map Candidate in Go, move the one-time work out of the repeated path, cache reusable state, or batch the operation so the loop only does per-item work."
    },
    {
      "id": "sort_before_first_or_membership_only",
      "label": "Sort Before First or Membership Only",
      "description": "Sorting a collection when only the first element or min/max is needed.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Sort Before First or Membership Only matters because the pattern here, Sorting a collection when only the first element or min/max is needed, is worth review because tiny costs inside frequently executed code paths turn into real CPU and allocation bills at traffic scale.",
      "fix": "For Sort Before First or Membership Only in Go, move invariant work out of the hot path, reuse buffers or parsed state, and only do per-request work that must actually happen there."
    },
    {
      "id": "stable_value_normalization_in_inner_loop",
      "label": "Stable Value Normalization in Inner Loop",
      "description": "Stable value normalization (ToLower, TrimSpace, etc.) repeated inside inner loops.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Stable Value Normalization in Inner Loop matters because the pattern here, Stable value normalization (ToLower, TrimSpace, etc.) repeated inside inner loops, turns local work into repeated cost that spreads with traffic or input size.",
      "fix": "For Stable Value Normalization in Inner Loop in Go, move one-time initialization, parsing, or allocation outside the loop when the per-iteration state does not actually change."
    },
    {
      "id": "strconv_repeat_on_same_binding",
      "label": "Strconv Repeat on Same Binding",
      "description": "The same string binding is converted with strconv parsing helpers multiple times in one function.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Strconv Repeat on Same Binding matters because the pattern here, The same string binding is converted with strconv parsing helpers multiple times in one function, is worth review because tiny costs inside frequently executed code paths turn into real CPU and allocation bills at traffic scale.",
      "fix": "For Strconv Repeat on Same Binding in Go, prefer a builder, buffer, or one-time composition step instead of growing the same value incrementally."
    },
    {
      "id": "strings_builder_without_grow_known_bound",
      "label": "Strings Builder without Grow Known Bound",
      "description": "strings.Builder used without Grow when approximate output size is locally visible.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Strings Builder without Grow Known Bound matters because the pattern here, strings.Builder used without Grow when approximate output size is locally visible, removes a guardrail or signal that reviewers expect before trusting the surrounding code.",
      "fix": "For Strings Builder without Grow Known Bound in Go, add Grow when approximate output size is locally visible or move the code to a path where that safeguard is guaranteed before the risky work happens."
    },
    {
      "id": "strings_split_same_input_multiple_times",
      "label": "Strings Split Same Input Multiple Times",
      "description": "The same string input is passed through strings.Split* or strings.Fields* helpers multiple times in one function.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Strings Split Same Input Multiple Times matters because the pattern here, The same string input is passed through strings.Split* or strings.Fields* helpers multiple times in one function, is worth review because tiny costs inside frequently executed code paths turn into real CPU and allocation bills at traffic scale.",
      "fix": "For Strings Split Same Input Multiple Times in Go, prefer a builder, buffer, or one-time composition step instead of growing the same value incrementally."
    },
    {
      "id": "template_parse_in_hot_path",
      "label": "Template Parse in Hot Path",
      "description": "html/template or text/template parse calls observed on request-style paths instead of startup-time caching.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Template Parse in Hot Path matters because the pattern here, html/template or text/template parse calls observed on request-style paths instead of startup-time caching, pushes configuration or startup ownership into the wrong phase of the program.",
      "fix": "For Template Parse in Hot Path in Go, load, validate, and wire this configuration at startup so request paths and imports only consume already-owned state."
    },
    {
      "id": "time_parse_layout_in_loop",
      "label": "Time Parse Layout in Loop",
      "description": "time.Parse(...) or ParseInLocation(...) observed inside loops with a stable layout.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, Time Parse Layout in Loop matters because the pattern here, time.Parse(...) or ParseInLocation(...) observed inside loops with a stable layout, pushes avoidable work into a repeated path where the cost compounds quickly.",
      "fix": "For Time Parse Layout in Loop in Go, move the one-time work out of the repeated path, cache reusable state, or batch the operation so the loop only does per-item work."
    },
    {
      "id": "url_parse_in_loop_on_invariant_base",
      "label": "URL Parse in Loop on Invariant Base",
      "description": "url.Parse(...) or ParseRequestURI(...) observed inside loops with a stable-looking base input.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, URL Parse in Loop on Invariant Base matters because the pattern here, url.Parse(...) or ParseRequestURI(...) observed inside loops with a stable-looking base input, pushes avoidable work into a repeated path where the cost compounds quickly.",
      "fix": "For URL Parse in Loop on Invariant Base in Go, move the one-time work out of the repeated path, cache reusable state, or batch the operation so the loop only does per-item work."
    },
    {
      "id": "uuid_hash_formatting_only_for_logs",
      "label": "UUID Hash Formatting Only for Logs",
      "description": "UUID or hash formatting observed inside loops only for log output.",
      "defaultSeverity": "info",
      "status": "stable",
      "configurability": [
        "disable",
        "ignore",
        "severity_override"
      ],
      "explanation": "In Go, UUID Hash Formatting Only for Logs matters because the pattern here, UUID or hash formatting observed inside loops only for log output, pushes avoidable work into a repeated path where the cost compounds quickly.",
      "fix": "For UUID Hash Formatting Only for Logs in Go, move the one-time work out of the repeated path, cache reusable state, or batch the operation so the loop only does per-item work."
    }
  ]
}
